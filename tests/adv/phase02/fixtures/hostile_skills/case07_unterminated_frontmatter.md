---
id: hostile-unterminated
applies_to_tasks: ["*"]
applies_to_languages: ["*"]
Body — no closing fence anywhere; the scanner walks to the cap and returns
None, surfaced as `frontmatter_unterminated`.
