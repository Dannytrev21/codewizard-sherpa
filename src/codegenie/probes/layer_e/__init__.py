"""Layer-E probes (cross-repo / operational) — S6-05.

One real probe (``OwnershipProbe`` — CODEOWNERS parser) plus two
deferred stubs (``ServiceTopologyStubProbe``, ``SloStubProbe``) whose
real data sources are Phase-9-or-later. See ``docs/localv2.md §5.5``
and ``docs/phases/02-context-gather-layers-b-g/stories/S6-05-*``.
"""
