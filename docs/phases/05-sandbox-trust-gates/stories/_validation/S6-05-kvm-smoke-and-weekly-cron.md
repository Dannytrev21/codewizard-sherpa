# Validation report — Story S6-05 `firecracker-smoke` KVM-gated CI integration tests + weekly cron

**Date:** 2026-05-25
**Validator:** `phase-story-validator` (single-agent inline mode)
**Validator agent run:** automated (`story-validation-corrector` scheduled task)
**Verdict:** **HARDENED**

## Context brief

S6-05 is the **last** story in Phase 5's Step 6 substream: it lands the two KVM-gated integration tests (`test_firecracker_smoke.py`, `test_firecracker_network_policy.py`), wires the self-hosted KVM runner workflow (`.github/workflows/firecracker-smoke.yml`), and stands up the weekly cron with on-call paging. It is the gate-keeping evidence for production [ADR-0019](../../../../production/adrs/0019-sandbox-stack.md) (sandbox stack resolution) and the tripwire that catches rootfs / kernel / digest drift before it reaches an operator.

Read for context:
- `docs/phases/05-sandbox-trust-gates/phase-arch-design.md` §Goal 6 (line 21), §Component design `FirecrackerClient` (line 495–502, performance envelope = 300 s), §Physical view (line 314–347, Linux CI self-hosted KVM runner), §Testing strategy (line 874–930, `pytest.mark.skip_if_no_kvm` predicate; integration ~25%), §Open Q6 (line 1061, weekly Firecracker cron infra ownership = blocker), §Data model (line 760–795, `Attempt` JSONL line shape; `backend` + `gate_isolation_class` live on `SandboxRun`, not on `Attempt`).
- `docs/phases/05-sandbox-trust-gates/High-level-impl.md` §Step 6 (line 152–178) — the 6a/6b split mitigation if the KVM runner is not provisioned.
- `docs/phases/05-sandbox-trust-gates/ADRs/0004-dind-default-macos-with-gate-isolation-class.md` §Consequences (line 36) — verbatim "`tests/integration/sandbox/test_firecracker_smoke.py` is `pytest.mark.skip_if_no_kvm`; weekly cron job exercises it on the self-hosted runner".
- `docs/phases/05-sandbox-trust-gates/ADRs/0009-firecracker-network-policy-host-side-nftables.md` §Consequences (line 37) — `test_firecracker_network_policy.py` is `skip_if_no_kvm`.
- `docs/phases/05-sandbox-trust-gates/ADRs/0001-two-chokepoint-sandbox-seam.md` — `GateRunner` is the only consumer of `SandboxClient`; the smoke test exercises a real gate, not a raw `client.execute`.
- `docs/phases/05-sandbox-trust-gates/stories/_validation/S6-01-firecracker-client-kvm-boot.md` HARDENED — `SandboxSpec.logs_dir` / `copy_out_root` are **phantom fields** (S1-02 puts them on `SandboxRun`); required `SandboxSpec` fields are `pids_limit`, `base_image`, `enable_trace`, `copy_out`, `label`, `sandbox_spec_hash` (`extra="forbid"` rejects any extras); `_valid_spec_kwargs` is the canonical fixture helper imported from `tests/sandbox/test_contract_models.py`; AC-2 puts `backend` and `gate_isolation_class` on **`SandboxRun`**, written to `.codegenie/sandbox/runs/<sandbox_run_id>/run.json`, not on the `Attempt` row.
- `docs/phases/05-sandbox-trust-gates/stories/_validation/S6-02-firecracker-nftables-policy-gap-4.md` HARDENED — `apply_policy(spec, *, run_id, runner=..., resolver=..., clock=...)` returns a `NetNamespaceConfig` with `teardown()`; nftables table name is `inet cgsbx_<run_id[:12]>`, TAP name is `cgsbx-<run_id[:8]>`; `EVENT_SANDBOX_FIRECRACKER_TAP_ORPHAN = "sandbox.firecracker.tap.orphan"` consumed by `sandbox gc` (S8-01). The S6-02 integration test placeholder uses ONLY `@pytest.mark.skip_if_no_kvm` (no internal `pytest.skip`) — S6-05 lands the real assertions by editing the test body.
- `docs/phases/05-sandbox-trust-gates/stories/_validation/S6-03-rootfs-digests-and-prepare.md` HARDENED — **`FirecrackerClient.from_digests_yaml()` is REMOVED** (AC-FACTORY-5); the canonical construction is `FirecrackerClient.from_pinned_digests(load_pinned_digests(digests_yaml=...), artifacts_root=..., api_socket_factory=..., process_handle_factory=..., vsock_exec_port=..., clock=...)`. `codegenie sandbox prepare --backend firecracker --check` (read-from-disk verify; ≤ 5 s; ≤ 5 s is the PR-time fence) vs `--check-rebake` (re-bake to tmpdir; ≤ 6 min; operator-bump verification). `find_project_root() -> Path` is the load-bearing helper at `src/codegenie/_paths.py`.
- `docs/phases/05-sandbox-trust-gates/stories/_validation/S6-04-auto-detect-macos-fallback.md` HARDENED — reason strings are namespaced via the regex `^[a-z][a-z0-9_]*\.[a-z][a-z0-9_]*$`; event constants live in `sandbox/logging.py` alphabetized in sorted `__all__`; the `STARTED/COMPLETED/FAILED` verb convention from S1-01 HARDENED is mandatory.
- `docs/phases/05-sandbox-trust-gates/stories/_validation/S1-01-…` HARDENED — `STARTED/COMPLETED/FAILED` event-verb canonical-table.
- CLAUDE.md commitments: warning/error-ID regex; "Functional core / imperative shell"; "Extension by addition — no silent edits" (ADR-0043); "Match the existing convention"; "Tests verify intent, not just behavior" (Rule 9); "Fail loud" (Rule 12).

Pattern-lineage: S6-05 is the **seventh** consumer in Phase 5 of the Hexagonal-DI / FCS / closed-Literal-`reason` / `_BACKEND_NAME`-`Final` / module-purity-AST-walker / `STARTED/COMPLETED/FAILED` canonical-events stack (S3-01 / S3-02 / S3-03 / S6-01 / S6-02 / S6-03 / S6-04 each shipped explicit consumers). It is also the **first** consumer that lands a *CI workflow file* — the pattern lineage for `.github/workflows/*.yml` discipline is therefore established here (paths-filter completeness, `concurrency:` group, runner-label specificity, secret-handling) and will be re-used by every subsequent CI-touching story in this and later phases.

## Four-critic findings (inline-synthesized)

### Coverage critic

| # | Severity | Finding |
|---|---|---|
| C-1 | **block** | Acceptance criteria are bare unnumbered checkboxes; every sibling HARDENED story in this phase (S1-02 / S1-05 / S3-02 / S6-01 / S6-02 / S6-03 / S6-04) carries `AC-XX-N` IDs. Reviewers + the executor's Ralph-Wiggum validator cannot reference items unambiguously without IDs. |
| C-2 | **block** | **Open Q6 is not actually closed.** AC-9 (and Implementation §5) claim the README "documents the rotation owner — Open Q6 is closed here", but the prescribed workflow YAML has the literal placeholder `<owner>` and the prescribed PagerDuty action is the placeholder `PagerDuty/pagerduty-change-events-action@v1   # replace with org-standard action`. A "closed" Open Question with two unfilled placeholders is a stub-closure that contradicts ADR-0043 ("no silent edits") and arch §Risks risk-2 ("6b is required for §Goal 6"). The story must either (a) name the owner + action concretely (operational deliverable — out of this story's scope per the 6a/6b split) OR (b) mark Open Q6 as **deferred to 6b** with the operational gating criterion explicit. The story must NOT claim closure while leaving placeholders. |
| C-3 | **block** | **No `concurrency:` group on the workflow.** A self-hosted KVM runner has finite capacity (typically one microVM at a time). `pull_request` + `workflow_dispatch` + `schedule` triggers can fire concurrently; without a `concurrency.group:` declaration two workflow runs can race on the same runner, leak nftables tables (S6-02 AC-TEARDOWN-1..-4), and contaminate the perf JSONL. Resolution: AC-CONC-1 pins `concurrency: { group: firecracker-smoke, cancel-in-progress: false }` (do NOT cancel in progress — a cron run cancelled by a late PR loses cron evidence; queue instead). |
| C-4 | **block** | **Workflow `paths-filter` is incomplete.** Draft lists `src/codegenie/sandbox/firecracker/**`, `tools/firecracker/**`, `tools/digests.yaml` — but a PR that edits *only* `tests/integration/sandbox/test_firecracker_smoke.py` or `.github/workflows/firecracker-smoke.yml` itself would not trigger the workflow. The smoke's own behavior is then not gated by the smoke. Resolution: AC-PATHS-1 extends the filter to `tests/integration/sandbox/test_firecracker_*.py` AND `.github/workflows/firecracker-smoke.yml` AND `src/codegenie/sandbox/firecracker/network_policy.py` AND `src/codegenie/gates/runner.py` AND `src/codegenie/gates/catalog/stage6_validate.yaml`. |
| C-5 | **block** | **24h queue-threshold alert (AC-10) is prose-only — no implementation.** "PagerDuty's queue-monitor mechanism" is not a default. Either (a) the workflow itself ships a separate scheduled "queue-watcher" job that queries the GitHub API for queued runs > 24h and fires a PagerDuty incident, or (b) the AC is explicitly deferred to 6b ops. Resolution: AC-QUEUE-1..-3 split — ship a `firecracker-queue-watcher.yml` companion workflow (cron-only, runs on `ubuntu-latest`, no KVM dep) that queries `gh api repos/{owner}/{repo}/actions/workflows/firecracker-smoke.yml/runs --jq '[.workflow_runs[]|select(.status=="queued" and ((now - (.created_at|fromdateiso8601)) > 86400))]'`; on non-empty result, fires the same PagerDuty action with reason `sandbox.ci.firecracker_queue_stalled`. OR, deferred to 6b. Pick one; the AC must not stay vague. |
| C-6 | **harden** | No AC pins runner-identity capture into the perf JSONL. If the self-hosted runner is silently replaced (e.g., kernel rebuild), the latency baseline shifts without provenance. Resolution: AC-PROV-1 — perf JSONL line schema widens to `{run_id, backend, wall_seconds, exit_code, run_ts, runner_name, runner_os, kernel_release, kvm_module_version}`; captured via `socket.gethostname()`, `platform.platform()`, `os.uname().release`, and `cat /sys/module/kvm/version` (or `modinfo kvm \| grep ^version`) at fixture entry. |
| C-7 | **harden** | No fence test prevents accidental removal of the `@pytest.mark.skip_if_no_kvm` decorator. A contributor on a macOS laptop pushing a refactor that drops the decorator would silently turn the tests into hard failures on every macOS dev box (and on every standard `ubuntu-24.04` runner). Mirrors the S6-02 AC-INTEG-1 discipline. Resolution: AC-FENCE-1 — `tests/schema/test_kvm_gated_tests_carry_marker.py` AST-walks `tests/integration/sandbox/` for any `test_firecracker_*.py`, asserts the `pytestmark` module-level assignment includes the `skip_if_no_kvm` marker (no decorator-on-function-only allowed — module-level only). Planted-positive flips the marker off and asserts the fence fires. |
| C-8 | **harden** | No AC for **pre-test resource isolation**. The hello-node fixture (`tests/fixtures/repos/hello-node`) carries a committed `node_modules/` from prior phases (Phase 3/4 carry-forward). `npm ci` is contractually a delete-and-reinstall but the network-policy test's mutation-thinking failure mode (T-3) means a cached `~/.npm` directory on the runner could let `npm ci` succeed even with a totally broken nftables ruleset. Resolution: AC-ISO-1..-3 — workflow step `rm -rf ~/.npm /tmp/npm-cache-* /tmp/cgsbx-*` runs as a pre-step; the test fixture is copied to `tmp_path` (never used in-tree); `npm ci` inside the microVM passes `--cache /tmp/empty-cache` to defeat any cache pre-population. |
| C-9 | **harden** | No AC enforces 300 s budget across **multiple** runs (mitigates single-run-variance false negative). Resolution: AC-PERF-1..-3 — the perf JSONL is the recording surface; the 300 s assertion is a hard ceiling for the single run; an additional `tests/perf/test_firecracker_smoke_p95.py` (skip_if_no_kvm, `pytest.mark.slow`, runs nightly via the perf workflow not the smoke workflow) reads the last 30 JSONL lines and asserts `p95 ≤ 300` AND `p50 ≤ 240`. This is the **input** S7-02 will gate on. |
| C-10 | **harden** | No AC for **PagerDuty-step coverage**: the workflow's `if: failure() && github.event_name == 'schedule'` is a load-bearing condition; if a typo turns it into `||` or omits the schedule guard, *every* PR failure pages on-call. No test verifies this. Resolution: AC-PD-TEST-1 — `tests/ci/test_firecracker_smoke_workflow.py` parses the YAML and asserts: (a) the page step's `if:` literal is `failure() && github.event_name == 'schedule'`; (b) the action `uses:` line is a pinned SHA, not `@v1` (supply-chain hygiene); (c) `concurrency.group:` is present and non-templated. |
| C-11 | **nit** | Module-purity AC not applicable here (these are tests + workflows, not production modules); explicit acknowledgement avoids the "is the pattern inherited?" debate. Resolution: Validation notes record explicit non-inheritance. |
| C-12 | **nit** | No AC for the README-block file shape (`.yml.README.md` is unusual). Phase-arch testing-strategy precedent uses workflow-header docstrings. Resolution: AC-DOC-1 — pin a top-of-workflow `# === Documentation ===` block (lines 1–25) covering runner-label requirement, PagerDuty service routing, rotation-owner reference (the actual owner lives in a separate ops doc to keep secrets out of the workflow YAML), cron rationale; remove the `.yml.README.md` sibling file. |

### Test-quality critic

| # | Severity | Finding |
|---|---|---|
| T-1 | **block** | **`FirecrackerClient.from_digests_yaml()` was REMOVED by S6-03 HARDENED.** Both test bodies in the draft (lines 154, 166, 195, 202) call `FirecrackerClient.from_digests_yaml()`. The canonical construction is `FirecrackerClient.from_pinned_digests(load_pinned_digests(digests_yaml=find_project_root()/"tools/digests.yaml"), artifacts_root=find_project_root()/"tools/firecracker", api_socket_factory=..., process_handle_factory=..., vsock_exec_port=..., clock=...)`. Following the draft literally is a compile-time error. Resolution: AC-CONSTRUCT-1..-3 require the canonical factory invocation and provide a `_kvm_smoke_client_factory()` test helper at `tests/integration/sandbox/_helpers.py` so both tests construct identically. The helper resolves DI ports via real defaults (KVM-only runner — real ports are fine). |
| T-2 | **block** | **`SandboxSpec(logs_dir=..., copy_out_root=...)` are phantom fields.** Per S1-02 + S6-01 HARDENED, both belong on `SandboxRun`, not `SandboxSpec`; `extra="forbid"` rejects construction. Draft test lines 204–210 + 214 (the `model_copy(update={... logs_dir, copy_out_root ...})`) would `ValidationError` on construction. Resolution: AC-SPEC-1 imports `_valid_spec_kwargs` from `tests/sandbox/test_contract_models.py` (the S1-02 canonical helper); the test uses `_valid_spec_kwargs(network="scoped", egress_allowlist=("registry.npmjs.org",), cmd=("sh","-c","npm ci --silent"))` overrides. `logs_dir` and `copy_out_root` are read from the resulting `SandboxRun`, never set on `SandboxSpec`. |
| T-3 | **block** | **Required `SandboxSpec` fields omitted from every fixture.** Per S1-02 + S6-01 HARDENED, `pids_limit`, `base_image`, `enable_trace`, `copy_out`, `label`, `sandbox_spec_hash` have no defaults. Draft `SandboxSpec(cmd=..., copy_in=..., logs_dir=..., copy_out_root=..., time_budget_seconds=180, memory_limit_mib=1024, network="scoped", egress_allowlist=["registry.npmjs.org"], env={"PATH": "..."})` `ValidationError`s. Resolution: same as T-2 — use `_valid_spec_kwargs`. |
| T-4 | **block** | **`attempts.jsonl` schema mis-read.** Draft asserts `last["signals"]["backend"] == "firecracker"` and `last["signals"]["gate_isolation_class"] == "microvm"`. Per arch §Data model (line 766–778), `Attempt` carries `signals: ObjectiveSignals` plus `attempt_id`, `sandbox_run_id`, `outcome`, `started_at`, `ended_at`, `prev_hash`, `chain_hash`. Per arch §Component design (line 110–111) `backend` and `gate_isolation_class` are on `SandboxRun`, which the orchestrator persists at `.codegenie/sandbox/runs/<sandbox_run_id>/run.json` (S6-01 AC-A3). To verify these from the JSONL, the test must (a) parse `last["sandbox_run_id"]`, (b) load `.codegenie/sandbox/runs/<sandbox_run_id>/run.json`, (c) assert `run.backend == "firecracker"` and `run.gate_isolation_class == "microvm"`. Reading them off `signals` would silently pass against a totally broken adapter. Resolution: AC-RUN-LOOKUP-1..-3 pin the two-file lookup; `tests/integration/sandbox/_helpers.py::load_sandbox_run(run_dir, attempts_line)` does the join. |
| T-5 | **block** | **Mutation-thinking on the network-policy test fails.** `npm ci` against hello-node passes the "allowlist permits npm" assertion **even if the nftables ruleset is empty / wide-open / missing entirely**, because (a) `~/.npm` on the runner may carry a cached registry mirror that satisfies the install offline, OR (b) the no-policy default in a Firecracker microVM is *no network at all* (if S6-02's `apply_policy` silently returned a no-op `NetNamespaceConfig`, `npm ci` would still fail closed but the github.com block would also "succeed" — both for the wrong reason). Resolution: AC-MUT-1..-3 — (1) workflow pre-step `rm -rf ~/.npm` (also covers C-8); (2) the network-policy test asserts a third probe `curl -sf https://registry.npmjs.org/ -o /dev/null` *succeeds with exit 0* AND `curl -sf https://github.com -o /dev/null` *fails* AND `curl -sf https://1.1.1.1 -o /dev/null` *fails* — three positives + two negatives makes the ruleset's selectivity observable; (3) a fourth assertion reads `client.execute(spec_curl_npm).run.signals.network_policy` (S6-02 AC-12 — the structured event) and verifies the policy tag matches `"scoped"`. |
| T-6 | **block** | **Tests over-broad `nft list tables` post-condition.** Draft asserts `"cgsbx-" not in out` against the *entire* `nft list tables` output. If a concurrent run (C-3) or a leaked table from an earlier run is present, this masks our table's leak (assertion fails because of the OTHER table) — or, if our cleanup hasn't happened yet (no synchronization), we false-positive on a still-present table. Per S6-02 AC-TEARDOWN-1..-4, our table is uniquely named `inet cgsbx_<run_id[:12]>`. Resolution: AC-NFT-1..-3 — assert *our specific* table name is absent: `subprocess.check_output(["nft","list","tables","inet"])` returns the textual table list; for the `run_id` we used, `f"cgsbx_{run_id[:12]}"` does NOT appear. The autouse fixture (T-9) tears down our table at module exit regardless of test outcome. |
| T-7 | **block** | **300 s wall-clock assertion is a single-sample test.** A single high-variance sample is the worst kind of perf test (false-failure cascade in CI). Resolution: AC-PERF-4 — the wall-clock assertion is split: (a) hard ceiling 300 s in `test_firecracker_smoke.py` (a single failure here is a real regression — investigate); (b) the p95 / p50 trend assertion lives in `tests/perf/test_firecracker_smoke_p95.py` (C-9); (c) the inline test additionally logs `wall_seconds_p95_last_30_runs` to the JSONL so a single-sample failure carries its own baseline context for the on-call. |
| T-8 | **block** | **PagerDuty step `uses:` line is unpinned + version-mutable.** `PagerDuty/pagerduty-change-events-action@v1` is (a) the wrong action class (`change-events` is for deployment annotations, NOT incidents; the incident-create action is `PagerDuty/pagerduty-incident-create-action` or the org-standard `pagerduty/alerts-action`), (b) `@v1` is a moving tag — supply-chain hygiene requires a pinned SHA. Resolution: AC-PD-1..-3 — (1) replace with the org's standard PagerDuty incident-create action; (2) pin the action to a SHA (not `@v1`/`@v2`); (3) the integration-key secret name is `PAGERDUTY_KVM_RUNNER_SERVICE_KEY` (or the org standard); (4) `severity: error` is replaced with the action's documented severity enum (most use `critical` / `error` / `warning` / `info`; the action picked determines the value); (5) Open Q6 6b explicitly captures the action choice + the SHA pin. |
| T-9 | **harden** | **Autouse fixture for perf JSONL emit lives in Refactor (TDD plan §Refactor) but per-test `try/finally` lives in Implementation §2.** Pick ONE. The autouse fixture is the right shape — it covers both tests on day 1, including failure paths (failure recording is the whole point — a failed smoke run with no JSONL is missing evidence). Resolution: AC-PERF-EMIT-1 — autouse fixture from day 1 (not Refactor); per-test `try/finally` removed from Implementation §2. |
| T-10 | **harden** | **`StrictAndGate.from_yaml(...)` factory is invented.** Arch + S5-02 surface is `Gate` ABC + `StrictAndGate` class; no `from_yaml` classmethod is in the arch. The Implementation §2 line "or, if that is too coupled, exercise `client.execute(spec)` directly" admits the test is contract-uncertain. Resolution: AC-GATE-1..-3 — the test imports the canonical `StrictAndGate` constructor (per S5-02 HARDENED — likely `StrictAndGate(required_signals=..., retry_policy=...)` or a `gate_definition_from_yaml(path) -> GateDefinition` helper); if S5-02's surface is `gate_runner.factories.gate_from_yaml(path) -> Gate`, use that. The story's TDD plan must reference the EXACT S5-02 HARDENED factory; until that's verified, the AC is conditional and the executor MUST verify against the S5-02 HARDENED report before writing the test. |
| T-11 | **harden** | **`GateContext(worktree=..., run_id=...)` arity unverified.** Per arch line 163–169, `GateContext` has more fields than `worktree` + `run_id`; per arch line 169 it also has `with_prior_attempt(o)`. The exact constructor signature is owned by S1-04 (gates contract); the test must use whatever signature S1-04 HARDENED pinned. Resolution: AC-CTX-1 — the executor reads S1-04 (and its `_validation/` report if present) and constructs `GateContext` per that contract; the TDD plan calls this out as a verify-before-write. |
| T-12 | **harden** | **`RetryLedger(run_dir=..., gate_id=..., prev_chain_head=None)` arity unverified.** Same as T-11 — S2-01 owns the `RetryLedger` contract. Resolution: AC-LEDGER-1 — verify against S2-01 HARDENED before write. |
| T-13 | **harden** | **No assertion of perf-JSONL append-only / one-row-per-run semantics.** Resolution: AC-JSONL-1 — fixture asserts the file is opened in `"a"` mode, the line is a complete JSON object terminated by `\n`, and after the test the line count equals the pre-test count + 1. Property: two consecutive test invocations append exactly two lines. |
| T-14 | **harden** | **`test_firecracker_smoke.py` has no failure-path coverage.** The 300 s budget assertion only fires when `outcome.passed`. If `outcome.passed == False` *and* `elapsed > 300`, both fail simultaneously, masking ordering. Resolution: AC-FAIL-PATH-1 — assert `outcome.passed` FIRST with `outcome.summary` in the message; assert wall-clock SECOND (so the failure message points at the right cause). |
| T-15 | **nit** | **No `pyproject.toml` change confirmation.** Resolution: AC-DEP-1 — no new dependencies (stdlib `subprocess`, `time`, `socket`, `platform`, `os`, `json`; already-in-closure `pytest`, `pydantic`). PagerDuty action is a GitHub Actions dependency, not a Python one — no `pyproject.toml` impact. |

### Consistency critic

| # | Severity | Finding |
|---|---|---|
| K-1 | **block** | **Cross-story consistency: `from_digests_yaml` is removed (S6-03 HARDENED AC-FACTORY-5).** See T-1 — same finding viewed through the Consistency lens. Following the draft literally regresses S6-03 by re-introducing a removed factory. Resolution: AC-CONSTRUCT-1..-3 use `from_pinned_digests` + `load_pinned_digests`. |
| K-2 | **block** | **References error: `phase-arch-design.md §Risks risk-2` does not exist.** The arch has §Tradeoffs / §Open questions / §Gap analysis but no `Risks risk-2` section. The "risk-2 self-hosted KVM CI runner not provisioned" mitigation lives in `final-design.md §Risks risk-2` AND in `High-level-impl.md §Step 6 — Risks specific to this step` (line 178). Resolution: the References block is corrected to point to `final-design.md §Risks risk-2` AND `High-level-impl.md §Step 6 — Risks specific to this step`; the inline citation in Notes-for-implementer (line 271 in draft) is fixed identically. |
| K-3 | **block** | **`Depends on:` line under-enumerates ancestors.** Draft names S6-02, S6-03. The real closure also includes S1-04 (`GateContext` contract), S2-01 (`RetryLedger` contract), S5-02 (`GateRunner` contract), S3-05 (`gates/catalog/stage6_validate.yaml`), S6-01 (`FirecrackerClient` surface), S6-04 (`auto_detect`; even though S6-05 uses Firecracker directly via the factory, the marker registration sits next to S6-04's marker-consumer tests). S1-05 establishes `EVENT_SANDBOX_REGISTRY_SELECTED`. Resolution: rewritten `Depends on:` block enumerates the closure. |
| K-4 | **harden** | **Event constant for the smoke run is missing.** Per S1-01 HARDENED + S6-03 / S6-04 pattern, every cross-phase observable event needs a `Final[str]` constant in `sandbox/logging.py`. The smoke test should emit `EVENT_SANDBOX_FIRECRACKER_SMOKE_STARTED = "sandbox.firecracker.smoke.started"` and `EVENT_SANDBOX_FIRECRACKER_SMOKE_COMPLETED = "sandbox.firecracker.smoke.completed"` and `EVENT_SANDBOX_FIRECRACKER_SMOKE_FAILED = "sandbox.firecracker.smoke.failed"` via the autouse fixture so the post-run health-capture step has structured anchors to correlate against. Resolution: AC-EVT-1..-3 add the three constants (alphabetized into sorted `__all__`); the autouse fixture emits each at the right boundary; the perf JSONL line carries a back-reference to the event chain. |
| K-5 | **harden** | **Cron `0 7 * * 1` semantics need an inline anchor.** GitHub Actions evaluates `cron:` in UTC; the draft Notes confirm this but the YAML lacks an inline comment. Without it, a future maintainer can silently change `7` to `07` or interpret as local time. Resolution: AC-CRON-1 — replace the literal with `# Monday 07:00 UTC (UTC always — GitHub Actions cron is UTC; do not interpret as local). Picked to land before US/EU work hours.` immediately above the `cron:` line. Constant + comment per the original Refactor §"Replace the literal cron string with a constant + comment". |
| K-6 | **harden** | **`runs-on: [self-hosted, kvm]` is correct but YAML semantics require BOTH labels.** Per GitHub Actions docs, label arrays are AND-conjoined. A runner with only `self-hosted` (no `kvm`) will not pick up the job — this is what we want, but contributors unfamiliar with the conjunction can misread it as OR. Resolution: AC-RUNNER-1 — top-of-workflow doc-block (per AC-DOC-1) explicitly states "AND semantics" and lists the runner-registration command-line. |
| K-7 | **harden** | **`PAGERDUTY_KVM_RUNNER_KEY` secret name is invented.** The org-standard secret-naming convention is owned by ops; the story should reference the convention, not invent a name. Resolution: AC-PD-3 (also under T-8) — the secret name is referenced as `secrets.PAGERDUTY_KVM_RUNNER_SERVICE_KEY` (matching the org's `PAGERDUTY_*_SERVICE_KEY` convention; if the actual ops naming differs, the workflow's doc-block names the convention source). |
| K-8 | **harden** | **`workflow_dispatch` triggered by anyone with write access** — no AC pins which actors can fire the smoke ad-hoc. This is acceptable but should be acknowledged (vs guarded by `inputs:` requiring an explicit confirmation token). Resolution: AC-DISPATCH-1 — `workflow_dispatch.inputs.acknowledge` required boolean `true`; documents that a curious contributor running ad-hoc gives the on-call no incident, but the smoke result still uploads to artifacts. |
| K-9 | **nit** | **`tests/integration/sandbox/test_firecracker_network_policy.py` placeholder origin.** S6-02 HARDENED ships the file as a `@pytest.mark.skip_if_no_kvm`-only placeholder (no internal `pytest.skip`); S6-05 populates the body, not creates the file. Resolution: Files-to-touch table marks the file as `EDIT — populate body, leave the `pytest.mark.skip_if_no_kvm` module-level marker AND the docstring referencing S6-05 in place`. |

### Design-patterns critic

| # | Severity | Finding |
|---|---|---|
| D-1 | **harden (elevated under rule-of-three)** | **Shared test-construction kernel missing.** Both smoke tests duplicate (a) the `FirecrackerClient` construction; (b) the `_valid_spec_kwargs` overrides; (c) the perf JSONL emission. With S7-* perf tests + S8-03 e2e tests + Phase 7 distroless smoke also targeting the same Firecracker surface, this is the **fourth** future consumer. Resolution: AC-HELPERS-1..-3 ships `tests/integration/sandbox/_helpers.py` from day 1 with `kvm_smoke_client_factory()`, `kvm_smoke_spec(**overrides) -> SandboxSpec`, `record_perf(run_id, wall_seconds, exit_code, **extras)`. Each smoke test imports these (single line per construction); future perf / e2e tests reuse without copy-paste. |
| D-2 | **harden** | **Workflow YAML hardcodes test file paths in two places.** `paths:` filter list + `pytest` invocation argv. A rename of `test_firecracker_smoke.py` silently breaks the filter (workflow doesn't fire) without breaking pytest (test still runs locally). Resolution: AC-YAML-CONST-1 — the workflow uses an explicit `env.KVM_TEST_FILES: "tests/integration/sandbox/test_firecracker_smoke.py tests/integration/sandbox/test_firecracker_network_policy.py"` at the workflow top; the `paths:` filter mirrors the same prefixes; an additional fence test (C-7's `tests/schema/test_kvm_gated_tests_carry_marker.py`) asserts the workflow env var enumerates the same set the marker fence discovers. |
| D-3 | **harden** | **Smart-constructor opportunity for perf JSONL rows.** Anaemic dict (`{run_id, backend, wall_seconds, exit_code, run_ts}`) — sliding-window p95 / p50 reads cannot be type-checked, and a typo (`wall_seconds_s`) silently appends a row that the consumer (S7-02) can't read. Resolution: AC-PERF-MODEL-1..-3 — `class FirecrackerSmokePerfRow(BaseModel, frozen=True, extra="forbid")` with `model_dump_json()` writing the row; `class FirecrackerSmokePerfLog` reader the consumer uses (rejects rows missing fields). The JSONL is a typed log, not an anaemic stream. |
| D-4 | **harden** | **Workflow-as-data — capture-tooling Open/Closed seam.** The on-failure capture step is `codegenie sandbox health` (S8-01) and `actions/upload-artifact@v4`. The next-phase distroless story (Phase 7 S10-04) will want to add a `codegenie sandbox baseimage-trace` capture step. The workflow currently lists steps inline; a single inline list scales linearly with capture targets. Resolution: Notes-for-implementer records the pattern opportunity for Phase 7 to introduce a `_post_failure_capture.yml` composite action; **defer** in this story (rule-of-three not yet reached — only one consumer). |
| D-5 | **nit** | **Constant-extract: `_BACKEND_NAME: Final[str] = "firecracker"` does not apply to tests/CI** (it's a production-module convention); the workflow YAML carries `"firecracker"` literal in 2+ places. Resolution: D-2 covers this via env var; no separate `Final` is needed. |
| D-6 | **nit** | **Concurrency mutex** (C-3) is the registry-pattern equivalent for CI runners — a "registry of one" on the self-hosted runner. Notes for implementer documents the discipline; AC-CONC-1 enforces. |

**Researcher stage skipped:** No findings tagged `NEEDS RESEARCH`. Every weakness is answerable from Phase 5 arch + ADRs + the six prior HARDENED reports + CLAUDE.md + standard GitHub Actions reference docs.

## Conflict resolutions

1. **Coverage critic C-2 (Open Q6 not closed) vs Story Goal ("closes Open Q6").** The story cannot operationally close Open Q6 without the on-call rotation owner + the org-standard PagerDuty action — both of which are operational deliverables outside the autonomous-implementer's scope. Resolution: **Story goal is narrowed** — S6-05 ships the code-side artifacts (tests, workflow, marker, perf JSONL writer, doc-block) and *prepares* the seam for Open Q6 closure; Open Q6 is **explicitly deferred to 6b ops** (matching the 6a/6b split arch §Risks risk-2 + High-level-impl §Step 6 already prescribe). The story's `Status:` carries `BLOCKED-PARTIAL` semantics for the "Open Q6 closure" sub-claim; the phase exit criterion is unmet until a 6b ops PR fills the placeholders.

2. **Test-quality T-5 (mutation thinking — `npm ci` may pass with no network policy) vs Reality (S6-02 already paid this rent via the policy contract).** Both are right. Resolution: the network-policy test asserts **three positives + two negatives** (T-5 AC-MUT-2) AND a fourth structured-event assertion (`run.signals.network_policy == "scoped"`); collectively no single failure mode masks another.

3. **Design-Patterns D-1 (shared helpers kernel) vs Rule 2 (premature abstraction).** Rule-of-three: two smoke tests in this story + (S7-02 perf) + (S8-03 e2e) + (Phase 7 distroless smoke) = four known consumers. Rule of three crossed; Design-Patterns wins. AC-HELPERS-1..-3 ships the kernel from day 1.

4. **Consistency K-8 (`workflow_dispatch` security) vs Coverage (`workflow_dispatch` is needed for ad-hoc reruns).** Resolution: keep `workflow_dispatch` but gate via `inputs.acknowledge: required: true`. Ad-hoc reruns require an explicit "yes I mean it" boolean; the on-call rotation is undisturbed.

5. **Test-quality T-1 / Consistency K-1 (`from_digests_yaml` removed) vs Story Implementation outline.** No conflict — the draft simply pre-dates S6-03 HARDENED. Resolution: replace every `from_digests_yaml()` callsite with the canonical factory chain; no other surgery needed.

## Edits applied to the story

The story file at `../S6-05-kvm-smoke-and-weekly-cron.md` was rewritten in place with the following structural changes (every change traces to one or more findings above):

1. **Status line** — `Ready` → `HARDENED 2026-05-25` with a `BLOCKED-PARTIAL` annotation noting the Open Q6 closure deferral to 6b ops.
2. **Validation notes block** appended after the header (mirror of every other HARDENED story in this phase) summarizing all changes + the Open Q6 deferral.
3. **`Depends on:`** widened to include S1-04, S2-01, S3-05, S5-02, S6-01, S6-02, S6-03, S6-04, S1-05 (resolves K-3).
4. **`ADRs honored:`** annotated; added ADR-0043 (no silent edits — the Open Q6 deferral discipline + paths-filter completeness).
5. **References block** — fixed broken `phase-arch-design.md §Risks risk-2` ref → `final-design.md §Risks risk-2` + `High-level-impl.md §Step 6 — Risks specific to this step` (resolves K-2); added the four prior HARDENED reports (S6-01..S6-04); added `_helpers.py` reference.
6. **Goal** — light edit narrowing scope: ships code + workflow artifacts AND prepares (does not close) Open Q6.
7. **Acceptance criteria** reorganized into 13 sections (§A–§M) with ~50 numbered ACs (resolves C-1):
   - §A — Marker registration + fence (AC-MARK-1..-3 + AC-FENCE-1; resolves C-7)
   - §B — Test client + spec construction kernel (AC-CONSTRUCT-1..-3 + AC-SPEC-1..-3; resolves T-1, T-2, T-3, K-1, D-1)
   - §C — `test_firecracker_smoke.py` body (AC-SMOKE-1..-6 + AC-RUN-LOOKUP-1..-3 + AC-FAIL-PATH-1; resolves T-4, T-14)
   - §D — `test_firecracker_network_policy.py` body (AC-NET-1..-6 + AC-NFT-1..-3 + AC-MUT-1..-3; resolves T-5, T-6)
   - §E — Workflow file (AC-WF-1..-10 + AC-CONC-1 + AC-PATHS-1 + AC-YAML-CONST-1 + AC-CRON-1 + AC-RUNNER-1 + AC-DISPATCH-1; resolves C-3, C-4, D-2, K-5, K-6, K-8)
   - §F — PagerDuty step (AC-PD-1..-5; resolves T-8, K-7)
   - §G — Pre-flight + cache hygiene (AC-PREP-1..-3 + AC-ISO-1..-3; resolves AC-7's `--check` invocation + C-8)
   - §H — Health capture on failure (AC-HEALTH-1..-3)
   - §I — Perf JSONL writer (AC-PERF-EMIT-1 + AC-PROV-1 + AC-PERF-MODEL-1..-3 + AC-JSONL-1; resolves T-9, T-13, C-6, D-3)
   - §J — Perf trend test (AC-PERF-1..-4; resolves C-9, T-7)
   - §K — Logging events (AC-EVT-1..-3; resolves K-4)
   - §L — Queue-watcher companion workflow (AC-QUEUE-1..-3; resolves C-5)
   - §M — Workflow YAML fence + meta-tests (AC-PD-TEST-1; resolves C-10)
8. **TDD plan** rewritten:
   - Test files: `test_firecracker_smoke.py`, `test_firecracker_network_policy.py`, `tests/integration/sandbox/_helpers.py`, `tests/integration/sandbox/conftest.py` (autouse perf fixture from day 1), `tests/schema/test_kvm_gated_tests_carry_marker.py`, `tests/ci/test_firecracker_smoke_workflow.py`, `tests/perf/test_firecracker_smoke_p95.py`.
   - Code snippets corrected to use `from_pinned_digests` + `_valid_spec_kwargs` + `load_sandbox_run` for the run-summary lookup.
   - Removed per-test `try/finally` perf emission (now lives in the autouse fixture).
   - Added explicit "verify against S1-04/S2-01/S5-02 HARDENED before writing" callouts for `GateContext` / `RetryLedger` / `StrictAndGate` arity (T-10, T-11, T-12).
9. **Implementation outline** rewritten to 9 steps mirroring §A–§M with kernel-first ordering (`_helpers.py` → marker → tests → fixture → workflow → companion workflow).
10. **Files to touch** widened to 11 entries: adds `tests/integration/sandbox/_helpers.py`, `tests/schema/test_kvm_gated_tests_carry_marker.py`, `tests/ci/test_firecracker_smoke_workflow.py`, `tests/perf/test_firecracker_smoke_p95.py`, `.github/workflows/firecracker-queue-watcher.yml`; removes `.github/workflows/firecracker-smoke.yml.README.md` (replaced by top-of-workflow doc-block per AC-DOC-1).
11. **Out of scope** widened: Open Q6 *operational* closure (6b ops PR); choice of org-standard PagerDuty action SHA (6b ops decision); the dispatch-input `acknowledge` UI; per-OS runner-image baking; multi-runner pool sizing.
12. **Notes for the implementer** expanded with: kernel-extract rationale (D-1); workflow YAML constant pattern (D-2); cron-UTC anchor (K-5); runner-label AND semantics (K-6); PagerDuty action choice and SHA-pin (T-8 + K-7); the 6a/6b split with concrete deliverable boundaries; queue-watcher pattern (C-5); Open/Closed for post-failure capture (D-4 — deferred to Phase 7).

## Verdict

**HARDENED.** No findings required full RESCUE — the goal traces cleanly to ADR-0004 §Consequences + ADR-0009 §Consequences + arch §Goal 6 + High-level-impl §Step 6, and the 6a/6b split is a sanctioned arch mitigation that the validator amplified (rather than reframed). The story shipped as HARDENED is ready for `phase-story-executor` to land 6a; Open Q6 closure remains 6b ops work.

## What was NOT changed

- The story's core goal (KVM-gated tests + workflow + cron + paging-on-failure).
- The 300 s wall-clock budget (non-negotiable per ADR-0019 evidence; T-4 / T-7 strengthen its meaning, not relax it).
- The cron schedule `0 7 * * 1` (Monday 07:00 UTC).
- The `runs-on: [self-hosted, kvm]` discipline.
- The Phase 5 x86_64-only scope.
- The deferral of operator CLI subcommands to S8-01.
- The deferral of perf-regression gates to S7-02.

## What was NOT auto-fixed (surfaced to humans / 6b ops)

- Operational owner for the self-hosted KVM runner (Open Q6 6b).
- Concrete PagerDuty action choice + SHA pin (Open Q6 6b).
- Concrete on-call rotation owner / PagerDuty service routing (Open Q6 6b).
- Org-standard secret-naming verification (the story names a convention; ops confirms the literal).

## Pattern lineage anchors

| Phase-5 ancestor | Pattern inherited | This story's AC |
|---|---|---|
| S1-01 HARDENED | `STARTED/COMPLETED/FAILED` event-verb canonical-table + warning-ID namespace regex | AC-EVT-1..-3 |
| S1-04 HARDENED | `GateContext` constructor contract (verify-before-write) | AC-CTX-1 (callout in TDD plan) |
| S2-01 HARDENED | `RetryLedger` constructor + JSONL chain semantics | AC-LEDGER-1 (callout in TDD plan) |
| S3-05 HARDENED | `gates/catalog/stage6_validate.yaml` schema | AC-GATE-1..-3 |
| S5-02 HARDENED | `GateRunner` + `StrictAndGate` constructor (verify-before-write) | AC-GATE-1..-3 |
| S6-01 HARDENED | `SandboxSpec` phantom-field discipline; `_valid_spec_kwargs`; `SandboxRun` lookup at `.codegenie/sandbox/runs/<id>/run.json` | AC-SPEC-1..-3 + AC-RUN-LOOKUP-1..-3 |
| S6-02 HARDENED | nftables table-name uniqueness; `apply_policy` + `NetNamespaceConfig.teardown()` contract; `EVENT_SANDBOX_FIRECRACKER_TAP_ORPHAN`; placeholder-test population discipline | AC-NFT-1..-3 + AC-NET-1..-6 |
| S6-03 HARDENED | `FirecrackerClient.from_pinned_digests(load_pinned_digests(...), artifacts_root=..., ...DI ports...)` canonical factory; `--check` (read-from-disk) for PR-time pre-flight | AC-CONSTRUCT-1..-3 + AC-PREP-1..-3 |
| S6-04 HARDENED | Reason-string namespace regex inheritance; module-level event constants alphabetized into sorted `__all__`; `_default_*` DI-port convention | AC-EVT-1..-3 + AC-PERF-EMIT-1 |
| ADR-0043 (production) | Extension by addition — no silent edits | Open Q6 deferral (rather than placeholder-closure) |

## Forward-compat anchor (for downstream stories)

- **S7-02 (perf regression gates):** reads `.codegenie/perf/firecracker_smoke.jsonl` via the typed `FirecrackerSmokePerfLog` reader (AC-PERF-MODEL-1..-3); the `runner_name` / `kernel_release` columns let S7-02 segment trends by runner. Story should consume `_helpers.py::record_perf` for symmetry with S6-05.
- **S8-01 (`codegenie sandbox health`):** the workflow's failure-capture step relies on `codegenie sandbox health` printing structured JSON; S8-01 must keep this output stable. The `sandbox-health-<run-id>.json` artifact name is a downstream contract.
- **S8-03 (e2e `codegenie remediate` smoke on Linux KVM):** reuses `_helpers.py::kvm_smoke_client_factory()` and the perf JSONL emission discipline; introduces the third concrete consumer of `_helpers.py` (rule-of-three reached → kernel-extract validated post-hoc).
- **Phase 7 S10-04 (distroless build gate):** introduces the post-failure capture composite-action seam (D-4 deferred opportunity); when Phase 7 lands a second on-failure capture target, the inline-steps shape becomes a `_post_failure_capture/action.yml` composite action.
- **6b ops PR (Open Q6 closure):** fills the placeholders (PagerDuty action SHA + integration-key secret name + rotation-owner doc) and marks Phase 5 §Goal 6 met. The doc-block in `firecracker-smoke.yml` is the closure surface; no code edits expected.
