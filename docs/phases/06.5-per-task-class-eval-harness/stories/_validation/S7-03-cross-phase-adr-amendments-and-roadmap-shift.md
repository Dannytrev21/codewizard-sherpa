# Validation report — S7-03 (cross-phase ADR amendments + roadmap §Phase 7 wording shift)

**Validated:** 2026-07-26
**Validator:** scheduled `story-validation-corrector` task
**Story:** `docs/phases/06.5-per-task-class-eval-harness/stories/S7-03-cross-phase-adr-amendments-and-roadmap-shift.md`
**Verdict:** **HARDENED-with-BLOCKED-partial**
**Findings:** 17 total — 2 blocks (structural, gated on upstream ADR amendment), 12 hardens, 3 nits
**Story status updated to:** `BLOCKED-PARTIAL` (AC-1 / AC-2 gated on ADR-0005 amendment per S2-05 rescue; AC-3 / AC-4 / AC-5 landable now)

## Stage 1 — Context loaded

Read in this order:

- `S7-03-cross-phase-adr-amendments-and-roadmap-shift.md` (target)
- `phase-arch-design.md §"What's next — handoff to Phase 7"`, §"Cross-phase ADR amendments land with the code that depends on them"
- `High-level-impl.md §"Step 7"` and §"Implementation-level risks #2"
- Phase 6.5 ADRs: `0002-promotion-gate-keys-on-lower-bound-95.md`, `0005-cassette-canary-seed-parameterization.md`, `0007-bench-invocation-tagging-on-sandbox-cost-entry.md`, `0009-automatic-demotion-as-recommendation-shift.md`
- Sibling validation: `stories/_validation/S2-05-canary-seed-shim.md` (**RESCUE / BLOCKED** — the load-bearing dependency for AC-1/AC-2 of this story)
- Sibling story: `stories/S2-06-cost-tag-shim.md` (Ready / HARDENED 2026-05-26 — dependency for AC-3)
- Sibling validation: `stories/_validation/S7-02-audit-chain-integration-and-snapshots.md` (framing template for this report)
- Upstream files being edited (current state on disk):
  - `docs/phases/04-vuln-llm-fallback-rag/final-design.md` — grep `Canary.mint` returns **0 hits**; no `ADR-P4-006` reference.
  - `docs/phases/04-vuln-llm-fallback-rag/ADRs/` — no `ADR-P4-006-canary-seed-kwarg.md` file exists.
  - `docs/phases/05-sandbox-trust-gates/ADRs/0010-cost-sandbox-run-ledger-schema.md` — grep `bench_invocation` and `CODEGENIE_BENCH_INVOCATION_TAG` return **0 hits**.
  - `docs/phases/05-sandbox-trust-gates/ADRs/0016-per-task-class-eval-harness-as-trust-evidence.md` — grep `0009-automatic-demotion` and `human-authored PR` return **0 hits**.
  - `docs/roadmap.md` — grep `bench_score\.mean` returns **0 hits repo-wide**; the *only* `bench_score` references (lines 180, 184, 189) already use `lower_bound_95`. Line 189 (inside `## Phase 6.5 — …` section, restating Phase 7's precondition) is the load-bearing mention.
- Actual Phase 4 canary surface: `src/codegenie/fallback/fence/wrapper.py:258` — `FenceWrapper(nonce_source: Callable[[], HexNonce] = _default_nonce_source)`. `Canary` class exposes no `.mint(...)` method. `src/codegenie/engines/canary.py` does not exist.

**Context Brief:**

S7-03 is the *forcing function* for four cross-phase amendments the phase-arch-design and High-level-impl-Step-7 both name as gates on Phase 6.5's close. The intent is right — cross-phase changes must land before the phase merges or the audit trail is broken. But three of the four amendments have factual problems the story does not surface:

1. **AC-1 / AC-2 (Phase 4 `Canary.mint(seed=...)` + new ADR-P4-006):** ADR-0005 prescribes a phantom Phase 4 API. Phase 4 actually shipped `FenceWrapper(nonce_source=Callable[[], HexNonce])`. S2-05 (the code-side shim story) was verdict RESCUE / status BLOCKED on 2026-05-26 for this exact reason; the phase architect owes an amendment to ADR-0005 before S2-05 or S7-03's Phase 4 amendments can proceed. Documenting a nonexistent kwarg in Phase 4's final-design would land a lie on disk.
2. **AC-5 (roadmap `bench_score.mean` → `lower_bound_95`):** the `bench_score.mean` substring appears **zero times** in `docs/roadmap.md` today. The intended substitution is already reflected (line 189 in `## Phase 6.5 — …` restates Phase 7's precondition with `lower_bound_95`). The AC-as-written treats the substitution as pending; the TDD test would additionally FAIL its `"lower_bound_95" in section` assertion because the roadmap's `## Phase 7` section itself contains no `bench_score` string. Either the edit landed silently outside this story's audit trail, or the AC and TDD plan are looking in the wrong section.
3. **AC-3 (Phase 5 ADR-0010 `bench_invocation`) + AC-4 (Phase 5 ADR-0016 clarification):** the underlying ADRs (Phase 6.5 ADR-0007 and ADR-0009) are internally coherent and the amendment shape is right. AC-3 is contingent on Phase 5 S7-03 landing `src/codegenie/sandbox/cost.py` (per S2-06's §"Cross-phase amendment train (gated)"); AC-4 has no code dependency and can proceed independently.

Blocks trace to defective upstream ADRs and factual roadmap state, not to executor discipline. Two ACs are HARDENED-with-BLOCKED-partial (AC-1, AC-2, gated on ADR-0005 amendment). Two ACs (AC-3, AC-4) are HARDENED. AC-5 is RESCUE-refactored: rewritten as an *invariant assertion* (no `bench_score.mean` may appear anywhere in `docs/roadmap.md`) plus a *positive presence* assertion at line 189's precondition text — both landable today. A new AC-14 formalizes the "if S2-05 rescue lands under a different ADR-0005 shape, this story's AC-1/AC-2 must be re-scoped in the same PR" gate.

## Stage 2 — Critic findings

### Coverage critic

| ID | Severity | Finding | Fix |
|---|---|---|---|
| F-COV-1 | harden | AC-5 verifies the *absence* of `bench_score.mean` in one section, but not the invariant the roadmap ADR-0002 actually pins: `bench_score.mean` may not appear as a *promotion-gate signal* anywhere in `docs/roadmap.md`. The current roadmap already satisfies the invariant globally — an AC that only checks Phase 7 section could drift silent if a future editor reintroduces `bench_score.mean` in Phase 8 / Phase 13 wording. | New AC-5a — repo-wide grep `bench_score.mean` (regex `bench_score\.mean\b`) returns zero hits in `docs/roadmap.md`. AC-5b — the *cross-phase-precondition text* at `docs/roadmap.md §"Phase 6.5"` (currently line 189, restated as pointer-anchored search) still names `bench_score.lower_bound_95` as the Phase 7 hard precondition — a positive-presence guard. |
| F-COV-2 | harden | AC-4 pins the clarification paragraph exists via `"recommendation" in text.lower()` — but `"recommendation"` already appears in ADR-0016 (§Options considered, §Consequences). The test would pass today, before the amendment lands, and cannot distinguish "amendment landed" from "the ADR happens to contain the substring elsewhere." | AC-4 rewritten: assertion pins the ADR-0009-signature phrasing verbatim, mirroring ADR-0009's own text: `"'automatic' refers to the verdict recomputation"` (single-quoted, exact substring). This substring appears in no other Phase 5 ADR. Plus AC-4b — the "Related" or "Amended by" block gains a bidirectional link to `0009-automatic-demotion-as-recommendation-shift`. |
| F-COV-3 | harden | No AC pins the "Amended by" cross-link on Phase 5 ADR-0010. AC-3 asserts the *content* addition but leaves the metadata cross-link (which Phase 6.5 ADR-0009's §Consequences requires symmetrically for both edits) unverified. | New AC-3b — Phase 5 ADR-0010's `Related` block gains a pointer to `../../06.5-per-task-class-eval-harness/ADRs/0007-bench-invocation-tagging-on-sandbox-cost-entry.md`. Grep-asserted independently of the amendment-section presence check. |
| F-COV-4 | harden | ADR-P4-006's AC (AC-2) checks four Nygard sections exist but does not check the ADR carries the standard Phase-6.5-family header block (`Status:`, `Date:`, `Tags:`, `Related:`). Missing header block is the #1 way a new ADR gets published in a shape that trips a future linter or docs-site build. | AC-2 amended: additionally require `**Status:**`, `**Date:**`, `**Tags:**`, `**Related:**` header lines match Phase 6.5 ADR-0005's shape (exact-substring anchors). |
| F-COV-5 | harden | AC-6 asserts each amendment is "referenced by a commit message" but has no verifiable oracle in the test suite — commit messages are not present in a squash-merged history and are not readable from a clean checkout. The AC is unenforceable at CI time. | AC-6 rewritten: land a single-line entry per amendment in `docs/phases/06.5-per-task-class-eval-harness/CHANGELOG.md` (new file if absent; append if present) naming the originating Phase 6.5 ADR and the target file. The doc-level trail is CI-verifiable; the commit-message discipline stays a Notes-for-implementer hint. |
| F-COV-6 | harden | No AC gates the "phase merge" side: the story says "all four amendments merged before Phase 6.5 closes" but leaves *who checks that* undefined. If S7-03 itself is the gate, an assertion that all four edits are on the current branch (not merely on `main`) closes the drift window. | AC-7 amended: `git ls-files` on the current branch returns each of the six §Files-to-touch paths (five edits + the CHANGELOG entry, plus the new ADR-P4-006 file when AC-1/AC-2 unblock); a fence test in `tests/docs/test_cross_phase_amendments_present.py` walks the working tree, not a git-log query. |
| F-COV-7 | **block** (gated on ADR-0005 amendment) | AC-1 asks the executor to document `Canary.mint(seed=...)` in Phase 4's final-design. Phase 4 shipped no such method — the actual seam is `FenceWrapper(nonce_source: Callable[[], HexNonce])`. Landing AC-1 as-written would put a lie into a docs file that outlives the story. S2-05's RESCUE report already surfaced this; ADR-0005 requires an amendment (or a superseding ADR) before AC-1 can proceed under any shape. | New AC-14 — AC-1 and AC-2 are BLOCKED until Phase 6.5 ADR-0005 is amended (or superseded) to name the actual Phase 4 seam. The amendment is the phase architect's owed follow-up per `_validation/S2-05-canary-seed-shim.md §"Owner of unblock"`. Under the amended ADR-0005, AC-1 and AC-2 are re-scoped in one of two shapes: (a) if ADR-0005 keeps the amendment-to-Phase-4 posture but names the correct seam (adds a `seed` kwarg to `FenceWrapper.__init__` or a new `nonce_source` overload), AC-1/AC-2 document that shape; (b) if ADR-0005 pivots to "consume the existing `nonce_source` seam" (the S2-05-rescue path), AC-1 is deleted and AC-2 becomes "ADR-P4-006 documents the consumption pattern, not a Phase 4 API change." Either path is landable; the current shape is not. |
| F-COV-8 | **block** (factual — AC-as-written unlandable) | AC-5's TDD test (`test_roadmap_phase_7_uses_lower_bound_95_not_mean`) asserts `"lower_bound_95" in section` where `section` is the roadmap's `## Phase 7 —` block. That block currently contains **zero** `bench_score` references — the Phase 7 precondition text lives in the `## Phase 6.5 —` section at line 189. Under the current roadmap, the assertion fails vacuously; the AC as-written asks the executor to insert `bench_score.lower_bound_95` text into Phase 7 that has never been there. | AC-5 rewritten (F-COV-1 fix supersedes) — the invariant is "no `bench_score.mean` anywhere in `docs/roadmap.md`" (already true) plus "the Phase-7-precondition line at Phase 6.5 §Exit criteria row 7 still names `lower_bound_95`" (already true, positive presence anchor). If the phase architect wants Phase 7's *own* section to name the precondition redundantly, that's an ADR-0002 scope expansion — call it out but do not silently invent it here. |

### Test-Quality critic

| ID | Severity | Finding | Fix |
|---|---|---|---|
| F-TQ-1 | harden | `test_phase4_final_design_documents_canary_seed_kwarg` uses `"Canary.mint(seed:" in text or "Canary.mint(seed=" in text` — a substring test that would pass on inline discussion prose ("earlier we considered `Canary.mint(seed=...)` but rejected it") as easily as on the canonical signature block. If AC-1 unblocks (per F-COV-7's rescue path), the test needs a code-fence anchor. | Test rewritten (post-unblock) to look for the signature inside a fenced code block or an API-section heading — `re.search(r"```python\n[^`]*Canary\.mint\(seed[:=]", text, re.S)` — anchored oracle, harder to fake. |
| F-TQ-2 | harden | `test_phase5_adr_0016_amended_with_demotion_clarification` uses `"human-authored PR" in text or "human authored PR" in text` — but ADR-0016 §"Context" already discusses "human-authored" flow. Passes trivially before amendment lands. | Test rewritten to look for the ADR-0009 signature phrase `"'automatic' refers to the verdict recomputation"` (verbatim substring — appears in no existing ADR) AND the cross-link `0009-automatic-demotion-as-recommendation-shift`. Two anchors, both introduced by the amendment. |
| F-TQ-3 | harden | `test_roadmap_phase_7_uses_lower_bound_95_not_mean` regex `r"##\s*Phase\s*7\b(.*?)(?=^##\s*Phase\s*\d|\Z)"` — the `\b` word-boundary between `7` and `.` matches, so this regex captures `## Phase 7 —` (correct) but the non-greedy `(?:...)` with lookahead `^##\s*Phase\s*\d` correctly stops at `## Phase 7.5`. Subtle: if a future roadmap adds `## Phase 70` before `## Phase 7`, the regex mismatches. Also the assertion "lower_bound_95 in section" is factually wrong (F-COV-8). | Test rewritten: (a) global assertion `re.search(r"bench_score\.mean\b", text) is None` (repo-wide); (b) positive-presence assertion at the Phase 6.5 §Exit criteria row 7 (`"bench_score.lower_bound_95 ≥ tier_threshold[bronze]"` verbatim substring anywhere in `docs/roadmap.md`). No fragile section-slicing regex. |
| F-TQ-4 | harden | No test that the two Phase 5 ADRs each gain the bidirectional cross-link (ADR-0010 ↔ Phase 6.5 ADR-0007; ADR-0016 ↔ Phase 6.5 ADR-0009). Only the ADR-0016 → ADR-0009 direction is currently checked. | Two new tests — `test_phase5_adr_0010_links_back_to_phase65_adr_0007` and `test_phase65_adr_0009_links_forward_to_phase5_adr_0016` (the second one is a symmetry check on Phase 6.5's own ADR-0009; already exists as "Related: [Phase 5 ADR-0016]", so this test is a *regression fence* against link removal). |
| F-TQ-5 | harden | No CHANGELOG.md test — F-COV-5's fix depends on it. | New test — `test_phase65_changelog_names_all_four_amendments`: `docs/phases/06.5-per-task-class-eval-harness/CHANGELOG.md` exists; contains one line per amendment matching the pattern `- <YYYY-MM-DD> <ADR-NNNN>: <target file>` (four rows minimum, four ADRs enumerated). Enforces the doc-level audit trail. |
| F-TQ-6 | harden | Test module is a single `tests/docs/test_cross_phase_amendments_present.py` — as more cross-phase amendments accrue (Phase 7, Phase 8, Phase 11 all will need this pattern), the file grows and violates single-responsibility. | Directory-per-family: `tests/docs/cross_phase_amendments/test_phase65_amendments.py` — the *directory* is the collection point for future `test_phase7_amendments.py`, `test_phase8_amendments.py`. Zero-edit extension when the next phase ships (Open/Closed at the test-organization level). |
| F-TQ-7 | nit | No test asserts that the four grep-asserts are actually running (a broken import in the test file would produce zero collected tests + green CI). Standard pytest-collection sanity is easy. | New test — `test_cross_phase_amendment_test_count_matches_expected`: `pytest --collect-only tests/docs/cross_phase_amendments/` yields ≥ 6 test functions (loose lower bound; grows with additions). |

### Consistency critic

| ID | Severity | Finding | Fix |
|---|---|---|---|
| F-CON-1 | **block** | AC-1 / AC-2 descend from ADR-0005; ADR-0005 is defective per S2-05's RESCUE (2026-05-26). The story is faithful to a broken source. Landing it would either (a) put phantom-API prose into Phase 4's final-design, or (b) require an executor pass to invent the reconciliation on-the-fly. | See F-COV-7 fix — AC-14 gates AC-1/AC-2 on ADR-0005 amendment. Story `**Depends on:**` line amended to include "Phase 6.5 ADR-0005 amendment or supersession". Story status flips to `BLOCKED-PARTIAL`. |
| F-CON-2 | harden | AC-3 depends on Phase 5 S7-03 landing `src/codegenie/sandbox/cost.py` — per S2-06's §"Cross-phase amendment train (gated)" note, that story was HARDENED but not GREEN as of 2026-05-26. This story's `**Depends on:**` line names S2-06 but not the Phase 5 S7-03 gating. Silent dependency on an upstream phase's not-yet-GREEN story. | `**Depends on:**` line amended to add "Phase 5 S7-03 (sandbox `cost.py` GREEN)"; AC-3 acquires a §Precondition note that the amendment PR opens only after Phase 5 S7-03 lands. |
| F-CON-3 | harden | AC-7's "all four amendments merged before Phase 6.5 closes" is a Phase-close discipline the story cannot enforce from a CI test. The gate belongs to the Phase 6.5 phase-close checklist, not to S7-03's TDD suite. | Story clarifies: S7-03's ACs verify the amendments are *present on disk* (verifiable in CI); the phase-close checklist (in `phase-arch-design.md §"What's next"`) is the meta-gate that all Phase 6.5 stories including S7-03 must reach `GREEN` before the phase can merge. AC-7 rescoped to on-disk presence only. |
| F-CON-4 | nit | The story cites `templates/adr-amendment.md` as an existing artifact but the repo has no `templates/` directory. Files-to-touch table does not include it. | Notes-for-implementer amended to name the concrete artifact path if the template is created (`docs/templates/adr-amendment.md`) and to note that S7-03 does *not* gate on template existence — the four amendments land in-line with the existing Nygard structure of the target ADRs, no template needed. Template creation deferred to a separate story if repeatedly-needed. |

### Design-Patterns critic

| ID | Severity | Finding | Fix |
|---|---|---|---|
| F-DP-1 | harden | The story treats "four amendments" as a monolithic goal. But each amendment is (a) targeted at a different phase / owner, (b) reversible independently, and (c) blocked or unblocked independently. Bundling them into one goal violates Rule 3 (Surgical Changes) and creates an all-or-nothing merge dependency where partial progress is not visible. | Story restructured: goal split into *per-amendment* sub-goals within one story (each an independently-verifiable AC + independently-runnable test), plus a "compile" AC that checks *all landed* only at phase-close. This preserves the phase-close gate while allowing S7-03 to ship AC-3 / AC-4 / AC-5 today and defer AC-1 / AC-2 until ADR-0005 is amended. Command pattern at the AC level. |
| F-DP-2 | harden | The test file `tests/docs/test_cross_phase_amendments_present.py` proposes five inline test functions (one per amendment). At Phase 7 / Phase 8, this file will accrete more amendments; the pattern "one file, one test per amendment" scales O(N amendments) in a single file. | Directory-per-family (see F-TQ-6): `tests/docs/cross_phase_amendments/test_phase65_amendments.py` — extension by addition at the test-file level; adding Phase 7's amendments creates `test_phase7_amendments.py` with zero edits to the Phase 6.5 file. Registry-of-tests pattern (implicit; pytest discovers). |
| F-DP-3 | harden | AC-6's commit-message discipline is enforced only by convention. A CHANGELOG.md entry (F-COV-5 fix) is data — grep-verifiable, review-visible, discoverable by future readers. Prefer data over prose-discipline. | AC-6 rewritten to CHANGELOG.md entry (see F-COV-5). The commit-message hint remains in Notes but is not the audit-trail *primary*. |
| F-DP-4 | harden | The story hard-codes six file paths in §Files to touch, but the same list of paths is referenced by the TDD test's constants (`REPO / "docs/phases/04-vuln-llm-fallback-rag/final-design.md"` etc). Path drift risk: if a future roadmap-restructure moves a file, both the story and the test must update. | Introduce a `tests/docs/cross_phase_amendments/_amendment_manifest.py` (or a `.yaml`) that lists each amendment as `(source_adr, target_path, required_substrings)`. Tests iterate the manifest; story cites the manifest instead of six raw paths. Newtype/registry pattern applied to the amendment concept itself — "an amendment is data, not prose." Adding a Phase 7 amendment is one manifest row + one substring assertion. |
| F-DP-5 | (info) | The `_amendment_manifest.py` pattern is generalizable: future phase-close story-writers will want the same shape. YAGNI now (Rule 2), but note as follow-up trigger — if Phase 7 / Phase 8 both ship the same shape (rule of three), promote to `codegenie.docs.amendment_manifest` production module. | Surfaced in Notes; no AC. |
| F-DP-6 | (info) | ADR-P4-006 lives in Phase 4's ADRs directory (CODEOWNERS: Phase 4). A single PR containing edits under both Phase 4's and Phase 5's directories cross-cuts CODEOWNERS; PR reviews will bounce. | Notes-for-implementer amended to recommend either (a) split the four amendments into four PRs (one per target-phase directory), reviewed independently, merged in a train; or (b) if bundled, get pre-review sign-off from Phase 4 + Phase 5 CODEOWNERS in advance. No AC — a review-workflow concern. |
| F-DP-7 | nit | The `verify().ok` gating discipline is invoked inline in AC-7 but not surfaced as a design point: "amendments are data on disk; presence is grep-checkable; there is no runtime-check for consistency between them." That's exactly the *functional core / imperative shell* separation the phase reifies — flag it in Notes. | Notes-for-implementer paragraph added: "This story deliberately uses grep-anchored file-presence checks, not YAML/schema parsing. Rationale — the amendments are prose, and prose is the artifact; parsing would introduce a shell over a core that has no shape. Rule 2 (Simplicity First)." |

## Stage 3 — Researcher

**Skipped.** No `NEEDS RESEARCH` findings. The patterns are well-established in the repo (see S7-02's snapshot-canonicalization and F-DP-1 pattern lineage) and the load-bearing blockers are project-specific (ADR-0005 defective; roadmap already-substituted). No canonical-pattern lookup would move this story forward.

## Stage 4 — Synthesizer + Editor

**Conflict resolutions applied:**
- **Coverage says "verify roadmap Phase 7 section names lower_bound_95"; Consistency says "the current roadmap has no bench_score in Phase 7 section"** → Consistency wins (source of truth); AC-5 rewritten as invariant + positive-presence anchor at the Phase 6.5 line, not Phase 7 section.
- **Coverage says "AC-1/AC-2 must land Phase 4 amendment"; Consistency says "ADR-0005 is defective and S2-05 is BLOCKED"** → Consistency wins; AC-1/AC-2 marked BLOCKED-PARTIAL per F-COV-7.
- **Design-Patterns says "amendment manifest as data"; Rule 2 (Simplicity First) says "three similar lines is better than premature abstraction"** → Manifest pattern is at rule-of-three threshold (Phase 6.5 is the first family; Phase 7 will be second; Phase 8 third per roadmap). Land the *pattern* as a manifest here (F-DP-4) so the third phase does not have to invent it, and cite Rule 2 as the ceiling — no further abstraction.

**Edits applied to the story:**
- Status: `Ready` → `BLOCKED-PARTIAL` (AC-1 / AC-2 gated on ADR-0005 amendment).
- Depends-on line: added Phase 6.5 ADR-0005 amendment gate + Phase 5 S7-03 gate.
- Validation notes block appended after the header.
- Acceptance criteria: 8 existing ACs → 14 ACs (5 amended, 6 new — 5a, 5b, 3b, 6-rewritten, 7-rescoped, 14 the ADR-0005 gate). AC-1 / AC-2 explicitly stamped BLOCKED-PARTIAL with the unblock preconditions.
- TDD plan rewritten: split into `tests/docs/cross_phase_amendments/test_phase65_amendments.py` + `_amendment_manifest.py`; tests anchored on ADR-0009-signature phrasing (AC-4) and repo-global `bench_score.mean` absence (AC-5).
- Files-to-touch table: three new rows (`_amendment_manifest.py`, `CHANGELOG.md`, moved test file); AC-1/AC-2 file rows tagged BLOCKED with unblock precondition.
- Notes-for-implementer: two new paragraphs — ADR-0005 amendment gate + amendment-manifest design rationale + CODEOWNERS-split PR recommendation.

**Verdict:** **HARDENED-with-BLOCKED-partial** — the story is now internally consistent, the landable ACs (3, 3b, 4, 5a, 5b, 6, 7) have oracle-anchored tests that would fail against a wrong implementation, and the un-landable ACs (1, 2) are explicitly gated on the upstream ADR amendment (AC-14) instead of silently blocking the phase-merge train. The rescue for AC-1/AC-2 rides with the phase architect's owed ADR-0005 amendment; this validator does not rewrite the goal (per validator anti-goal).
