# Attempt log — S5-02 (Lockfile + exec adversarial corpus)

## Attempt 1 — 2026-05-15 — GREEN (all 16 ACs across 5 named groups)

### Outcome

Three new adversarial test files land — yarn regex-DoS (2 tests),
planted `node` shim (1 test), unsafe YAML tag (2 tests). Full suite
green: **1474 passed**, 0 new failures, 2 pre-existing xfail, 93.06 %
coverage. Walltime for the three new files: **0.65 s** (AC-16 budget:
10 s). No production code changed — every defense being tested was
already in place from S1-03 (`CSafeLoader`), S2-04 (`exec.run_allowlisted`
+ `_is_sensitive`), and S3-03 (`_yarn._parse_handrolled`).

### Files touched

- `tests/adv/test_regex_dos_yarn_lock.py` — **NEW**. 2 tests:
  (a) runtime budget on ~5 MB pathological multi-spec-header body
  (AC-1 + AC-2), and (b) AST/source-text structural assertion that
  `_yarn.py` neither imports `re` nor references `re.<func>` from any
  of `{parse, _parse_handrolled, _dequote_entry_header, _pyarn_parse}`
  (AC-3). `_pathological_yarn_lock(approx_bytes)` is a module-level
  helper.
- `tests/adv/test_planted_node_on_path_ignored.py` — **NEW**. 1 async
  test that writes a POSIX shim to `tmp_path/fake-bin/node`, prepends
  `fake-bin` to PATH via `env_extra`, passes `SENTINEL_FILE` via
  `env_extra`, sets six sensitive parent-env canaries via
  `monkeypatch.setenv`, passes four sensitive `env_extra` sentinels,
  awaits `cg_exec.run_allowlisted(...)`, and verifies (AC-5) the shim
  ran, (AC-6) no `PARENT_LEAK_CANARY` reached the child for any of six
  parent-env keys, (AC-7) no `EXTRA_LEAK_CANARY_*` reached the child
  via `env_extra` while `MY_LEGIT_VAR=passes-through` did, (AC-8)
  exactly four `subproc.env_extra.sensitive_key_dropped` structlog
  events fired with the expected key set. Decorated
  `@pytest.mark.skipif(sys.platform == "win32", ...)` (AC-9).
- `tests/adv/test_yaml_unsafe_tag.py` — **NEW**. 2 tests:
  (a) `!!python/object/apply:os.system ["touch {tmp_path}/adv-canary.txt"]`
  raises `MalformedYAMLError` with `__cause__` a `yaml.YAMLError` subclass
  and no canary file is created (AC-10 + AC-11), and (b) positive
  control on minimal `lockfileVersion: '6.0'\n` (AC-12).

### Divergences from the literal story text (surfaced + reconciled)

- **Implementation-outline `_yarn.py:110` comma-split surface.** The
  pathological yarn body is exactly as the outline prescribed (multi-spec
  headers `"foo@^1.0.{i}", "foo@^2.0.{i}":` with `version "1.0.0"` lines).
  On Python 3.13 the ~5 MB body parsed in ~0.3 s — well under the 2 s
  budget. No deviation needed.
- **`result: dict[str, object] | None` typing in
  `test_regex_dos_yarn_lock.py`.** Story used `dict | None`; ruff's
  E501 + mypy's `type-arg` rule push toward an explicit element type.
  Annotated `dict[str, object]` with one `# type: ignore[assignment]`
  on the `_yarn.parse(...)` line (the production return type is the
  TypedDict `YarnLock`, which is structurally a `dict[str, ...]` but
  mypy infers it as the wider TypedDict — assignment-narrowing is
  cosmetic-only and does not change the test's behavior). Test mypy is
  not strict-checked in CI (`make verify` runs `mypy --strict src/`
  only), so this is for local-hygiene rather than a CI gate.
- **Docstring path `0008-in-process-parse-caps-not-per-probe-sandbox.md`
  exceeds E501 even on a docstring line.** Rewrote that block as
  "Phase-1 ADR-0008 (...)" / "Phase-1 ADR-0009 (...)" prose instead of
  raw filenames. Same factual content, no line-length workaround.
- **No production code touched.** The story's "Out of scope" is
  explicit: "Refining the hand-rolled yarn parser to handle pathological
  input gracefully — if the parser raises `MalformedLockfileError` under
  1 s, that's acceptable." The hand-rolled scanner already handles
  ~5 MB cleanly under the budget (returns a non-empty `entries` dict).
  Did not "improve" anything; surgical-changes (Rule 3).
- **AC-15 `adv` marker registration owned by S5-01.** Verified
  `pyproject.toml:182-189` already has the `adv` marker registered (from
  S5-01's commit `e903a44`). `--strict-markers` did not fail — the
  dependency is satisfied; no edit to `pyproject.toml` needed.

### Validation evidence (Ralph-Wiggum naive pass)

| AC | Evidence |
|---|---|
| AC-1 | `tests/adv/test_regex_dos_yarn_lock.py::test_yarn_pathological_input_under_runtime_budget` — passed (0.30 s parse, asserts `< 2.0`). |
| AC-2 | Same test — `len(result["entries"]) > 0` asserted post-parse; silent-empty branch forbidden. |
| AC-3 | `tests/adv/test_regex_dos_yarn_lock.py::test_yarn_handrolled_scanner_uses_no_regex` — AST walker, passed. |
| AC-4 | `test_planted_node_shim_runs_in_stripped_env` — `cg_exec.run_allowlisted([...])` direct, async. |
| AC-5 | Same test — `sentinel.read_text().splitlines()[-1] == "node-shim-invoked"`. |
| AC-6 | Same test — `_PARENT_LEAK_CANARY not in body` + per-key `parsed[key] != _PARENT_LEAK_CANARY`. |
| AC-7 | Same test — `_EXTRA_CANARY_PREFIX not in body` + `parsed["MY_LEGIT_VAR"] == "passes-through"`. |
| AC-8 | Same test — `len(drops) == 4` and keyset `{OPENAI, ANTHROPIC, AWS_SECRET, GITHUB}`. |
| AC-9 | `@pytest.mark.skipif(sys.platform == "win32", ...)` decorator. |
| AC-10 | `tests/adv/test_yaml_unsafe_tag.py::test_unsafe_python_object_tag_refused` — `pytest.raises(MalformedYAMLError)` + `not canary.exists()`. |
| AC-11 | Same test — `isinstance(exc_info.value.__cause__, yaml.YAMLError)`. |
| AC-12 | `tests/adv/test_yaml_unsafe_tag.py::test_safe_yaml_positive_control_loads_minimal_lockfile` — `result.get("lockfileVersion") == "6.0"`. |
| AC-13 | All tests assert specific typed exceptions + specific outcomes (entries shape / sentinel marker / canary absence). |
| AC-14 | All env / module-attribute mutation routed through `monkeypatch.setenv` / `monkeypatch.setattr`. Grep: no `os.environ[` writes in any new file. |
| AC-15 | All 5 tests decorated `@pytest.mark.adv`. |
| AC-16 | 5-test walltime: **0.65 s** vs. 10 s budget. |

### Refactor decisions (design-pattern lens)

- **Newtype / domain-primitive** — not applicable; tests speak in raw
  `bytes` / `str` shapes that are the production contract already.
- **Functional core, imperative shell** — `_pathological_yarn_lock` is
  a pure functional-core builder taking `approx_bytes: int` and
  returning `bytes`; the imperative test shell does I/O around it.
  Already structured this way; no further extraction.
- **Helper extraction (rule of three)** — explicitly deferred per the
  validator's HARDENED block: S5-03 needs neither a shim factory nor a
  PATH-prepend primitive. `SHIM_BODY` / `UNSAFE_YAML` / the pathological
  yarn builder all stay at module scope. If a Phase-2 story adds a
  second consumer, lift then.
- **Strategy / plugin** — N/A; these are leaf adversarial-corpus tests.
- **Sum type / tagged union** — N/A.

### Lesson for next attempt (cross-story, append to `_lessons.md`)

When the validation report says "production code already correct, this
is a permanent-fixture pin," do not pre-emptively touch production code
in the executor pass. Three lines of fully-passing tests landed against
S1-03 / S2-04 / S3-03 production code without any source-tree changes
— the entire green pass was tests-only, exactly as the HARDENED story
predicted (no `Files to touch` outside `tests/adv/`). Trust the
validation block; verify by running, not by editing.
