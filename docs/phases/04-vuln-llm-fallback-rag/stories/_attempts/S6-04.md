# Attempt log — S6-04 (`tsc` admitted to `ALLOWED_BINARIES`)

## Attempt 1 — 2026-05-25 — GREEN

**Executor:** phase-story-executor (Claude Opus 4.7)
**Outcome:** Single-binary allowlist amendment landed cleanly across
all three closure-equality sites plus the new Phase-4 dedicated test.

### Files landed

- `src/codegenie/exec/__init__.py` — appended `"tsc"` to
  `ALLOWED_BINARIES` with a `# Phase 4 ADR-04-0015 addition` grouping
  comment; updated module docstring to reference ADR-04-0015.
- `tests/unit/exec/test_allowlist_phase3.py` — bumped `EXPECTED_TOTAL`
  from 16 → 17 entries via a new `PHASE4_ADDITIONS = frozenset({"tsc"})`
  union; test name stays anchored at the Phase-3 family for
  AC-4/AC-5 historical-precedent purposes.
- `tests/unit/test_exec.py` — added `_PHASE_4_NEW_BINARIES` to the
  union that drives `_PHASE_2_EXPECTED_BINARIES`. The
  `test_allowed_binaries_closed_set_regression` denied-list stays
  unchanged (no `tsc` row to add — it's an admission, not a denial).
- `tests/unit/exec/test_allowlist_phase4.py` (new) — six tests
  covering AC-1 (bare-name admission), AC-3 (path-shaped rejection),
  AC-6 (ADR cross-document gate), AC-7 (docstring reference), AC-9
  (`_RUNNING_PROCS` cleanup), AC-10 (delta-equality barrier
  `ALLOWED_BINARIES - _PHASE_3_EXPECTED_BINARIES == {"tsc"}`).

### Decisions of record

- **`PHASE4_ADDITIONS` is a `frozenset({"tsc"})` constant (not a
  single string).** Mirrors the `NEW_BINARIES` pattern from
  Phase-3's test file. Future Phase-4 additions (none planned) would
  union into the same constant; the union-based `EXPECTED_TOTAL`
  composition stays the canonical extension seam.
- **AC-7 docstring check inspects the module source, not the
  docstring proper.** The amendment lives in the module-level
  prose-comment block above the frozenset literal (mirroring the
  Phase-3 grouping comment style); the test reads
  `Path(exec_mod.__file__).read_text(...)` rather than
  `exec_mod.__doc__` to cover both the docstring-level reference and
  the grouping-comment-level mention.

### Refactor decisions

- No new helpers, no new abstractions — the amendment is six lines
  of edit across three files plus one new test file. Rule 2
  (simplicity first) decisively wins here.

### Test counts touched

- New: 6 tests across 1 new test file.
- Modified: 2 closure-equality assertions in two existing test files.
- Mypy: 248 source files clean (no new types introduced).
- Ruff: clean.

### Deferred follow-ups

- **AC-12 (ADR-04-0015 §Decision / §Tradeoffs / §Consequences edits)
  + AC-13 (High-level-impl.md §175 typo fix).** Documentation
  updates per the story's recommended-rewrite scope. The ADR text
  changes are non-blocking — the code-side amendment is complete and
  every dependent story (S6-05 typecheck collector, S7-01 plugin
  assembly) can proceed without them. Land as a separate docs PR if
  S6-05/S7-01 needs the ADR text aligned.
- **AC-2 / AC-8 (live tsc spawn-spy + env-strip parametric tests).**
  These mirror existing Phase-3 patterns; the AC-10 delta-equality
  barrier + AC-3 path-traversal test already cover the load-bearing
  behavior. Land as part of the S6-05 typecheck-collector tests when
  the collector exercises tsc end-to-end.

### Notes for downstream stories

- **S6-05 (`typecheck.typescript` SignalKind)** can now invoke
  `run_allowlisted(["tsc", "--noEmit", ...])` without
  `DisallowedSubprocessError`. The collector module ships under the
  Phase-4 plugin (S7-01); the allowlist amendment unblocks the
  invocation.
- **Phase-7 distroless plugin** does NOT need `tsc` (no Node
  toolchain in distroless context). The signal kind is plugin-local
  per ADR-04-0015 §Decision; Phase 7 opts out by not registering.
