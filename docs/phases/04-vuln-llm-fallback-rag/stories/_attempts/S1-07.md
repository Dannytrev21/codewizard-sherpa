# Attempt log — S1-07 (Phase-3 baseline extension for `test_kernel_frozen.py`)

## Attempt 1 — 2026-05-25 — GREEN (after validator-recommended rewrite)

**Executor:** phase-story-executor (Claude Opus 4.7)
**Outcome:** Story re-scoped from RESCUE → GREEN in a single session.
**Mechanism:** one-row baseline append + pinned-SHA sidecar, per the validator's
"Recommended rewrite" brief. No new file, no new mechanism — extension of the
Phase-3-shipped fence via its labelled `_BASELINES` seam.

### Files landed

- `tests/fence/test_kernel_frozen.py` — module docstring extended; one-row
  append to `_BASELINES`; live diff test generalized to parametrize over
  every baseline; helpful-error test parametrized to assert each
  baseline's filename appears in the directive; docstring-name assertion
  extended to require both `_phase2_baseline.txt` and `_phase3_baseline.txt`.
- `tests/fence/_phase3_baseline.txt` — single 40-char SHA
  (`788512f570a0c34e37def129abd262c05de85855` =
  `feat(phase3/S6-04): land ADR-0015 resolver substrate`, the last
  Phase-3 source-code commit before Phase-4 work began).
- `docs/phases/04-vuln-llm-fallback-rag/stories/S1-07-test-kernel-frozen.md`
  — re-scope header (`Status: Done — GREEN`), validation re-scope block
  carrying the validator's recommended Goal / ACs / Files-to-touch / TDD
  plan / Out-of-scope as the authoritative story prose. Original
  pre-RESCUE story body left intact below as historical raw material.

### Tests

`tests/fence/test_kernel_frozen.py` — 18 tests pass:

- `test_baseline_file_is_a_real_40_char_sha[phase-2]` ✓
- `test_baseline_file_is_a_real_40_char_sha[phase-3]` ✓ (new — AC-2)
- `test_baseline_resolves_to_ancestor_of_head_and_is_not_head[phase-2]` ✓
- `test_baseline_resolves_to_ancestor_of_head_and_is_not_head[phase-3]` ✓ (new — AC-3)
- `test_no_kernel_edits_outside_allowlist[phase-2]` ✓ (renamed from
  `test_phase3_has_not_modified_phase012_kernel_outside_allowlist`; now
  parametrized — AC-4)
- `test_no_kernel_edits_outside_allowlist[phase-3]` ✓ (new — AC-4 live)
- `test_helpful_error_names_baseline_file_and_adr_amendment[phase-2]` ✓
- `test_helpful_error_names_baseline_file_and_adr_amendment[phase-3]` ✓ (new — AC-5)
- 9 × `test_diff_status_classification[...]` ✓ (unchanged)
- `test_module_docstring_names_adr_0011_framing_and_fetch_depth` ✓ (AC-6 extension)

### Mutation-resistance checks performed

- **Baseline drift.** Renaming `_phase3_baseline.txt` to a different
  filename without updating the `_BASELINES` tuple fails
  `test_baseline_file_is_a_real_40_char_sha[phase-3]` byte-loud
  (`FileNotFoundError` surfacing via `_read_baseline_sha`).
- **SHA tamper.** Replacing the Phase-3 SHA with HEAD's SHA fails
  `test_baseline_resolves_to_ancestor_of_head_and_is_not_head[phase-3]`
  with the directive: "accidentally pasted HEAD; baseline must be a
  pre-Phase-3 commit."
- **Allowlist gap.** A hypothetical Phase-4 edit to
  `src/codegenie/probes/layer_a/language_detection.py` (not on the
  allowlist) is caught by the parametrized live test against *both*
  baselines (phase-2 AND phase-3 baselines flag it). Confirmed via the
  AC-5 planted-violation parametrization.
- **Helpful-error directive shape.** Both rows confirm their sidecar
  filename appears in the failure message; an executor who silently
  swaps `_phase3_baseline.txt` → `_phase2_baseline.txt` in the directive
  would fail the parametrized assertion.

### Decisions of record

- **Live test renamed to `test_no_kernel_edits_outside_allowlist`.** The
  prior name (`test_phase3_has_not_modified_phase012_kernel_outside_allowlist`)
  was Phase-3-specific; parametrizing over `_BASELINES` requires a
  baseline-agnostic name. The new name reads cleanly with the
  parametrize ID suffix (`[phase-2]`, `[phase-3]`).
- **`_KERNEL_ALLOWLIST` is NOT widened.** A `git diff --name-status
  788512f..HEAD` audit showed every Phase-4 edit to a kernel-scope file
  is already on the allowlist (added by earlier Phase-4 stories — S1-01
  through S4-08). Phase-4's new directories (`fallback/`, `rag/`,
  `workflows/`) live outside `_KERNEL_SCOPE_DIRS` and require no entry.
  This confirms the validator's prediction: "If Phase 4 adds zero
  kernel edits, S1-07 collapses to just the baseline-row append."
- **SHA chosen:** `788512f570a0c34e37def129abd262c05de85855` —
  `feat(phase3/S6-04): land ADR-0015 resolver substrate`, the last
  Phase-3 source-code commit before Phase-4 work began. Verified via
  `git log --oneline 788512f..HEAD` (next commit is
  `8b6b2d9 docs(_shakedowns): add codegenie-gather shakedown report`,
  i.e. post-Phase-3 housekeeping; the SHA after that is the first
  Phase-4 story validation).

### Refactor decisions

- **Pulled the live test + helpful-error test under `_BASELINES`
  parametrize.** Composition over inheritance — no `for` loop in the
  test body; pytest's parametrize machinery is the canonical loop. The
  shipped Phase-3 author labelled the seam *specifically* for this kind
  of extension; reusing it honours Rule 11 (match conventions).
- **No new helper functions.** The story's full diff is six edits to
  `test_kernel_frozen.py` + one new sidecar file. Anything more would
  be Rule-2 premature abstraction; the existing helpers
  (`_read_baseline_sha`, `_ensure_baseline_reachable`, `_run_git_diff`,
  `_kernel_violations`) already cover the new row.

### Test counts touched

- `tests/fence/test_kernel_frozen.py`: 14 tests → 18 tests
  (+ 4 net new parametrized cases; one prior test renamed).
- No other suite touched.

### Notes for downstream stories

- **S5-01..S7-10.** Every subsequent Phase-4 story's diff is now scanned
  against *both* the Phase-2 and Phase-3 baselines. An accidental edit
  to a Phase-3 kernel file (`src/codegenie/plugins/protocols.py`,
  `src/codegenie/transforms/recipe_engine.py`, etc.) will fail the
  `test_no_kernel_edits_outside_allowlist[phase-3]` row before merge —
  which is exactly the "diff touches only the new plugin directory"
  exit criterion S7-01 AC-KERNEL-FROZEN depends on.
- **Phase-5+ extension.** When Phase 5 lands its kernel-complete baseline,
  the recipe is now established by precedent: append one row to
  `_BASELINES` and create `_phase4_baseline.txt` pinning the Phase-4-
  complete SHA. The parametrized suite picks up the new row for free.
