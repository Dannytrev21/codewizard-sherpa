# Attempt log — S4-04 YAML canonical records + `manifest.yaml` chain head

## Attempt 1 — 2026-05-25 (GREEN on first pass)

### What shipped

| File | Change |
|---|---|
| `src/codegenie/rag/store.py` | **Extended.** Added `_roll_chain_head` (pure functional core, `Iterable[bytes] -> ChainHead`), `_compute_chain_head` (imperative shell that re-reads canonical YAML records and translates `FileNotFoundError` → `StoreCorrupted`), `_canonical_yaml_dump` (single PyYAML serialization surface), `_atomic_write_text` (tmp + `os.replace`), `_validate_record_id_path_safe` (AC-14 path-traversal guard), `_parse_manifest_or_raise` (defensive translator → `StoreCorrupted`), `_Manifest` Pydantic frozen model (`schema_version: Literal[1]`, `records`, `chain_head`). Rewrote `_load_existing_record_ids` to read `manifest.yaml` (order-of-truth) instead of chromadb. Extended `add()` to write canonical YAML record first → chromadb → manifest last (all inside the existing `asyncio.Lock`); on manifest-write failure pops the in-memory append so the next successful add re-rolls a consistent manifest (in-process self-heal — AC-12). Rewrote `digest()` to `StoreDigest(_compute_chain_head(...))` rolling over canonical YAML bytes (was record-ID strings in S4-03). Added `_records_dir` to `__slots__`. |
| `src/codegenie/rag/models.py` | **Extended.** Added `SolvedExample.from_yaml(path: Path) -> Self` classmethod sharing the exact `model_validate(yaml.safe_load(...))` core the AC-6 Hypothesis roundtrip property exercises. |
| `tests/unit/rag/test_store_yaml_canonical.py` | **Created.** 17 tests covering AC-1, AC-2, AC-3, AC-4 (both first-add-fails and Nth-add-fails cases), AC-5 (consistency + independent oracle), AC-7 (byte-identical manifest), AC-9 (unknown schema_version, malformed YAML, missing keys), AC-10 (round-trip + negative), AC-12 (manifest-write failure leaves YAML + chroma intact), AC-13 (manifest references missing record), AC-14 (path-traversal rejection), AC-15 (empty-store invariants). |
| `tests/unit/rag/test_chain_head_monotonic.py` | **Created.** 4 tests covering AC-8 — monotonic-unique (10 sequential adds yield 10 distinct heads), pure-core prefix-stability (synthetic-byte table test catches the rehash-all-each-add mutant), empty-iter equals `blake3(b"")`, two-store cross-check (store A with N records vs store B with N+5 — A's `digest()` equals BLAKE3 of B's first-N canonical bytes). |
| `tests/property/test_solved_example_yaml_roundtrip.py` | **Extended.** Hypothesis property `test_solved_example_yaml_roundtrip_is_exact` with explicit per-field strategies (`solved_examples()` composite — every field bound, no inference). The serialize side routes through the same `_canonical_yaml_dump` helper `add()` uses; the parse side mirrors `from_yaml`. `@settings(max_examples=50, deadline=None, database=None)`. |

### Per-AC evidence

| AC | Status | Evidence |
|---|---|---|
| AC-1 — canonical YAML record written first; sorted keys (full top-level ordering) | ✓ | `tests/unit/rag/test_store_yaml_canonical.py::test_add_writes_canonical_yaml_record` + `::test_add_writes_top_level_keys_in_sorted_order` |
| AC-2 — `manifest.yaml` updated last; byte-identical PyYAML options to record dump | ✓ | `::test_chain_head_equals_digest_after_add` (`manifest["records"] == ["ex-a", "ex-b"]` + `schema_version == 1`) |
| AC-3 — `_load_existing_record_ids` reads from `manifest.yaml`, not chromadb | ✓ | `::test_load_existing_record_ids_from_manifest` |
| AC-4 — chromadb-write failure semantics (orphan YAML, no manifest update, no `_record_ids` append) | ✓ | `::test_chromadb_failure_on_first_add_leaves_yaml_orphan` + `::test_chromadb_failure_on_nth_add_leaves_manifest_unchanged` (byte-identical manifest snapshot) |
| AC-5 — `chain_head == digest()`; one shared `_compute_chain_head`; `digest()` re-wraps `ChainHead → StoreDigest` at the Protocol boundary | ✓ | `::test_chain_head_equals_digest_after_add` (consistency) + `::test_chain_head_is_blake3_of_concatenated_canonical_yaml` (independent oracle, deliberately non-sorted insertion: `ex-b` before `ex-a`) |
| AC-6 — Hypothesis YAML roundtrip property exact for Pydantic equality | ✓ | `tests/property/test_solved_example_yaml_roundtrip.py::test_solved_example_yaml_roundtrip_is_exact` |
| AC-7 — `chain_head` deterministic across two stores in same order (raw-bytes manifest compare) | ✓ | `::test_two_stores_same_order_produce_identical_manifest_bytes` |
| AC-8 — chain head monotonic + unique AND prefix-stable | ✓ | `tests/unit/rag/test_chain_head_monotonic.py::test_chain_head_monotonic_and_unique` + `::test_roll_chain_head_is_prefix_stable_on_synthetic_bytes` (pure-core table) + `::test_chain_head_prefix_stable_across_two_stores` |
| AC-9 — schema-versioned manifest; all malformed cases → `StoreCorrupted` (never raw `YAMLError` / `ValidationError`); `schema_version` checked before `_Manifest.model_validate` | ✓ | `::test_unknown_schema_version_raises_store_corrupted` + parametrized `::test_malformed_manifest_raises_store_corrupted` |
| AC-10 — `SolvedExample.from_yaml(path)` round-trip + negative test | ✓ | `::test_from_yaml_roundtrips_a_written_record` + `::test_from_yaml_raises_validation_error_on_malformed` |
| AC-11 — `ruff check` + `ruff format --check` + `mypy --strict` clean | ✓ | `ruff check src/ tests/` all checks passed; `ruff format --check` 860 files already formatted; `mypy --strict src/` 231 source files OK |
| AC-12 — manifest-write failure leaves YAML record + chromadb on disk; in-process self-heal via `_record_ids.pop()` then next successful `add()` rewrites a consistent manifest | ✓ | `::test_manifest_write_failure_leaves_yaml_and_chroma_intact` |
| AC-13 — manifest references missing record file → `StoreCorrupted` (not raw `FileNotFoundError`) | ✓ | `::test_manifest_references_missing_record_file_raises_store_corrupted` |
| AC-14 — record id is filesystem-path-safe (`/`, `\`, NUL, `..`, leading-`.` rejected before any write) | ✓ | `::test_record_id_with_path_traversal_rejected_before_write` |
| AC-15 — empty-store invariants (no `manifest.yaml`, `_record_ids == []`, `digest()` is `blake3(b"")`); first `add()` materializes manifest | ✓ | `::test_empty_store_has_no_manifest_and_empty_chain_digest` + `::test_empty_store_writes_manifest_on_first_add` |

### Gates

| Gate | Result |
|---|---|
| `pytest tests/unit/rag/ tests/property/test_solved_example_yaml_roundtrip.py --no-cov` | 137 passed |
| `pytest tests/fence/ tests/unit/test_pyproject_fence.py --no-cov` (S4-04-touched fences still green) | 622-test scoped run green |
| `ruff check src/ tests/` | all checks passed |
| `ruff format --check src/ tests/` | 860 files already formatted |
| `mypy --strict src/` | 231 source files no issues |
| `lint-imports --config pyproject.toml --no-cache` | **11 contracts kept, 0 broken** (no new edges — `_load_existing_record_ids` no longer imports chromadb in the manifest-read path, but the existing `ignore_imports = ["codegenie.rag.store -> chromadb"]` edge from S4-03 still covers the remaining `__init__` import) |

### Cross-story housekeeping (Rule 7 — surface conflicts, don't average them)

The story's Validation notes flagged that S4-03 AC-6 prose still describes `digest()` rolling over record-ID **strings** while this story rerolls over canonical YAML **bytes**. The S4-03 `digest()` test was the order-sensitive one in `tests/unit/rag/test_store.py::test_digest_is_insertion_order_sensitive` — that test continues to pass after the reroll (the order-sensitive contract holds; the *input domain* changed from IDs to bytes). The S4-03 prose update was deferred — the docstring of `digest()` itself now carries the canonical-YAML-bytes contract (S4-04 §"Re-wraps :func:`_compute_chain_head`..."), and the S4-03 story file's narrative wording is left untouched to preserve the validator's audit trail. A future doc sweep can amend S4-03 AC-6 prose once Phase 4 is fully closed.

### Pre-existing flakes (carry-over from S4-03 L-2)

- `tests/adv/test_tsconfig_pathological.py::test_gather_under_pathological_tsconfig_silently_swallows_under_two_seconds` — macOS timing flake (documented in `_lessons.md` L-2; CI Linux clean).
- 3 environmental failures on `pytest -q` from a developer box (`tests/unit/test_lint_imports_canary.py::*` + `tests/unit/test_precommit_and_docs_config.py::test_pre_commit_run_all_files_exits_zero`) when the `.venv/bin` directory is not on `PATH` at test-collection time. The tests use `shutil.which("lint-imports")` / `which("pre-commit")` so they require the venv activated; CI activates the venv before `make check`. Not introduced by this story.
- `tests/integration/portfolio/test_portfolio_sweep.py::test_portfolio_sweep` — fails locally when stale `.codegenie/` directories exist under `tests/fixtures/portfolio/<fixture>/` from prior local `codegenie gather` invocations (the regen_golden `--check` path runs gather against the canonical fixtures, mutating them, so the AC-31 byte-identity assertion fails). CI starts from a clean checkout and the dedicated `portfolio` matrix job passes (`1 passed in 23.15s` on the merge-base of master). Not introduced by this story.

### Lessons (L-S404-*)

- **L-S404-1** — `_compute_chain_head` deliberately re-reads every record file on each `add()` (O(N) per add, O(N²) for full ingest). A running hasher held on `self` would be O(1) amortized but is hidden mutable state that can desync from disk and make `digest()` silently lie (Rule 12 violation). The stateless re-read keeps `digest()` a pure projection over disk and makes AC-12's "next `add()` recomputes from disk" recovery actually true. Revisit only if a perf test against the < 50 ms/add budget fails — never speculatively (story Notes §10).
- **L-S404-2** — `_Manifest` deliberately gets **no** `from_yaml` classmethod even though the public `SolvedExample` does (AC-10). The defensive inline parse in `_load_existing_record_ids` (via `_parse_manifest_or_raise`) is the only reader — translating every malformed case to `StoreCorrupted` with a diagnostic. A `_Manifest.from_yaml` would force every caller through the typed `pydantic.ValidationError` surface instead of the intended `StoreCorrupted` family. Only the public `SolvedExample.from_yaml` is named (S4-07's `codegenie rag rebuild` consumer).
- **L-S404-3** — `schema_version` is checked on the **raw** dict *before* `_Manifest.model_validate(...)` because `_Manifest` is `extra="forbid"` with `schema_version: Literal[1]` — without the pre-check, a hypothetical v2 manifest (new fields, `schema_version: 2`) would fail validation with a generic `ValidationError` instead of the intended `StoreCorrupted("unknown manifest schema_version")` diagnostic that names the upgrade path. The genuine Open/Closed seam (a table keyed on `schema_version`) arrives only when Phase 11 introduces v2 — not now (Rule 2; story Notes §8).
