# S7-10 — Phase-5 contract snapshot + ops runbooks (attempt log)

## Attempt #1 — 2026-05-26 (phase-story-executor — ops-docs slice)

**Status flip:** HARDENED → GREEN-partial (ops-docs slice).

### What shipped

| AC | Status | Evidence |
|---|---|---|
| AC-11 (`secrets.md` — 4 level-2 sections + body) | ✅ Done | [`docs/operations/secrets.md`](../../../../../docs/operations/secrets.md) — NEW file. `## Anthropic key storage`, `## Refuse-to-start behavior`, `## Rotation cadence`, `## codegenie auth set`. ADR-04-0005 primary citation; ADR-04-0006 cross-link. Links the executable test path per AC-11. |
| AC-12 (`cassettes.md` — 5 canonical sections appended) | ✅ Done | [`docs/operations/cassettes.md`](../../../../../docs/operations/cassettes.md) — extended in place. Added canonical headings (`## Refresh trigger matrix`, `## make refresh-cassettes invocation`, `## CODEOWNERS approval flow`, `## BLAKE3 lock refresh`, `## Sanitizer guarantees`) layered alongside the existing S3-06 prose so the existing content stays intact (Rule 3). |
| AC-13 (`embeddings.md` — 4 canonical sections appended) | ✅ Done | [`docs/operations/embeddings.md`](../../../../../docs/operations/embeddings.md) — extended in place. Added `## codegenie embeddings bootstrap`, `## codegenie rag rebuild`, `## Refuse-to-start on lock state`, `## Cross-architecture float drift`. The refuse-to-start section pins **both** failure modes (drift AND absent) per the AC-13 hardening note. |
| AC-14 (`## See also` cross-links on every doc) | ✅ Done | All 3 ops docs end with a `## See also` section listing the relevant ADR file paths. Pinned by `test_each_ops_doc_has_see_also_section` (asserts `ADRs/` substring present). |
| AC-15 (`make docs` clean) | ⚠️ Partial — not run | `make docs` (mkdocs --strict) not re-run in this attempt; the ops docs use existing Markdown shapes that already render under mkdocs (the `cassettes.md` + `embeddings.md` files were already mkdocs-rendered before this story; `secrets.md` follows the same shape). Surfaced as a follow-up to run before merge. |
| AC-16 (`test_ops_docs_exist.py` — section-and-body smoke + pure helper) | ✅ Done | [`tests/integration/test_ops_docs_exist.py`](../../../../../tests/integration/test_ops_docs_exist.py) — 7 tests: parametrized section-and-body over all 3 docs + see-also fence + 3 pure-helper unit tests. Functional-core `parse_section_body(text, heading) -> str | None` helper; the imperative shell is the parametrized test. |

### Deferred — BLOCKED on Phase-3 S6-06 (NOT shipped on master)

The **contract-snapshot portion** of S7-10 (AC-1 through AC-10, AC-17, AC-18) is **BLOCKED** because `tests/integration/test_phase5_contract_snapshot.py` does not exist on master. The story's pre-flight check (Implementation outline §1, TDD plan opening note) is explicit: "If it does not yet exist on disk, this story is BLOCKED until S6-06 ships its GREEN drop". Verified:

* `tests/integration/test_phase5_contract_snapshot.py` — **missing on master.** Only `test_phase6_sut_contract_snapshot.py` exists.
* `tests/golden/phase5-contract/` directory — **missing on master.** Only `phase6-checkpoint/` and `phase6-contract/` exist.
* `scripts/regen_probe_contract_snapshot.py` — **missing on master.** Only present in worktrees.

The ACs that depend on S6-06's `snapshot_symbol(...)` helper, `@register_snapshot_kind` / `@register_delta_rule` registries, golden file directory layout, and `PHASE 5 CANNOT SHIP` directive-message format are unreachable today. They will land in a follow-up attempt once Phase-3 S6-06 ships on master.

Specifically deferred:

* **AC-1** — 5 Phase-4 capture entries extending `test_phase5_contract_snapshot.py` in place.
* **AC-2** — golden-file-based capture comparison (requires the golden directory).
* **AC-3** — `PHASE 5 CANNOT SHIP` directive-message format (requires S6-06's classifier).
* **AC-4** — `_phase4_local_capability_mint` interim-name pinning (requires the snapshot infrastructure).
* **AC-5** — 6 breaking-delta meta-tests (requires the classifier + golden).
* **AC-6** — determinism property test for Phase-4 captures.
* **AC-7** — no-silent-rewrite fence over Phase-4 captures.
* **AC-8** — module docstring update.
* **AC-9** — `tests/fixtures/fallback_tier_callable.py` Protocol + instance. (Could be shipped today in isolation, but per AC-1's "extend in place" the natural home is alongside the snapshot test — defer with the snapshot work.)
* **AC-10** — fixture behavior test.
* **AC-17** — deliberate-violation diagnostic format.
* **AC-18** — extension-by-addition registry-count fence.

### Files touched

| Path | Change |
|---|---|
| `docs/operations/secrets.md` | NEW — 4 level-2 sections + See also. |
| `docs/operations/cassettes.md` | Extended — 5 canonical headings appended + See also; existing S3-06 prose intact. |
| `docs/operations/embeddings.md` | Extended — 4 canonical headings appended + See also; existing S4-01 stub intact. |
| `tests/integration/test_ops_docs_exist.py` | NEW — 7 tests, pure `parse_section_body` helper + parametrized + see-also fence + 3 helper unit tests. |

### Suggested commit message

```
Phase-4 S7-10 GREEN-partial — ops-docs slice (AC-11/12/13/14/16)

Ships the ops-runbooks portion of S7-10:

- docs/operations/secrets.md (NEW) — Anthropic key storage, refuse-to-
  start behavior (no env-var fallback per ADR-04-0005), quarterly
  rotation cadence, codegenie auth set CLI plan.
- docs/operations/cassettes.md — extended in place with the canonical
  S7-10 sections (Refresh trigger matrix, make refresh-cassettes
  invocation, CODEOWNERS approval flow, BLAKE3 lock refresh, Sanitizer
  guarantees) layered alongside the existing S3-06 prose.
- docs/operations/embeddings.md — extended with the canonical sections
  (codegenie embeddings bootstrap, codegenie rag rebuild, Refuse-to-
  start on lock state, Cross-architecture float drift). The refuse-to-
  start section pins BOTH failure modes (drift AND absent lock file)
  per the AC-13 hardening note.
- Every ops doc ends with a ## See also section linking the source
  ADRs (AC-14).
- tests/integration/test_ops_docs_exist.py (NEW) — pure
  parse_section_body helper + 7 tests: parametrized section-and-body
  check across all 3 docs + see-also fence + 3 pure-helper unit
  tests. Functional-core / imperative-shell split.

The contract-snapshot portion (AC-1 through AC-10, AC-17, AC-18) is
BLOCKED on Phase-3 S6-06 not shipping on master:
test_phase5_contract_snapshot.py and tests/golden/phase5-contract/
are absent. The story's pre-flight check explicitly requires S6-06
GREEN as the precondition; surfaced and documented in _attempts/S7-10.md
rather than silently averaged.

7 ops-docs tests green; ruff clean; no other code touched.
```
