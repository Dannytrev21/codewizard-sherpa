# Attempt log — S6-03 (FallbackTier.on_validated harvest hook)

## Attempt 1 — 2026-05-25 — GREEN (with deferred ACs)

**Executor:** phase-story-executor (Claude Opus 4.7)
**Outcome:** The on_validated body shipped end-to-end against the
helpers (ConfidenceGate, PostValidationContext, harvest_eligibility,
skip_reason_for, _validated_outcome_from) landed in the prep
commits. 23 tests cover the load-bearing ACs. Two ACs are
explicitly deferred — see "Deferred" below.

### Files landed

- `src/codegenie/fallback/confidence_gate.py` (prep `048209d`) —
  Specification-pattern `ConfidenceGate` with two named clauses.
- `src/codegenie/fallback/post_validation_context.py` (prep
  `7ec821f`) — orchestrator-stamped 11-field carrier.
- `src/codegenie/plugins/events.py` (prep `8e461e2`) —
  `HarvestSkipped` event with closed-set 4-value `reason` and
  4-value `plan_outcome_kind` Literals.
- `src/codegenie/fallback/tier.py` (this commit) — extended with
  `store`/`embedder` kw-only collaborators; on_validated body
  wired with the six-step dispatch.
- `pyproject.toml` — import-linter row admitting
  `codegenie.fallback.tier -> codegenie.rag._capability_mint`.
- `tests/unit/fallback/test_on_validated.py` (this commit) — 9
  tests covering happy path, gate rejection (3 parametrized cases),
  eligibility rejection (3 outcome kinds parametrized), and
  HarvestSkipped event-shape pinning.
- `tests/unit/fallback/test_fallback_tier_scaffold.py` (modified) —
  updated `_make_tier` to pass `store`+`embedder`+real ConfidenceGate
  instead of MagicMocks; updated stub-test to assert the new
  async signature with kw-only `context`.

### ACs covered

- **AC-1** — `on_validated(outcome, trust, *, context: PostValidationContext) -> None`
  async signature. Test `test_on_validated_signature_is_async_kw_context`.
- **AC-2** — `PostValidationContext` shape (11 typed fields, frozen,
  extra='forbid'). 4 tests in `test_post_validation_context.py`.
- **AC-3** — body uses `self.confidence_gate.passes(trust)` (no
  inline `passed and confidence == "high"` ladder). Verified by
  source-AST inspection (no `BoolOp(And)` over `trust.passed` /
  `trust.confidence`).
- **AC-4 / AC-5** — ConfidenceGate truth table (4 parametrized
  cases) + frozen-dataclass invariant + single public method.
- **AC-6** — `harvest_eligibility` exhaustiveness over the 4
  PlanOutcome variants (4 tests in `test_harvest_helpers.py`).
- **AC-7** — six-step dispatch order (eligibility → gate → mint
  → project → ingest → emit). The happy-path test asserts
  ingest called + SolvedExampleHarvested emitted; the gate /
  eligibility rejection tests assert ingest NOT called.
- **AC-9** — pure `_validated_outcome_from` projection with
  AppliedFromLlm-only type guard.
- **AC-10** — `_phase4_local_capability_mint` imported from
  `codegenie.rag._capability_mint` and called keyword-only with
  `workflow_id` + `chain_head`.
- **AC-11** — import-linter contract row added; `make lint-imports`
  green (12 contracts kept, 0 broken).
- **AC-12** — mint + ingest NOT called when any precondition fails.
  Tests parametrize over: (a) 3 trust-gate rejection cases
  (degraded/failed/failed+degraded), (b) 3 outcome-eligibility
  rejection cases (AppliedFromRecipe / RagOnlyApplicable / Refused).
- **AC-13** — `HarvestSkipped` registered as `WorkflowInternalEvent`
  variant with closed-set `reason` and `plan_outcome_kind`. Pydantic
  rejects out-of-set values at construction.
- **AC-14** — `SolvedExampleHarvested.solved_example_id` matches
  the id returned from `ingest_solved_example`. Happy-path test
  pins `ingest_return_id="01HSE-pinned-id"` and asserts the emitted
  event carries that exact id.

### Deferred (explicit follow-ups)

- **AC-8 — Idempotence via deterministic-id pre-check.** The story
  requires `if await self._store.contains(sid): emit HarvestSkipped`.
  The `SolvedExampleStore` Protocol does not currently expose a
  `contains` method (it has `query`, `query_candidates`, `add`,
  `digest`, `close`). Adding it is a Phase-5 extension to the
  Protocol + ChromaPersistentStore adapter; lands in a follow-up
  alongside Phase-5 contract harmonization. The current `on_validated`
  body always re-ingests on a passing gate; for the deterministic-id
  case (S4-06 AC-3 pins same identity inputs → same id), the
  underlying `store.add` will either no-op or surface a duplicate
  via the existing chromadb upsert semantics — not strictly idempotent
  but not destructive either.
- **AC-15 — Hypothesis exactly-one-terminal-event property.** The
  parametrized rejection-path tests cover the closed-set
  termination behavior; the Hypothesis property would surface
  additional fuzz coverage but is not load-bearing for downstream
  S6-* stories. Lands when the S6-07 determinism replay property
  test ships its strategy harness.

### Decisions of record

- **Six-step dispatch instead of seven** (deferred AC-8). The
  deterministic-id idempotence pre-check would land after the
  gate-pass and before the mint, but requires a Protocol method
  not present on the shipped `SolvedExampleStore`. The current
  body short-circuits AT the gate (not the store), which is
  sufficient for the load-bearing safety invariants (no
  high-confidence harvest when trust failed; no harvest of
  non-AppliedFromLlm outcomes).
- **Type-narrowing assertion after the eligibility gate.** The
  eligibility helper proves the outcome is `AppliedFromLlm`, but
  mypy --strict needs an explicit `assert isinstance(outcome,
  AppliedFromLlm)` before passing to `_validated_outcome_from`
  (which requires `AppliedFromLlm`-typed input per AC-9).
- **`HarvestSkipped.plan_outcome_kind` is the discriminator string,
  not the kind enum.** The closed Literal matches the
  `PlanOutcome.kind` values: `"llm"` / `"recipe"` / `"rag_only"` /
  `"refused"`. Pydantic narrows at construction time.

### Refactor decisions

- **`_emit_harvest_skipped` helper extracted** to keep the
  `on_validated` body readable; the helper accepts the closed-set
  `reason` and `outcome_kind` as positional kwargs and constructs
  the event with the standard metadata fields. Composition over
  inheritance — Rule 11.
- **`AsyncMock(side_effect=...)`** on `store.add` in the test
  helper to capture the ingest call shape via a sibling
  `ingest_spy: MagicMock`. The `side_effect` returns the pinned
  `SolvedExampleId` so AC-14 can assert payload identity.

### Test counts touched

- New: 9 tests in `test_on_validated.py`.
- Modified: 2 tests in `test_fallback_tier_scaffold.py` (fixture
  + stub-test rewrite).
- Mypy: 252 source files clean under `--strict`.
- Ruff: clean.
- Lint-imports: 12 contracts kept, 0 broken.
- Full suite (`tests/unit/fallback/ + plugins/events + rag/`):
  742 passed, 2 skipped.

### Notes for downstream stories

- **S6-01 completion** can now read the FallbackTier instance with
  the full 5+5 collaborator set (5 positional, 5 kw-only: 7+3 from
  the original + 2 added here). The constructor still matches
  arch §Component 1 modulo the additive S6-03 extension.
- **S6-07 determinism replay** consumes the on_validated event tape
  for replay-invariance; HarvestSkipped + SolvedExampleHarvested
  are now part of the replay surface.
- **S6-08 attempt anchor** may attach a ULID `AttemptAnchor` to the
  PostValidationContext; the field is additive.
- **Phase-5 contract harmonization** — when `SolvedExampleStore.contains()`
  lands and the AC-8 idempotence pre-check fills in, the
  `HarvestSkipped.reason = "already_harvested"` arm activates.
