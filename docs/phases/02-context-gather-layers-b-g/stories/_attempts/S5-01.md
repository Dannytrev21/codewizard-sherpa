# S5-01 — `ScenarioResult` + `ScannerOutcome` shared discriminated unions

## Attempt 1 — GREEN (2026-05-17) — SUCCESS

**Context:** Story HARDENED on 2026-05-16 with 12 in-place edits (see
`_validation/S5-01-scenario-scanner-outcome-types.md`). S1-01 (`IndexFreshness`)
was the 1st canonical sum-type story; this is the 2nd and mirrored S1-01's
pattern almost line-for-line — discriminator-string pinning, nested-type
round-trip, JSON-shape pin, exhaustive `match` at every level of the sum,
`frozen=True` + `extra="forbid"`, literal `__all__` pin, source-scan against
`model_construct`, Hypothesis property test.

**Approach:** Strict red-green-refactor TDD with ReAct outer loop.

1. **RED.** Wrote unit tests under `tests/unit/probes/_shared/` and
   `tests/unit/probes/layer_c/`, plus the property test at
   `tests/property/test_sum_types_roundtrip.py`. Confirmed all three failed to
   collect with `ModuleNotFoundError`.
2. **GREEN.** Created `src/codegenie/probes/_shared/{__init__,scanner_outcome}.py`
   and `src/codegenie/probes/layer_c/{__init__,scenario_result}.py`. Made every
   AC test pass in one cycle. **Two test failures on first run** —
   `test_*_module_has_no_model_construct`. Cause: the module docstrings
   literally contained the string `model_construct` while explaining the ban.
   Fix: rephrased the docstrings to "validation-bypass pydantic ctor" so the
   source-scan stays at zero hits while the educational intent is preserved.
3. **GREEN (forbidden-patterns).** Extended `scripts/check_forbidden_patterns.py`
   with a new `_is_under_phase2_s5_01_sum_type_modules` predicate + a new
   `Rule(...)` row (Open/Closed at the script level — zero edits to
   `_scan_file` / `main`). Extended
   `tests/unit/pre_commit/test_forbidden_patterns_phase2_extension.py` with
   12 positive cells (3 paths × 4 source forms) + 3 negative-coverage cells
   (`layer_a/synth.py`, `layer_b/synth.py`, `layer_c/runtime_trace_probe.py`).
4. **Refactor.** Confirmed `STDERR_TAIL_CAP_BYTES: Final[int] = 4096` is the
   single source of truth (validator + tests + property strategy all import
   it). Module docstrings name producers / consumers / cap-vs-writer
   contrast. `__all__` pinned literally to the variant + alias + reason
   types — the public surface is the union; variants are public only for
   construction.

**ReAct cycles:** ~8 (read story → read precedent S1-01 → read JSONValue +
forbidden-patterns + import-linter config → RED tests → GREEN modules →
fix source-scan docstring → extend script + test → final gates).

**Validator report (Stage 3) — all PASS, runtime evidence:**

```
- AC-1   test_scenario_result_all_exports_are_pinned
- AC-2   _shared/__init__.py re-exports; _shared/ tests + property test
         + S5-04 / S6-06 consumers (future) all import the same ScannerOutcome
- AC-3   test_*_are_frozen + test_*_rejects_extra_field (parametrized over
         every variant of both unions)
- AC-4   test_*_unknown_discriminator_is_rejected (requires the wrapper)
- AC-5   test_*_roundtrip_identity (nested-type-preservation for Stale-shaped
         reasons + Finding list)
- AC-6   test_*_match_is_exhaustive (top-level union)
- AC-6a  test_trace_failure_reason_match_is_exhaustive,
         test_trace_skip_reason_match_is_exhaustive (inner unions)
- AC-7   StraceUnavailable / DockerBuildFailed / ScenarioTimeout /
         ImageDigestUnresolved each carry kind: Literal[...]; TraceFailureReason
         union round-trips
- AC-8   NoDockerfile / ImageBuildUnavailable round-trip
- AC-9   test_scanner_failed_stderr_tail_truncates_at_cap (cap boundary
         {0, 1, 4095, 4096, 4097, 8192} → {0, 1, 4095, 4096, 4096, 4096})
- AC-10  test_repo_wide_warn_unreachable_is_true (S1-11 — already in place;
         S5-01 confirms the invariant holds without per-module override)
- AC-11  Forward-looking constraint; no probe imports a variant outside the
         declared kernel modules today (no consumers — story is kernel-only)
- AC: forbidden-patterns extension covers _shared/** and
       layer_c/scenario_result.py — 12 new positive + 3 new negative cells
- AC-12  test_*_discriminator_strings_are_exactly_pinned (per union, exact
         string literal)
- AC-13  test_*_json_shape_pinned (literal dict comparison)
- AC-14  Parametrized over all four unions (ScannerOutcome, ScenarioResult,
         TraceFailureReason, TraceSkipReason)
- AC-15  tests/property/test_sum_types_roundtrip.py — Hypothesis properties
         for both unions; bounds match the per-outcome caps
- AC-16  test_finding_metadata_jsonvalue_roundtrip — deeply-nested JSONValue
         payload byte-identical round-trip
- AC-17  test_*_are_frozen (mutate kind) + test_*_rejects_extra_field
- AC-18  test_*_all_exports_are_pinned (literal set equality)
- AC: STDERR_TAIL_CAP_BYTES named constant — test_stderr_tail_cap_bytes_is_named_constant
- AC: source-scan against model_construct — test_*_module_has_no_model_construct

Gates
- pytest (full suite): PASS — 2431 passed / 15 skipped / 2 xfailed (pre-existing)
- ruff check: PASS
- ruff format --check: PASS (304 files)
- mypy --strict src/: PASS (97 source files)
- lint-imports: PASS (2 contracts kept / 0 broken)
- Coverage: 93.30% (above 85% floor)

Design quality (anti-pattern smells from references/design-patterns.md):
- Sum-type discipline + ADR-amendment-gated extension (NOT registry-by-addition)
- Make-illegal-states-unrepresentable: ScannerSkipped.reason is Literal closed-set
  not str; Finding.metadata uses recursive JSONValue not dict[str, Any]
- Smart constructor at the cap boundary: STDERR_TAIL_CAP_BYTES is the single
  source of truth; field_validator + tests + property strategy all import it
- Functional core: no I/O, no logger, no registry side-effects
- Frozen models — no hidden state
- Forbidden-patterns extension is Open/Closed at the script: new Rule(...) +
  new path predicate, zero edits to _scan_file / main
- Module docstrings name producers (this module) / consumers (S5-02 / S5-04 /
  S6-06 / S6-07 / S6-08) / cap-vs-writer contrast

Recommendation: PROCEED to Stage 4.
```

**Refactor decisions:**

- **`scenario_name` left as raw `str`** per the story's notes: S1-05 is the
  canonical newtype story and the cross-module surface is too narrow today
  to justify pre-empting it (Rule 2 — simplicity first).
- **`__all__` order alphabetical** to match the convention used by Phase 1's
  `codegenie.parsers` and Phase 2 S1-01's `codegenie.indices`.
- **No edit to `pyproject.toml` `[tool.mypy]` overrides** per the hardened
  AC-10: `warn_unreachable = true` is already repo-wide since Phase 0 S1-02
  and `mypy --strict src/` covers both new modules by default (no `exclude`
  rule needed).
- **No edit to `codegenie/probes/__init__.py`** — `_shared/` and `layer_c/`
  are kernel-only this story; the importer's grouped-import block stays
  pinned at the seven Layer B probes. The first Layer C probe (S5-02
  `RuntimeTraceProbe`) will add its own `from codegenie.probes.layer_c
  import ...` block when it lands.

**Open follow-ups (not in scope):**

- `phase-arch-design.md §"Data model"` line 731 still pins
  `[internal] codegenie/probes/layer_g/scanner_outcome.py` — the location
  evolved to `_shared/` during High-level-impl.md authoring (§172). The
  story's "Notes for implementer" already calls this out; the doc edit is
  surgical-violation territory (Rule 3) and belongs to a docs-only sweep.
- The deliberate-`case`-deletion exhaustiveness smoke test from the story is
  documented but not committed (it would intentionally break mypy at HEAD).
  Treated as a developer-runnable check per the story's last bullet.
