# Attempt log: S4-02 — `stale-scip` fixture stub + load-bearing adversarial

## Attempt 1 — 2026-05-16 — SUCCESS

**Approach:** Landed exactly the seed-vs-runtime split prescribed by the
hardened story: tracked seed material (`_seed/scip-slice.template.json`,
`regenerate.sh`, `README.md`, `.gitignore`, `.gitattributes`, `package.json`,
`main.ts`) + runtime-materialized `.git/` and `.codegenie/` (both
gitignored). One main adversarial in `tests/adv/phase02/` + three
companion unit tests + the `phase02_adv` marker registration in
`pyproject.toml`.

**ReAct cycles:** ~12.

**What worked:**
- Reading S4-01's `clean_freshness_registry` snapshot-restore fixture
  pattern verbatim — copied the body, no inventing.
- Substituted `time.perf_counter` for `@pytest.mark.timeout(10)` because
  `pytest-timeout` is not on the dev-dep list (story permitted the
  fallback explicitly).
- Ran `regenerate.sh` once locally before the main test — confirmed two
  commits exist, parent SHA materialized into `scip.json`.
- Mutation pass (T-06) executed directly via a small Python harness:
  swapped `default_freshness_registry._checks[IndexName("scip")]` for
  two wrong implementations and captured the diagnostics verbatim:

  Mutation A (always `Fresh`):
  ```
  Expected `Stale`, got `Fresh` with value:
  {
    "kind": "fresh",
    "indexed_at": "2026-05-16T19:17:45.377244Z"
  }
  ```

  Mutation B (always `Stale(IndexerError('nope'))`):
  ```
  Expected `Stale(reason=CommitsBehind)`, got `Stale(reason=IndexerError)`.
  Full freshness:
  {
    "kind": "stale",
    "reason": { "kind": "indexer_error", "message": "nope" }
  }
  ```

  Both diagnostics confirm the test fails at the *expected* AC step —
  Rule 9 verified empirically.

**What didn't / friction:**
- Ruff `I001` auto-import-sort fired on three test files after first
  write; `ruff check --fix` resolved cleanly, no manual edits needed.
  L2 lesson re-confirmed.
- `mypy --strict tests/...` standalone errors with "Skipping analyzing
  codegenie.*: module is installed, but missing library stubs or py.typed
  marker" because the project has no `py.typed` and `src/` isn't on
  `MYPYPATH` outside the pre-commit hook context. Pre-commit's own
  `mypy --strict src/` hook passed; that is the project's contract for
  AC-11. Carry-forward: future adversarial AC-11 satisfactions should
  cite the pre-commit hook, not standalone mypy on test files.

**Validator report (Stage 3):**

```
## Validator report: S4-02 attempt 1

### Per-AC results
- [x] AC-1 (5 ordered assertions with loud diagnostics) → PASS — evidence:
      tests/adv/phase02/test_stale_scip_fixture.py::test_index_health_catches_stale_scip
      runs in 0.32s; T-06 mutation pass verified diagnostics fire at step
      2 and step 3.
- [x] AC-2 (current_HEAD derived at test time) → PASS — _current_head()
      at test_stale_scip_fixture.py:47-52.
- [x] AC-3 (fixture stub + tracked seed + runtime split) → PASS — `ls
      tests/fixtures/portfolio/stale-scip/` shows all tracked files;
      regenerate.sh produces .git/ + .codegenie/ at runtime.
- [x] AC-4 (README documents regeneration policy) → PASS — all 7 enumerated
      bullet topics present.
- [x] AC-5 (regenerate.sh refuses LAST_INDEXED==HEAD) → PASS — evidence:
      tests/unit/fixtures/test_stale_scip_regenerate_guard.py::test_regenerate_sh_refuses_last_indexed_equals_head.
- [x] AC-6 (failure mode loud + actionable) → PASS — verified via T-06
      mutation pass (see diagnostics above).
- [x] AC-7 (phase02_adv marker registered in pyproject.toml) → PASS —
      `pytest --markers` shows the marker; `pytest -m phase02_adv
      --collect-only -q` selects exactly the adversarial.
- [x] AC-8 (no pytest.skip path) → PASS — `pytest.fail()` for missing git.
- [x] AC-9 (empty-registry anti-false-pass) → PASS — evidence:
      tests/unit/probes/layer_b/test_index_health_empty_registry_adversarial.py
      shows {} != {"scip"} under empty registry.
- [x] AC-10 (< 10 s walltime) → PASS — per-test elapsed asserted inline;
      actual 0.32 s.
- [x] AC-11 (ruff, ruff format, mypy --strict, pre-commit) → PASS via
      pre-commit hook (project-canonical mypy invocation).
- [x] AC-12 (.gitignore policy verified by check-ignore) → PASS — evidence:
      tests/unit/fixtures/test_stale_scip_gitignore_policy.py.

### Gates
- pytest: PASS (2147 passed, 5 skipped env-only, 2 xfail intentional)
- ruff check: PASS
- ruff format --check: PASS
- mypy: PASS (via pre-commit hook — project-canonical)
- pre-commit run --all-files: PASS (all 9 hooks)
- Files-to-touch: PASS — every tracked file from §"Files to touch" exists.
- Coverage: 93.53% (floor 85%)
- Design quality: PASS — no `Any`, no `if/elif` ladders, no broad except,
  no new mutable module-level state, no fold-in refactors.

### Recommendation
PROCEED to Stage 4.
```

**Final files touched:**
- `pyproject.toml` — extended `[tool.pytest.ini_options].markers` with
  `phase02_adv`.
- `tests/adv/phase02/__init__.py` — created (empty).
- `tests/adv/phase02/conftest.py` — created (`fixture_path` fixture).
- `tests/adv/phase02/test_stale_scip_fixture.py` — created (the main
  adversarial; 5 ordered structural assertions).
- `tests/fixtures/portfolio/stale-scip/regenerate.sh` — created
  (executable; env-overrideable LAST_INDEXED guard).
- `tests/fixtures/portfolio/stale-scip/README.md` — created
  (regeneration policy + load-bearing warning + structural assertion).
- `tests/fixtures/portfolio/stale-scip/.gitignore` — created (fixture-local).
- `tests/fixtures/portfolio/stale-scip/.gitattributes` — created
  (binary marker for placeholder).
- `tests/fixtures/portfolio/stale-scip/_seed/scip-slice.template.json`
  — created (synthetic SCIP sibling slice with `PARENT_COMMIT` token).
- `tests/fixtures/portfolio/stale-scip/_seed/scip-index.scip.placeholder`
  — created (empty file; S7-02 replaces with real blob).
- `tests/fixtures/portfolio/stale-scip/package.json` — created (v0 commit
  content).
- `tests/fixtures/portfolio/stale-scip/main.ts` — created (v1 commit
  content).
- `tests/unit/fixtures/test_stale_scip_regenerate_guard.py` — created.
- `tests/unit/fixtures/test_stale_scip_gitignore_policy.py` — created.
- `tests/unit/probes/layer_b/test_index_health_empty_registry_adversarial.py`
  — created.

**Tests added:**
- `tests/adv/phase02/test_stale_scip_fixture.py::test_index_health_catches_stale_scip`
  — main adversarial; verifies all 5 ordered assertions per AC-1.
- `tests/unit/fixtures/test_stale_scip_regenerate_guard.py::test_regenerate_sh_refuses_last_indexed_equals_head`
  — AC-5.
- `tests/unit/fixtures/test_stale_scip_gitignore_policy.py::test_fixture_gitignore_policy`
  — AC-12.
- `tests/unit/probes/layer_b/test_index_health_empty_registry_adversarial.py::test_empty_registry_fails_adversarial`
  — AC-9.

**Deviations from the story spec:** none. `pytest-timeout` substitution
was permitted by the story.

**Follow-ups surfaced this attempt:**
- The rule-of-three threshold for extracting `tests/adv/phase02/_helpers.py`
  (DP1 from the story) will trip when S5-05 / S5-06 / S6-07 / S7-04
  land — they each construct `RepoSnapshot` + `ProbeContext` inline and
  drive a single probe. Today's `_snapshot` and `_ctx` helpers live in
  `test_stale_scip_fixture.py`; lift to `_helpers.py` on the 3rd
  adversarial.

### Refactor decisions (DP1-DP4 lens)
- **DP1 — rule-of-three deferred.** Helpers `_snapshot`, `_ctx`,
  `_current_head`, `_FRESHNESS_ADAPTER` stay inline in the test file
  (Rule 2 — simplicity first). Extract when the 3rd phase-02 adversarial
  lands.
- **DP2 — Functional Core / Imperative Shell at the fixture boundary.**
  Tracked seed (`_seed/scip-slice.template.json` with `PARENT_COMMIT`
  token + `regenerate.sh`) is the reviewable contract surface; `.git/`
  and `.codegenie/` are the regenerated impure state. **Applied.**
- **DP3 — `TypeAdapter[IndexFreshness]` as round-trip-at-the-boundary.**
  The adversarial round-trips through the discriminated union (`Fresh |
  Stale` with `kind` discriminator), then `isinstance(freshness, Stale)`
  gives the named, intent-encoding failure. **Applied.**
- **DP4 — Open/Closed at the file boundary.** Adding the next adversarial
  under `tests/adv/phase02/` requires zero edits to this test, to the
  marker registration, or to `tests/adv/phase02/conftest.py`.
  **Structurally established.**

### Status

Done. All 12 ACs have runtime evidence. Story status block staged.
