# Attempt log: S5-03 — `OpenRewriteRecipeEngine` scaffold

## Attempt 1 — 2026-05-20 — BLOCKED (stopped at Stage 1 hard gate)

**Outcome:** No code shipped. The `phase-story-executor` Context-Loader hard
gate fired during Stage 1. S5-03 inherits the **identical** `RecipeEngine
.apply` 2-tuple-vs-Protocol-conformance contradiction that BLOCKED S5-02 on
2026-05-20, *plus* two secondary missing-prerequisite blockers caused by
S5-02 shipping no code. Per global Rule 12 (fail loud) and the executor's
failure-mode table ("test infrastructure missing → a prerequisite story did
not run → stop, surface to user"), the story is marked **BLOCKED** rather
than forcing a contract-divergent engine through.

### Blocker A (root cause — shared verbatim with S5-02)

S5-03 specifies **two** things that cannot both be true:

1. **2-tuple `apply` return contract** — Goal text + AC-Contract-1 (line
   119): `OpenRewriteRecipeEngine.apply(...) -> tuple[RecipeOutcome,
   Transform | None]`. Every TDD-plan test destructures it: `outcome,
   transform = await engine.apply(...)`.
2. **AC-Surface-2(b)** (line 94): *"a mypy-strict assignment `_engine:
   RecipeEngine = OpenRewriteRecipeEngine(jail=fake_jail)` type-checks
   (fence file runs `mypy --strict` ... asserts exit code 0)."*

The **landed** `RecipeEngine` Protocol — shipped GREEN by S5-01 on
2026-05-19 at `src/codegenie/transforms/recipe_engine.py:82-91` — is:

```python
@runtime_checkable
class RecipeEngine(Protocol):
    async def apply(
        self, repo: SandboxedPath, plan: ApplicationPlan,
        capability: NpmInstallCapability,
    ) -> RecipeOutcome: ...
```

`apply` returns **`RecipeOutcome`** — a single value, **not** a tuple.
`tuple[RecipeOutcome, Transform | None]` is not assignable to
`RecipeOutcome`, so `_engine: RecipeEngine = OpenRewriteRecipeEngine(...)`
fails `mypy --strict` with `Incompatible types in assignment`. **AC-Contract-1
and AC-Surface-2(b) are mutually unsatisfiable.** `RecipeEngine` is
`@runtime_checkable`, so AC-Surface-2(a) (`isinstance`) would pass
regardless — Python's runtime Protocol check ignores method signatures.
Only the **mypy half** breaks. This is the same defect documented in
`_attempts/S5-02.md` Blocker A and cross-story lesson `_lessons.md` #16.

### Blocker B — `NpmLockfileRecipeEngine` does not exist

- **AC-Surface-2(c)** (line 94) requires `inspect.signature(
  OpenRewriteRecipeEngine.apply).return_annotation` to equal
  `inspect.signature(NpmLockfileRecipeEngine.apply).return_annotation`.
- The TDD plan's `test_engine_apply_signature_matches_npm` (line 409) does
  `from codegenie.transforms.engines.npm_lockfile import
  NpmLockfileRecipeEngine`.

S5-02 is `BLOCKED` with **no code shipped** —
`src/codegenie/transforms/engines/` does not exist and
`NpmLockfileRecipeEngine` is defined nowhere (`grep -rln
"NpmLockfileRecipeEngine" src/ tests/` returns only docstring/comment
references in `recipe_engine.py` + `outcomes.py`). These AC/tests
`ImportError` unconditionally.

### Blocker C — S5-02-introduced engine fences do not exist

- **AC-Surface-4** (line 96) requires `openrewrite.py` to pass *"the
  existing `tests/fence/test_engines_no_module_state.py` fence (introduced
  by S5-02)"*.
- **AC-Pure-2** (line 115) requires passing *"the existing
  `tests/fence/test_engines_no_raw_os_open.py` AST-walk fence (introduced
  by S5-02)"*.

Neither file exists. They are S5-02 deliverables; S5-03 is not scoped to
create them (only `tests/fence/test_engines_pure_helpers.py` is S5-03's,
per AC-Pure-1). With S5-02 BLOCKED, S5-03 cannot "pass an existing fence"
that was never introduced.

### Story-doc discrepancy (not a blocker — note for the rewrite)

AC-Conf-1 (line 168) claims `tests/integration/test_recipe_engine_protocol
.py` was *"created by S5-01 HARDENED"* and must be **AMENDED**. That file
does not exist. S5-01 (GREEN) actually created
`tests/unit/transforms/test_recipe_engine_protocol.py` (per
`_attempts/S5-01.md` §"Files landed" → Tests). The S5-03 rewrite should
retarget AC-Conf-1 to the real path.

### Why the executor cannot resolve it

Identical to S5-02. The three architecturally-sound fixes all sit outside
S5-03's authority and outside S5-03's "Files to touch":

- **(a) Change the `RecipeEngine` Protocol return type to the 2-tuple** —
  edits `recipe_engine.py` (S5-01 `Done — GREEN`, ADR-0001-tracked
  Phase-5-contract surface). ADR amendment + S6-06 contract-snapshot
  re-baseline = architect scope.
- **(b) Widen `Applied` to carry the `Transform` instance** — edits
  `outcomes.py` (ADR-0001-frozen) = architect scope.
- **(c) Add a `TransformRegistry`** so `apply` returns plain
  `RecipeOutcome` and the orchestrator looks the Transform up by
  `Applied.transform_id` — no such registry exists; building one is a new
  story slotted before S5-02.

`_attempts/S5-02.md` already states: *"No Phase-3 Step-5+ story should be
executed until S5-02 is unblocked — it is on the longest dependency chain
(S5-02 → S5-04 → S6-01 → …)."* S5-03 is a Step-5 story; this attempt
confirms that guidance.

### Verification performed before declaring BLOCKED

- Read in full: the S5-03 story; `src/codegenie/transforms/recipe_engine.py`;
  `_attempts/S5-01.md`, `_attempts/S5-02.md`, `_attempts/_lessons.md`.
- `ls src/codegenie/transforms/engines/` → *No such file or directory*.
- Confirmed absent: `tests/fence/test_engines_no_raw_os_open.py`,
  `tests/fence/test_engines_no_module_state.py`,
  `tests/integration/test_recipe_engine_protocol.py`,
  `tests/fence/test_allowed_binaries_invariants.py`.
- `grep -rln "NpmLockfileRecipeEngine" src/ tests/` → no class definition.
- `recipe_engine.py:82-91` → `RecipeEngine.apply(...) -> RecipeOutcome`
  (single value) — confirmed against shipped, CI-green code.

### Recommended resolution path

S5-03 is blocked behind S5-02's architecture decision and cannot be
re-executed before it. Route through `/phase-architect` to decide how a
`RecipeEngine` surfaces its produced `Transform` (option (a), (b), or (c)
above — (c), a `TransformRegistry` story slotted before S5-02, is the least
invasive to the ADR-0001-frozen surface), then `/phase-story-writer` to:

1. Rewrite S5-02 **and** S5-03's `apply` contract — AC-Contract-1,
   AC-Surface-2 — against the chosen design.
2. Retarget S5-03's S5-02-dependent ACs — AC-Surface-2(c), AC-Surface-4,
   AC-Pure-2 — so they reference engine artifacts S5-02 will actually ship,
   or sequence S5-03 strictly after S5-02 with an explicit `Depends on:
   S5-02` line (the current header omits it).
3. Fix the AC-Conf-1 path discrepancy
   (`tests/unit/transforms/test_recipe_engine_protocol.py`).

Re-execute S5-02 first, then S5-03.

**No code shipped.** Story Status set to `BLOCKED`.

## Unblock — 2026-05-20 — story de-contradicted (codewizard-executer)

**No execution attempt.** S5-03 inherited S5-02's Blocker-A contradiction;
both are resolved by [ADR-0014](../../ADRs/0014-recipe-engine-surfaces-transform-via-transform-registry.md)
+ story **S5-01b** (`TransformRegistry`, GREEN). S5-03 was surgically
de-contradicted: a **Re-execution note** at the top of the story file is
now authoritative. Corrected contract: `apply(...) -> RecipeOutcome` (bare,
not a 2-tuple); constructor `__init__(self, jail, transform_registry, *,
cli_jar_path=None)`; the pure helper `_map_jail_result` still returns
`(outcome, transform)` (functional core) and the impure `apply` registers
the transform into the injected `TransformRegistry`. Blocker B
(`NpmLockfileRecipeEngine` absent) is sequenced away — `Depends on:` now
lists S5-02 as a must-be-GREEN-first prerequisite. Status: `BLOCKED` →
`HARDENED`.

## Attempt 2 — 2026-05-20 — GREEN (phase-story-executor; codewizard-executer)

**Outcome:** Shipped. `OpenRewriteRecipeEngine` + `DockerfileBaseImageTransform`
land at `src/codegenie/transforms/engines/openrewrite.py` (100 % branch
coverage); `JvmEnv` widens `JailedEnv` additively in `sandbox_jail.py`. Full
suite **5766 passed, 0 failed**, 4 deselected (the `phase_7_preview` test +
3 `bench`), mypy `--strict src/` clean (198 files), ruff clean, `lint-imports`
6/6 KEPT, pre-commit all hooks Passed.

### As-built drift resolutions (the HARDENED story still carried drift the
Re-execution note did not catch — cf. `_lessons.md` #20)

1. **Error taxonomy 5 → 6.** `_OpenRewriteErrorId` ships **six** entries, not
   the five AC-Tax-1 named. The as-built `JailedSubprocessResult` carries six
   variants — `JailSetupFailed` is the sixth (S4-01 GREEN) — and the
   `assert_never` exhaustive `match` in `_map_jail_result` (AC-Map-7) cannot
   compile under mypy-strict without a sixth arm, which needs its own
   `error_id`. Added `recipe.jail_setup_failed`. This is the *identical*
   correction S5-02 made (its taxonomy went 13 → 14); Notes §2 mandates
   mirroring S5-02 literally. `_ERROR_IDS` has 6 entries; the meta-test +
   the parametrized failure-mapping test assert 6.
2. **AC-Conf-1 path.** The conformance test S5-01 created lives at
   `tests/unit/transforms/test_recipe_engine_protocol.py`, **not**
   `tests/integration/...` (Attempt-1's log already flagged this). Amended
   the real file additively (two new cases, S5-01 cases preserved verbatim).
3. **AC-CI-2 path.** `tests/fence/test_allowed_binaries_invariants.py` never
   existed; the as-built allowed-binaries suite is
   `tests/unit/exec/test_allowed_binaries.py`. Appended
   `test_java_not_in_allowed_binaries_phase3` there (Rule 7 — one location).
4. **`apply` return contract is a bare `RecipeOutcome`** (Re-execution note +
   ADR-0014) — withdrew the TDD plan's 2-tuple `apply` and every
   `outcome, transform = await ...apply(...)` destructure. Tests obtain the
   transform via `registry.get(outcome.transform_id)`.
5. **`files_changed` shape.** `SandboxedPath.__truediv__` returns a *bare*
   `pathlib.Path`, so AC-Map-1's literal `files_changed == (repo /
   "Dockerfile",)` cannot hold against the `tuple[SandboxedPath, ...]` ABC
   annotation. `_build_transform` resolves `Dockerfile` via
   `SandboxedPath.create`; the test asserts
   `files_changed[0].absolute == repo.absolute / "Dockerfile"` (the S5-02
   `test_npm_lockfile_engine.py:632` `.absolute`-inspection precedent).
6. **`DockerfileBaseImageTransform` uses a regular `__init__`**, not Outline
   §7's `cls.__new__(cls)` + manual attribute assignment — `Transform.__new__`
   is annotated `-> Transform`, so the `cls.__new__` form fails mypy-strict
   (`Incompatible return value type`). The regular-`__init__` shape mirrors
   S5-02's `NpmLockfileTransform` exactly (Notes §2) and keeps AC-Tool-1's
   no-`cast` / no-`# type: ignore` rule. `create` builds via `cls(...)`; the
   AC-Smart-1 AST fence (no `DockerfileBaseImageTransform(` outside `create`)
   still holds — `create` calls `cls(`.
7. **Outline §12 `transforms/__init__.py` re-export withdrawn.** S5-02 (GREEN)
   deliberately did not re-export `NpmLockfileRecipeEngine` from
   `transforms/__init__.py` (that module is the ADR-0001 contract-surface
   home; `engines/__init__.py` ships "no public re-exports"). Mirrored S5-02;
   the story's Files-to-touch row is struck with the reason. AC-Tool-4's fence
   still passes (zero `src/` importers).
8. **`sandbox_jail` contract snapshot regenerated.** The additive `JvmEnv`
   widening changed `JailedSubprocessSpec.model_json_schema()`; the
   module-local golden `tests/golden/contracts/sandbox_jail.schema.json`
   (S4-01 AC-15 — distinct from the ADR-0001 Phase-5 snapshot AC-Env-3
   leaves alone) was regenerated. Diff is purely additive (`JvmEnv` `$def` +
   `jvm` discriminator entry + `$ref`). ADR-0006 amended accordingly.
9. **AC-Env-2 negative fence consolidated.** The negative
   (`test_jailed_env_mypy_negative.py`) is folded into
   `test_jailed_env_exhaustiveness.py` as a second test function (Rule 7 —
   one location; the Files-to-touch table lists only the one file).

### Per-AC evidence

| AC | Evidence |
|---|---|
| Surface-1 | `test_module_all_is_exactly_the_two_public_names`, `test_module_exposes_no_private_helper_in_all` |
| Surface-2(a) | `test_engine_satisfies_recipe_engine_protocol_runtime` |
| Surface-2(b) | `test_openrewrite_typing.py::test_mypy_strict_accepts_protocol_conformance` (`mypy --strict` exit 0) |
| Surface-2(c) | `test_apply_signature_matches_npm_engine` — both engines' return annotation `== "RecipeOutcome"` |
| Surface-3 | `test_transform_is_transform_abc_subclass_with_tuple_files_changed` |
| Surface-4 | `test_engine_holds_injected_collaborators_no_module_state` + `tests/fence/test_engines_no_module_state.py` (live check covers `openrewrite.py`) |
| Tax-1 | `test_error_id_taxonomy_is_closed_six_entry_set` (6 entries, drift-corrected) |
| Pure-1 | `tests/fence/test_engines_pure_helpers.py` — pure-helper purity + `await`-only-in-`apply` + thin-`apply`-body, with 6 planted-positives |
| Pure-2 | `tests/fence/test_engines_no_raw_os_open.py` live check (covers `openrewrite.py`) |
| Contract-1 | `test_apply_signature_matches_npm_engine`, `test_recipe_engine_protocol.py::test_openrewrite_engine_apply_return_annotation` |
| Contract-2 | `test_failure_variant_mapping` asserts `len(registry) == 0` on every non-Applied branch |
| Spec-1..4 | `test_spec_cmd_is_exact_openrewrite_cli_tuple`, `test_spec_budget_envelope_is_pinned`, `test_spec_network_policy_is_deny_all` + `test_engine_source_constructs_no_registry_allowlist`, `test_spec_env_is_jvm_typed` |
| Env-1..3 | `JvmEnv` in `sandbox_jail.py`; `test_jailed_env_exhaustiveness.py` (positive + negative mypy); ADR-0006 §2026-05-20 Amendment |
| Map-1..7 | `test_happy_path_returns_applied_and_registers_transform`, `test_failure_variant_mapping` (6 variants incl. `JailSetupFailed`), `test_nonzero_exit_carries_observed_exit_code`, `test_happy_jail_result_but_missing_dockerfile_raises_loud`; `test_openrewrite_mypy_negative.py` (missing-arm `mypy --strict` non-zero) |
| Smart-1/2 | `test_smart_constructor_rejects_empty_diff_bytes` / `..._empty_files_changed` / `..._transform_id_is_blake3_of_diff`, `test_transform_constructed_only_via_smart_constructor`, `test_provenance_carries_scaffold_sentinels` |
| Det-1/2 | `test_diff_bytes_byte_identical_across_ten_runs`, `test_diff_matches_committed_golden`, `test_unified_diff_passes_no_timestamp_arguments` |
| CI-1/2/3 | `test_this_suite_never_probes_for_java`; `test_allowed_binaries.py::test_java_not_in_allowed_binaries_phase3`; `tests/fence/test_openrewrite_phase7_markers.py` |
| Phase7-1/2 | `tests/integration/test_openrewrite_engine_phase7_preview.py` (marked + skipif); `pytest --collect-only -m phase_7_preview` lists exactly 1 test; default `addopts` deselects it |
| Conf-1 | `test_recipe_engine_protocol.py` — two additive cases, S5-01 cases preserved |
| Fix-1 | `tests/fixtures/openrewrite/dockerfile-base-image-swap/` — `Dockerfile`, `expected.Dockerfile`, `expected.diff` (engine-generated golden), `recipe.yml`, `README.md` |
| Tool-1..4 | `mypy --strict` clean; ruff clean; `openrewrite.py` 100 % branch coverage; `tests/fence/test_openrewrite_not_invoked_phase3.py` |

### Environment-gated / deferred

- **AC-Phase7-1** — `test_dockerfile_base_image_swap_under_real_jvm` ships
  marked `@pytest.mark.phase_7_preview` + `skipif(java is None)` + a macOS
  `pytest.skip`. The default `addopts` (`-m "not bench and not
  phase_7_preview"`) deselects it; it is **collectable** (`--collect-only -m
  phase_7_preview` lists exactly it — AC-Phase7-2 ✓). A *green run* of the
  body additionally needs Phase 7's authored OpenRewrite recipe + a
  provisioned `rewrite-cli.jar` (the fixture `recipe.yml` is a placeholder;
  `/opt/openrewrite/rewrite-cli.jar` is unprovisioned in Phase 3 — ADR-0009
  §Consequences). The test *infrastructure* — marker, skip guards, fixture,
  `expected.diff` golden — ships now; the body is deferred to Phase 7. This
  is the exact shape of S5-02's environment-gated AC-Gold-1.

### Gate log

- `pytest -q` (full suite): **5766 passed, 40 skipped, 4 deselected, 8 xfailed**.
- `mypy --strict src/`: Success, 198 files.
- `ruff check` / `ruff format --check`: clean.
- `lint-imports --config pyproject.toml`: 6 contracts kept, 0 broken.
- `openrewrite.py` branch coverage: 100 %.
- `pre-commit run` (all changed files): every hook Passed.

The 4 failures seen on the first full-suite pass were all environment
artifacts, none an S5-03 regression: `test_lint_imports_canary.py` ×2
(`import-linter` absent from the local venv + `.venv/bin` off `PATH` — pass
once installed / on `PATH`; CI installs `[dev]`), `test_goldens_match.py`
(the macOS/Linux toolchain golden flake — `_lessons.md` #19; passes on the
clean re-run and on Linux CI), and `test_sandbox_jail_contract_snapshot.py`
(the legitimate additive `JvmEnv` drift — resolved by regenerating the
module-local golden, drift-resolution #8 above).
