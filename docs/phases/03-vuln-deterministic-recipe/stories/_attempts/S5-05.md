# Attempt log: S5-05 — `RemediationReport` schema + YAML writer

## Attempt 1 — 2026-05-21 — GREEN

**Outcome:** Shipped. `src/codegenie/transforms/report.py` now exposes the
Phase-5 contract artifact model, module-local load/write error unions,
deterministic YAML serialization, atomic write, and the `from_yaml` smart
constructor. `TrustSignal` and `TrustOutcome` were added to
`transforms/outcomes.py` as the canonical Stage-6 validation-result home and
are re-exported from `codegenie.transforms`.

### As-built drift resolutions

1. **`SandboxedPath` is the real Pydantic capability model.** Tests construct
   it as `SandboxedPath(absolute=path)`; the report module unwraps it through
   `os.fspath(...)`, preserving the existing capability surface.
2. **Directory load behavior is platform-sensitive.** On macOS, `os.open` can
   succeed for a directory and fail later at `fdopen`. `from_yaml` now rejects
   non-regular files immediately after the symlink pre-check, preserving the
   pinned validation order.
3. **Focused pytest-cov source-target path remains pre-existing broken.**
   Explicit `--cov=codegenie.transforms.report` triggers the catalog YAML
   constructor issue already documented in S5-04. Coverage evidence used plain
   `coverage run` over the focused test module and passed at 97%.
4. **Existing full-suite gates exposed two contract drifts.** The outcomes
   exact-export test had to include `TrustOutcome`/`TrustSignal`, and the
   Phase-3 kernel-freeze allowlist needed the already-ADR-backed Layer-G
   scanner amendment from the previous ast-grep fix.

### Per-AC evidence

| AC group | Evidence |
|---|---|
| Surface / exports | `test_report_surface_symbols_exported`, `test_all_exports_exact_set`, and `test_all_names_resolve_in_package_init` pass. |
| ADR-0010 discipline | Frozen/extra-forbid report models and error variants; strict newtyped fields; no `dict[str, Any]`; timezone and outcome validators covered. |
| Load errors | Missing file, directory, YAML syntax, non-mapping root, extra field, unknown schema version, symlink, size cap, and discriminator-case tests pass. |
| Write errors | Atomic success, symlink refusal, replace failure, ENOSPC, EACCES, ELOOP, fallback `OSError`, and cleanup branches pass. |
| Round trip | All four remediation outcomes round-trip; byte-identical writes and tuple preservation pass. |
| Golden placeholder | `tests/golden/remediation-reports/README.md` added and committed in the RED checkpoint. |
| Coverage | `report.py` measured at 97% branch coverage. |

### Gate log

- RED checkpoint: `6c7b8f2 test(phase3/S5-05): RED - remediation report contract`.
- GREEN implementation: `68b7457 feat(phase3/S5-05): GREEN - remediation report writer`.
- Full-suite gate fix: `0e6d891 test(phase3): update fences for S5-05 reports`.
- `PATH=.venv/bin:$PATH pytest -q tests/unit/transforms/test_remediation_report.py --no-cov` — 31 passed.
- `PATH=.venv/bin:$PATH ruff check src/codegenie/transforms/report.py src/codegenie/transforms/outcomes.py src/codegenie/transforms/__init__.py tests/unit/transforms/test_remediation_report.py` — passed.
- `PATH=.venv/bin:$PATH ruff format --check ...` — passed.
- `PATH=.venv/bin:$PATH mypy --strict src/codegenie/transforms/report.py src/codegenie/transforms/outcomes.py` — passed.
- `COVERAGE_FILE=.coverage.s5_05 .venv/bin/coverage report --fail-under=95 -m src/codegenie/transforms/report.py` — 97%.
- `pre-commit run --files ...` for changed source/test files — passed.
- `PATH=.venv/bin:$PATH make check` — lint, strict mypy, full tests, and fence green: 5,846 tests passed plus 365 fence tests.
