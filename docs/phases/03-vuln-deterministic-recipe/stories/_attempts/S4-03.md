# S4-03 — `SandboxExecAdapter` (macOS) + `templates/macos-npm.sb` — attempt log

## Attempt 1 — GREEN 2026-05-19

### Deliverables landed

| Path | Role |
|---|---|
| `src/codegenie/transforms/sandbox/templates/macos-npm.sb` | Packaged `.sb` profile template — `(deny default)`, jail + system-framework reads, `$JAIL` / `$ALLOWLIST_HOSTS` placeholders. |
| `src/codegenie/transforms/sandbox/templates/__init__.py` | Importable sub-package so `importlib.resources.files(...)` resolves. |
| `src/codegenie/transforms/sandbox/sandbox_exec.py` | `SandboxExecAdapter(SubprocessJail)` — pure renderer + chokepoint call + `classify_outcome` kernel consumption + macOS-14+ gate + cleanup-on-exception. |
| `src/codegenie/transforms/sandbox/bwrap.py` | Added `_HELPER_VERBS` Final frozenset for AC-29 parity; surfaced `classify_outcome` in `__all__`. |
| `src/codegenie/transforms/sandbox/__init__.py` | Re-exports `SandboxExecAdapter`. |
| `pyproject.toml` | Registered `nightly_macos` pytest marker (AC-12). |
| `tests/unit/transforms/sandbox/test_sandbox_exec_unit.py` | 41 cross-platform unit tests covering AC-1, AC-2 (signature half), AC-3..AC-9, AC-12, AC-15..AC-25, AC-26 (smoke half), AC-27..AC-29. |
| `tests/unit/transforms/sandbox/test_sandbox_exec_mypy_negative.py` | AC-2 (subprocess-mypy fence) + AC-25 (`Hostname` newtype fence). |
| `tests/integration/transforms/test_sandbox_exec_hello_world.py` | AC-10 — nightly_macos, fail-not-skip on darwin missing `sandbox-exec`. |
| `tests/integration/transforms/test_sandbox_exec_network_policy.py` | AC-11 — nightly_macos, two-case allow + deny. |

### Per-AC evidence

| AC | Evidence |
|---|---|
| AC-1 | `test_macos_sb_profile_template_well_formed` — `(version 1)` + `(deny default)` line ordering + placeholder tokens present. |
| AC-2 | `test_sandbox_exec_adapter_signature` (params + return), `test_sandbox_exec_adapter_call_site_typechecks` (binds to `SubprocessJail`), `test_sandbox_exec_mypy_negative.py::test_mypy_strict_catches_bogus_adapter_signature`. |
| AC-3 | `test_generated_sb_substitution` — no `$NAME` residuals, jail substituted, `registry.npmjs.org:443` clause present, lines ordered, balanced parens. |
| AC-4 | `test_generated_sb_deny_all_has_no_allow_network` — comment-stripped check returns empty list. |
| AC-5 | `test_argv_full_shape` — `("sandbox-exec","-f",<path>,*spec.cmd)`. |
| AC-6 | `test_module_has_no_forbidden_subprocess_calls` — AST walk + alias map; no `subprocess.*` / `os.system` / `os.exec*` / async-subprocess calls. |
| AC-7 | `test_result_variant_translation` parametric (Completed / NetworkDenied×github.com / NetworkDenied×pypi.org) + `test_parse_sandbox_denial_*` unit coverage. |
| AC-8 | `test_env_mapping_reaches_chokepoint` — `npm_config_ignore_scripts="true"` captured at `run_allowlisted` kwarg. |
| AC-9 | `test_cmd_preserved_verbatim_including_ignore_scripts` — `tuple(argv[-len(cmd):]) == cmd`. |
| AC-10 | `tests/integration/transforms/test_sandbox_exec_hello_world.py` — `@pytest.mark.nightly_macos`; fails not skips on darwin missing sandbox-exec; pre-S4-05 accepts `JailSetupFailed(binary-not-allowlisted)` (documented). |
| AC-11 | `tests/integration/transforms/test_sandbox_exec_network_policy.py` — two-case allowlisted + denied; same pre-S4-05 acceptance. |
| AC-12 | `test_nightly_macos_marker_registered` — `pytestconfig.getini("markers")` carries `nightly_macos`. |
| AC-13 | `.venv/bin/mypy --strict src/` → 180 source files, 0 issues. `ruff check` + `ruff format --check` clean on touched files. |
| AC-14 | `.venv/bin/lint-imports` → 4 contracts kept, 0 broken; `codegenie.transforms must not import LLM SDKs` KEPT. |
| AC-15 | `test_sb_template_polarity_and_no_sweeping_writes` — comment-stripped template carries `(deny default)` exactly once; every `(allow file-write*)` form has `(subpath ...)`; every `(allow network*)` form has `(remote ...)`. |
| AC-16 | `test_typed_error_fence` parametric — `OSError` / `TimeoutError` / generic `Exception` all return typed variants; no exception escapes. |
| AC-17 | `test_render_is_deterministic` — byte-identical across (a) two calls with same spec and (b) `frozenset({a,b})` vs `frozenset({b,a})` constructions. |
| AC-18 | Three Hypothesis properties (`test_property_deny_all_has_no_allow_network`, `test_property_every_allowlisted_host_appears`, `test_property_render_byte_identical`), 25 examples each. |
| AC-19 | `test_cleanup_on_exception` — injects `OSError` in chokepoint; no `*.sb` leftovers in `spec.cwd`. |
| AC-20 | `test_concurrent_runs_use_distinct_profiles` — `asyncio.gather` × 8; all profile paths distinct. |
| AC-21 | `test_substrate_setup_failure_typed` — `Sandbox: ... error:` stderr signature → `JailSetupFailed(reason="kernel-setup-failed")` with stderr excerpt in `detail`. See "Coordination" note below. |
| AC-22 | `test_module_has_no_module_level_mutable_globals` — AST walk over module body; permits `__all__`, NewType-call binding, `Final[...]` AnnAssign only. |
| AC-23 | `test_classifier_kernel_is_shared_with_bwrap` — `bwrap.classify_outcome is sandbox_exec.classify_outcome` identity assert. See "Naming drift" below. |
| AC-24 | `_render_allowlist_clauses` uses `match` + `typing.assert_never`; `test_match_assert_never_fires_on_synthetic` exercises the wildcard. |
| AC-25 | `test_extract_hostname_validates` + `test_extract_hostname_rejects_bad_charset` + `test_render_allow_network_clause_consumes_hostname` + `test_sandbox_exec_mypy_negative.py::test_mypy_strict_rejects_raw_str_for_hostname`. |
| AC-26 | `test_template_loads_via_importlib_resources` (CWD-agnostic smoke). Full wheel-install survival fixture is deferred (story §"Out of scope" / `tox -e wheel-smoke` precedent). |
| AC-27 | `test_macos_13_raises_substrate_unsupported`, `test_macos_14_constructs`, `test_non_darwin_constructs`. |
| AC-28 | `test_placeholder_residual_raises_typed` — bad template with `$UNRESOLVED_TOKEN` raises `ProfilePlaceholderUnresolved(token="$UNRESOLVED_TOKEN")`. |
| AC-29 | `test_helper_verb_parity` — `bwrap._HELPER_VERBS == sandbox_exec._HELPER_VERBS`. |

### Gate log

```
.venv/bin/pytest tests/unit/transforms/sandbox/ --no-cov
   → 106 passed (41 new sandbox_exec tests + 65 pre-existing bwrap/_classify/_seccomp)

.venv/bin/mypy --strict src/
   → 180 source files, 0 issues

.venv/bin/ruff check ${touched_files}
   → All checks passed

.venv/bin/ruff format --check ${touched_files}
   → All formatted

.venv/bin/lint-imports
   → 4 contracts kept, 0 broken
```

### Trade-offs pinned

#### 1. `JailSetupFailed` reused; new `SubstrateSetupFailed` not introduced (AC-21)

The hardened story's AC-21 names a `SubstrateSetupFailed(reason, stderr_excerpt)` variant. The S4-01 sum
already ships `JailSetupFailed(reason: Literal[...closed set], detail: str)` with `kernel-setup-failed`
already covering "substrate refused to stand up the jail." Per Rule 7 (surface conflicts, don't average),
adding a parallel variant with overlapping semantics would have been duplicative — the macOS adapter
maps substrate parse errors to `JailSetupFailed(reason="kernel-setup-failed", detail=excerpt)`. The story's
intent ("typed, not sentinel `Completed(exit_code=N)`") is honoured: the variant is typed and discriminated;
the wire shape is just `JailSetupFailed` rather than a new sibling. Surface as doc-debt amendment to AC-21
if a future bake test demands a literal `SubstrateSetupFailed` name.

#### 2. Kernel symbol name: `classify_outcome` (not `_classify_outcome`) (AC-23)

The story header + AC-23 test sketch refer to `_classify_outcome`; the S4-02 hardening extracted the kernel
as the public `classify_outcome` symbol in `src/codegenie/transforms/sandbox/_classify.py`. Per Rule 11
(match the codebase's conventions), the macOS adapter imports `classify_outcome` verbatim and the AC-23
identity test asserts `bwrap.classify_outcome is sandbox_exec.classify_outcome` (function-object identity
beats name-string identity for the test's actual intent — proving no shadow copy). `bwrap.__all__` was
extended to expose `classify_outcome` so the meta-test can reach the symbol without `getattr`.

#### 3. macOS 14+ gate ships in `sandbox_exec.py`, not `sandbox_jail.py`

`SubstrateUnsupportedError` is a typed exception raised at construction time. It is adapter-local rather
than a sum-type variant (which is for *run-outcome* failures only) — defined in `sandbox_exec.py`. A future
`FirecrackerAdapter` or `DinDAdapter` can mirror the pattern without polluting `sandbox_jail.py`'s public
result surface.

#### 4. Pre-S4-05 `ALLOWED_BINARIES` posture (AC-10 / AC-11)

`sandbox-exec` is intentionally NOT in `ALLOWED_BINARIES` until S4-05. Integration tests therefore accept
both `Completed` (post-S4-05) and `JailSetupFailed(reason="binary-not-allowlisted")` (pre-S4-05) — same
documented pattern as `tests/integration/transforms/test_bwrap_hello_world.py`. The substitution
captures unambiguously: once S4-05 admits the binary, the test must produce `Completed` (otherwise the
nightly fails loud).

#### 5. Hypothesis fixtures: module-level path (no `tmp_path_factory`)

Hypothesis cannot inject pytest fixtures into a `@given`-decorated test. The three property tests use a
module-level `_HYPOTHESIS_TMP = Path(__file__).parent` — any existing directory suffices because the
spec.cwd value is only relevant as a literal in the rendered output (which the properties already check
via substring presence, not via filesystem effects).

#### 6. Template comment-stripping in AC-15

The packaged `.sb` profile carries `;;`-prefixed documentation lines that legally mention sandbox-exec
forms by name (e.g., "renders one `(allow network* (remote tcp ...))` per host"). AC-15's mutant guard
fences active clauses; the test pre-strips comment lines so a docstring mention does not trip the
sweeping-allow assertion.

### Pre-existing failures (NOT regressions)

Full `pytest --no-cov` run revealed four pre-existing failures unrelated to S4-03:

- `tests/unit/test_lint_imports_canary.py::*` — `shutil.which("lint-imports")` returns `None` in a venv
  whose `bin/` is not on `$PATH`. Pure environment issue; `.venv/bin/lint-imports` works.
- `tests/adv/phase02/test_secret_in_source.py::test_*` — `scip-index.scip` leaks plaintext seed bytes.
  Reproduces on `master` with no S4-03 changes applied (verified via `git stash` round-trip).

Neither is on the S4-03 critical path; both are tracked elsewhere.

### What did NOT land

- **`SubstrateSetupFailed` literal variant** — per Trade-off 1 above. If a future story (S5 / S7) requires
  the literal name, amend ADR / S4-01 and the AC-21 implementation here switches to that variant in one
  edit.
- **Wheel-install survival fixture** — `tox -e wheel-smoke` precedent referenced in AC-26 not yet wired;
  deferred. Smoke half (CWD-agnostic `importlib.resources` load) is green.
- **Live `sandbox-exec` invocation** — nightly_macos cadence per ADR-0006 §Consequences row 4. Will run
  on the next nightly darwin pass.

---

## Follow-up — Live macOS substrate hardening (post-S4-05)

After S4-05 admits `sandbox-exec` to `ALLOWED_BINARIES`, the two nightly_macos integration tests
under `tests/integration/transforms/test_sandbox_exec_{hello_world,network_policy}.py` start
actually spawning the substrate (pre-S4-05 they stop at the chokepoint with
`JailSetupFailed(binary-not-allowlisted)`). Two real defects surfaced on operator-laptop runs:

### Defect 1 — `/bin/echo` denied by sandbox profile

`Completed(kind='completed', exit_code=71, stdout_bytes=0, stderr_bytes=70)`. Stderr:
`sandbox-exec: execvp() of '/bin/echo' failed: Operation not permitted`. The packaged
`macos-npm.sb` profile granted `process-exec` only on `/usr/local/bin` and `/usr/bin`;
`/bin/echo` lives in `/bin` and was denied. Also missing from `file-read*`: `/bin`, `/sbin`,
the `(literal "/")` form dyld needs to stat the root directory on macOS 14+, the broader
`/System` subpath (not just `/System/Library/Frameworks`) for the dyld shared cache, and
`/private` for `/etc → /private/etc` symlink targets.

**Fix:** broaden `file-read*` and `process-exec` subpaths in
`src/codegenie/transforms/sandbox/templates/macos-npm.sb`. Verified end-to-end —
`Completed(exit_code=0)`.

### Defect 2 — SBPL rejects hostnames in `(remote tcp ...)`

`RegistryAllowlist` rendering emitted `(allow network* (remote tcp "registry.npmjs.org:443"))`,
which `sandbox-exec` rejects with `host must be * or localhost in network address`. SBPL only
accepts `*` or `localhost` inside `(remote tcp ...)` — hostname filtering is not expressible
at the profile layer. ADR-0006 §Consequences anticipated this: macOS hostname allowlisting
lives at the **pf** rule layer at the parent-process boundary, not in the `.sb` profile.

**Fix:** `_render_allowlist_clauses` now emits one `(allow network* (remote tcp "*:PORT"))`
clause per unique port in the URL set. Hostnames are still validated via `_extract_hostname`
so malformed URLs fail loud, but they are not rendered into SBPL. The hostname-rendering
helper `_render_allow_network_clause` is preserved with a docstring noting its reserved-for-
Phase-5-pf-rule-generation role.

`test_sandbox_exec_denies_non_allowlisted_host` is marked
`@pytest.mark.xfail(strict=True, raises=AssertionError)` conditional on `"sandbox-exec" in
ALLOWED_BINARIES` — i.e. activates post-S4-05 only. The xfail reason cites ADR-0006
§Consequences and Phase 5 (05-ADR-0004 Lima/DinD substitution). When Phase 5 wires pf rules
the xfail flips to pass.

### Defect 3 — `_is_substrate_setup_error` regex missed CLI-emitted stderr

`_SANDBOX_SETUP_ERROR_RE = rb"Sandbox:[^\n]*error:"` only matched the **kernel-emitted**
prefix. The `sandbox-exec` CLI itself emits stderr lines starting with `sandbox-exec:`
(execvp failures, profile parse errors) — those leaked through as generic `Completed(rc=N)`
instead of typed `JailSetupFailed(reason='kernel-setup-failed')`. Rule 12 — fail loud, fail typed.

**Fix:** broadened to `rb"(?:Sandbox:[^\n]*error:|^sandbox-exec:)"` with `re.MULTILINE`. New
parametric test `test_substrate_setup_failure_cli_prefix_typed` covers both CLI signatures.

### Hostname unit-test churn

`test_generated_sb_substitution` and `test_property_every_allowlisted_host_appears` were
updated to assert port-wildcard rendering (`"*:443"`) and to negative-assert that no rendered
hostname leaks through. Property test renamed to
`test_property_every_allowlisted_port_appears` to reflect the new invariant.
