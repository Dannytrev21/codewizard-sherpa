---
phase: 03-vuln-deterministic-recipe
run_utc: 2026-05-21T02:14:00Z
skill: phase-shakedown
trigger: user-invoked (/phase-shakedown, no argument)
target_detection: auto
gate_result: green
findings_total: 1
findings_by_class: { trivial: 1, bounded: 0, sub_system: 0, architectural: 0, critical: 0 }
critical: false
commit_report: false
---

# Phase 3 (`03-vuln-deterministic-recipe`) — shakedown 2026-05-21

**Verdict: healthy.** The assembled Phase 3 surface passes the full local
composition gate (`make check`, exit 0) and CI on the just-landed HEAD
(`15d30a4`, 20/20 jobs green). One trivial doc-vs-code drift was found and
fixed inline. No bounded / sub-system / architectural / critical findings.

## Stage 0 — environment doctor

All six expected tools resolved on `PATH` (with `.venv/bin` prepended):
`ruff`, `mypy`, `pytest`, `make`, `mkdocs`, `codegenie`. PASS.

## Stage 1 — target detection

Auto-detect. Raw `git log --name-only -30` tally:

| Phase folder | hits |
|---|---|
| `07.5-multi-language-foundations-python` | 63 |
| `03-vuln-deterministic-recipe` | 47 |
| `07-migration-task-class` | 9 |
| `01-context-gather-layer-a-node` | 1 |

The raw winner (`07.5`, 63) is a **single design-doc bulk commit**
(`27423bf docs(phase7.5): design pipeline — … 12 ADRs, 41 stories`); Phase 7.5
is design-only with no implementation, so "run end-to-end" is not meaningful
there. Target overridden to **Phase 3** — the most-recently-*implemented*
phase (story `S5-04` landed today as `15d30a4`). This is a deliberate
detection override, recorded here for the audit trail.

## Stage 2 — discovery cascade / execution plan

Phase 3 has no prior `_e2e/` report — this is the first shakedown. Docs read:
`High-level-impl.md` (9 step Done-criteria blocks), `README.md` (44-story
manifest + DAG), story `Status:` lines, `_attempts/_lessons.md` (315 lines,
27 lessons). Phase 3 is mid-flight: **24 stories Done, 20 HARDENED, 0
BLOCKED** — implemented through Step 5 (engine layer + lockfile policy);
Steps 6–9 (orchestrator, plugins, e2e, CI) are HARDENED-not-executed.

Execution plan (all read-only — no mutating-action prompt required):

1. `make check` — full composition gate (ruff, import-linter, mypy `--strict`,
   pytest + coverage `--cov-fail-under=85`, fence).
2. `codegenie --help` + `codegenie vuln-index --help` — CLI smoke.
3. Story-status scan — any `BLOCKED` / inconsistent.
4. Doc-consistency spot check — Step 5 Done-criteria + contract surfaces vs
   shipped code.

Note on "end-to-end": Phase 3's single-command e2e entry point
(`codegenie remediate`) is owned by Step 6 (`S6-05`), which is HARDENED but
not executed — so a true one-command Phase 3 run is not yet possible.
`make check` (which runs Phase 3's `tests/integration/` suites) is the
current end-to-end proxy.

## Stage 3 — execution

| # | Command | Result |
|---|---|---|
| 1 | `make check` | **exit 0** — lint + import-linter + mypy `--strict` + pytest(+coverage) + fence all pass |
| 2 | `codegenie --help` | exit 0 — CLI surface intact |
| 2 | `codegenie vuln-index --help` | exit 0 — Phase 3 `S3-03` subcommand intact |
| 3 | story-status scan | 24 Done / 20 HARDENED / **0 BLOCKED** |
| 4 | doc-consistency check | 1 drift — see F1 |

Corroborating signals (gathered during the same session, HEAD `15d30a4`):
full suite `pytest -q --no-cov` = 5804 passed / 41 skipped / 8 xfailed;
`import-linter` 6/6 contracts kept; `mkdocs build --strict` built;
CI run `26201043128` = **20/20 jobs success**.

## Stage 4 — triage

### F1 — `LockfilePolicy.from_yaml` return type stale in plan/arch docs — TRIVIAL

Three Phase-3 docs describe `LockfilePolicy.from_yaml(path)` as returning
`Result[LockfilePolicy, ParseError]`. The shipped `S5-04` code returns
`Result[LockfilePolicy, PolicyLoadError]` — a module-local six-variant
discriminated union. The `phase-story-validator` corrected this in the story's
Validation-notes / Goal / ACs but the pre-execution prose was not synced:

- `docs/phases/03-vuln-deterministic-recipe/High-level-impl.md:140`
- `docs/phases/03-vuln-deterministic-recipe/phase-arch-design.md:1163`
- `docs/phases/03-vuln-deterministic-recipe/stories/S5-04-lockfile-policy-yaml.md:27` (Context prose)

Class **trivial** — a stale type name in planning prose; the authoritative
design (story Validation-note 1, ADR-0010) and the shipped code agree on
`PolicyLoadError`. Misleads a future reader (e.g. the `S5-05` executor).

### Non-findings / observations (not routed)

- **O1 — Phase 3 is ~55% executed (24/44 stories).** Steps 6–9 are
  HARDENED-not-built; `High-level-impl.md` Step 5 "Features delivered" lists
  `transforms/report.py` (`S5-05`) which does not exist yet. This is normal
  in-flight state, not a defect — `S5-05` is the next story.
- **O2 — `.coverage` and `.claude/worktrees/` are untracked and not in
  `.gitignore`.** Repo-wide hygiene, not Phase-3-scoped — left for the
  operator. `.coverage` is a standard pytest-cov artifact; `.claude/worktrees/`
  is created by Claude Code worktree isolation. Recommend adding both to
  `.gitignore`.
- **O3 — uncommitted Phase 7 design docs in the working tree** (modified +
  untracked under `docs/phases/07-migration-task-class/` — new ADRs 0018–0029,
  ~18 new stories). Out of this shakedown's Phase-3 scope; flagged so the
  operator decides commit-vs-discard. Not routed.
- **O4 — `pytest --cov=<narrow.module>` breaks `codegenie.catalogs` YAML load
  locally.** Pre-existing environment quirk, reproduces with unrelated tests;
  the default `addopts` `--cov` path (and therefore `make check` / CI) is
  unaffected. Captured as `_lessons.md` #27.

## Stage 5 — routing

| Finding | Class | Route | Artifact |
|---|---|---|---|
| F1 | trivial | inline edit (3 lines) | `High-level-impl.md:140`, `phase-arch-design.md:1163`, `S5-04` story:27 — all changed `ParseError` → `PolicyLoadError` + a one-clause as-built note |

No bounded / sub-system / architectural / critical findings → no spawned
tasks, no draft stories, no draft ADRs, no `FINDINGS.md`.

## Gate summary

| Gate | Status |
|---|---|
| `ruff check` + `ruff format` | green (via `make check`) |
| `import-linter` | green — 6/6 contracts kept |
| `mypy --strict src/` | green — 200 source files |
| `pytest` + coverage (`--cov-fail-under=85`) | green (via `make check`, exit 0) |
| fence suite | green |
| `mkdocs build --strict` | green |
| CI (`26201043128`, HEAD `15d30a4`) | green — 20/20 jobs |
| CLI smoke (`codegenie`, `vuln-index`) | green |
| story integrity | 0 BLOCKED, 0 contradictory statuses |

## Budget

Wall-clock: ~14 min (dominated by one `make check` ≈ 4 min + one CI watch
≈ 11 min, overlapped). Token consumption: shakedown stage only, well within
the per-session budget; the shakedown reused gate results already produced
during the same session's `S5-04` execution rather than re-running them.

## Next-run primer

- Phase 3 is healthy through Step 5. The next shakedown should re-detect
  Phase 3 until Steps 6–9 land.
- When `S5-05` ships, Step 5 is complete; `transforms/report.py` should then
  exist — its absence today is expected, not a finding.
- The first *real* end-to-end (`codegenie remediate <repo> --cve <id>`)
  becomes runnable only after Step 6 (`S6-05`) ships. Until then, `make check`
  + the `tests/integration/` suites are the e2e proxy — a future shakedown
  should add the `remediate` happy-path run to its execution plan once
  `S6-05` is Done.
- F1's drift class (pre-execution prose not synced to a validator's
  in-story correction) is worth a glance on every shakedown: grep
  `High-level-impl.md` / `phase-arch-design.md` for contract surfaces of the
  most recently shipped story and diff against the as-built code.
- Observations O2/O3 remain open and are operator decisions, not Phase-3
  defects — re-surface them only if they start causing real friction.
