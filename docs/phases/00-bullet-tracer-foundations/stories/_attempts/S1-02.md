# S1-02 — Quality toolchain config — attempt log

## Attempt 1 — 2026-05-13 — SUCCESS

### Context

S1-01 had already landed `pyproject.toml` with `[build-system]`, `[project]`,
extras, `[project.scripts]`, and the three `[tool.hatch.*]` tables. No
quality-tool tables existed (`tool.ruff`, `tool.mypy`, `tool.pytest`,
`tool.coverage` were all absent). One existing test file
(`tests/unit/test_packaging.py`) had been written under whatever ruff default
ruled at the time — needed re-checking once `line-length = 100` landed.

### Red — failing test first

- Transcribed the story's six-test plan verbatim into
  `tests/unit/test_toolchain_config.py`.
- Ran `pytest -q tests/unit/test_toolchain_config.py` → all 6 failed (5 with
  `KeyError` on missing `[tool.*]` tables; AC-9 failed because without any
  `[tool.ruff]` config, `ruff check` on the canary returned exit 0).

### Green — minimum config

Appended five `[tool.*]` tables to `pyproject.toml`:

- `[tool.ruff]` — `target-version = "py311"`, `line-length = 100`.
- `[tool.ruff.lint]` — `select = ["E","F","I","B","UP","T201"]`. Chose `T201`
  (specific rule) over `T20` (family token) because phase-arch-design.md
  §Harness engineering names it explicitly.
- `[tool.ruff.format]` — empty table (the AC names the table as the opt-in
  marker; `ruff format` works against an empty sub-table).
- `[tool.mypy]` — `strict = true`, `warn_unreachable = true`,
  `python_version = "3.11"`.
- `[[tool.mypy.overrides]]` with `module = "tests.*"` and both relaxation
  flags set to `false`. Did NOT set `ignore_errors = true` (per story
  Implementer note — strict type-checking on fixture types still catches
  real bugs).
- `[tool.pytest.ini_options]` — `asyncio_mode = "auto"`, `testpaths =
  ["tests"]`, `addopts` with `--cov=src/codegenie --cov-branch
  --cov-fail-under=85` plus `-ra --strict-markers` for hygiene.
- `[tool.coverage.run]` — `branch = true`, `source = ["src/codegenie"]`.
- `[tool.coverage.report]` — `omit = ["src/codegenie/cli.py"]` (exact-equality
  per AC-4).

All 6 toolchain tests then passed.

### Refactor

- Added a section header comment above the toolchain block explaining the
  "one source of truth" stance and pointing future readers at the
  arch-design rationale for any change.
- Ran `ruff format .` — needed to reformat the new test file and the
  pre-existing `tests/unit/test_packaging.py` to 100-col. Diffs were purely
  cosmetic (no logic touched).
- Re-ran the full suite: 14 passed (8 packaging + 6 toolchain). `ruff check`,
  `ruff format --check`, and `mypy --strict src/` all exit 0.

### What worked / what didn't

- **What worked:** Going straight to the story's prescribed six-test plan
  produced six concrete failures with crisp diagnostics — the `[tool.*]`
  table additions then needed zero iteration to flip all six green.
- **What didn't:** Briefly considered using `T20` (the family token) in
  `select` to satisfy AC-1's "either/or" allowance; chose `T201` because the
  arch doc names it specifically and the explicit rule is easier to grep for.
  No actual cost either way.
- **Surprise:** `ruff format` reformatted `tests/unit/test_packaging.py` (an
  S1-01 file). Confirmed the diff was line-wrapping only, then committed —
  surgical-changes Rule 3 is preserved because the change is mechanically
  forced by the new `line-length = 100` contract, not by taste.

### Coverage carve-out

Per `High-level-impl.md §Step 1 Done-criteria`, the Step 1 PR may bypass
`--cov-fail-under=85` with `--cov-fail-under=0`. Locally, the full suite
hits ~26 % (`__main__.py` is 0 % since the help-text test runs as a
subprocess and coverage doesn't follow it). The gate goes live in S4-04.

### Evidence links

- Test file: `tests/unit/test_toolchain_config.py`
- Config: `pyproject.toml` `[tool.ruff]`, `[tool.ruff.lint]`,
  `[tool.ruff.format]`, `[tool.mypy]`, `[[tool.mypy.overrides]]`,
  `[tool.pytest.ini_options]`, `[tool.coverage.run]`, `[tool.coverage.report]`
- Tests run: `pytest -q tests/unit/test_toolchain_config.py --no-cov` →
  `6 passed`
- Full suite: `pytest -q --cov-fail-under=0` → `14 passed`
- Lint: `ruff check .` → exit 0; `ruff format --check .` → exit 0
- Types: `mypy --strict src/` → exit 0
