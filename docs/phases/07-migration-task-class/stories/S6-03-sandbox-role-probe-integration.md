# Story S6-03 — Integration test: `spawn(role=Role.PROBE)` boots microVM + Phase 5 regression suite green

**Step:** Step 6 — Phase 5 `SandboxRole` additive enum + `SandboxClient.spawn(role=...)` amendment
**Status:** HARDENED (phase-story-validator, 2026-08-15 — pre-executor pass; see `_validation/S6-03-sandbox-role-probe-integration.md`)
**Effort:** S
**Depends on:** S6-02 (`role` parameter on `spawn(...)`/`execute(...)` must exist, plus its `_normalize_run_for_byte_identity` normalization helper and `Final[tuple[str, ...]]` allow-list — see Preconditions §4), transitively S6-01 (enum) and S5-01 (allowlist); Phase 5 must have shipped `src/codegenie/sandbox/` **and** at least one pre-existing `tests/unit/sandbox/` or `tests/integration/test_sandbox_*.py` file for the regression suite to gate on — see Preconditions §1 and §3.
**ADRs honored:** Phase 7 ADR-0003 (primary), Phase 7 ADR-0002 (the consumer that this test pins for — but see Preconditions §5 on the `confidence`/`reason` category confusion this validator resolved), Phase 7 ADR-0001 (no parallel `probe-control` process), Phase 5 ADR-0001 (two-chokepoint sandbox seam), Phase 5 `final-design.md §Components §1 SandboxClient` + `§Failure modes` (canonical method name `execute(...)` and typed failure surface `SandboxRun.timed_out` / `.killed_by_oom` + `SandboxRunFailed` exception — see Preconditions §5)

## Preconditions

Read before starting:

1. **`src/codegenie/sandbox/` must exist at execution time.** As of 2026-08-15 the directory does not exist on `main` — mirrors S6-01 §1 and S6-02 §1. This story is `BLOCKED` on Phase 5's sandbox module landing (including at minimum: the `SandboxClient` Protocol, one concrete backend adapter, `SandboxSpec`, `SandboxRun`, and an audit-event model). Do not scaffold Phase 5 from within this story; escalate to Phase 5 and pause.
2. **Method-name reconciliation (`spawn` vs `execute`) inherits from S6-02 §2.** Phase 5's `final-design.md §Components §1` names the method `execute(spec: SandboxSpec) -> SandboxRun`; Phase 7 ADR-0003 and this story's arch references speak `spawn(...)`. Read `src/codegenie/sandbox/client.py` post-S6-02 and use whichever name S6-02 shipped verbatim throughout this story's tests. Do NOT alias, wrap, or rename. Log the resolution in `_attempts/S6-03.md`. All AC test-name examples in this story that say `test_..._spawn_...` may need to be renamed `test_..._execute_...` — the invariant is naming consistency with S6-02, not the literal string `spawn` in the test-function name.
3. **Phase 5 pre-existing test paths and integration-marker conventions must be discovered, not assumed.** As of 2026-08-15 no `tests/unit/sandbox/` directory and no `tests/integration/test_sandbox_*.py` files exist in this repo. Before writing this story's tests, `git grep -l 'sandbox' tests/` and locate Phase 5's *actual* test layout, its integration-test marker (the story guesses `@pytest.mark.phase07_integration` — Phase 5's convention may be `@pytest.mark.needs_kvm`, `@pytest.mark.integration_sandbox`, `@pytest.mark.privileged`, or something else), and its `--privileged`/KVM capability-detection fixture (candidates: `firecracker_available`, `has_kvm`, `runner_supports_microvm`). Rule 11 — match existing conventions. If Phase 5's convention is missing entirely (no prior integration tests for sandbox), coordinate with Phase 5 to add the marker in Phase 5's own test infrastructure PR, do **not** invent a Phase-7-only marker (that fragments the marker vocabulary Phase 5 owns).
4. **Reuse S6-02's `_normalize_run_for_byte_identity` helper and its `Final[tuple[str, ...]]` allow-list; do not re-declare.** S6-02 introduced (or will introduce) the normalization function and its enumerated-field allow-list for the unit-level 5-spec byte-identity matrix. AC-B here is the *runtime* complement of that check and MUST import S6-02's function verbatim. If S6-02 landed the helper under `tests/unit/`, promote it (with S6-02's coordination — a follow-up commit tagged `[promote]`) to `tests/_helpers/sandbox_normalization.py` or the location this repo already uses for cross-suite test helpers. Under Rule 3 (Surgical), do not duplicate the allow-list; a stealth divergence between S6-02's and S6-03's normalization sets would silently mask behavioral drift on the default path.
5. **Category correction: `confidence: "low"` and `reason: "build_failed"` live on the PROBE's `ProbeOutput`, NOT on `SandboxRun`.** ADR-0002 §Consequences row 5 is talking about `ShellInvocationTraceProbe`'s output (`ProbeOutput.confidence` / warning IDs `shell_invocation_trace.sandbox_boot_failed` and `shell_invocation_trace.build_failed`), which is S7-02's story. Phase 5's `SandboxRun` typed-failure surface is: `SandboxRun.timed_out: bool`, `SandboxRun.killed_by_oom: bool`, `SandboxRun.exit_code: int`, and the `SandboxRunFailed` exception for backend-level failures (see [Phase 5 `final-design.md §Failure modes`](../../05-sandbox-trust-gates/final-design.md)). AC-D in the original story asserted `SandboxRun.exit_status` carries `reason: "sandbox_boot_failed"` — that surface **does not exist on Phase 5's `SandboxRun`**. This validator rewrote AC-D to assert against Phase 5's actual typed failure surface. If the executor discovers Phase 5 later added an `exit_status`/`reason` field, coordinate with Phase 5's owners and update AC-D — do not silently re-invent a nonexistent field.
6. **Same-backend guard for the topology-identity assertion.** AC-A's `test_probe_topology_identical_to_gate` asserts `Role.GATE` and `Role.PROBE` produce identical `backend` / `gate_isolation_class` / resource-budget fields. Phase 5's `gate_isolation_class` is `"shared_kernel"` on DinD and `"microvm"` on Firecracker (Phase 5 `final-design.md` §Components §3 + §Components §4). If the test accidentally selects DinD for one call and Firecracker for the other on the same runner (e.g., different fixture invocations pick different clients), the assertion trivially fails — not because roles differ, but because backends differ. **Both calls in `test_probe_topology_identical_to_gate` MUST go through the same `SandboxClient` instance** — construct the client once, reuse for both `spawn(...)` calls, and assert `SandboxRun.backend` matches between the two runs as a preflight guard before comparing the other fields.
7. **`SandboxRun` field-name assumptions in the original AC list are contingent.** Story sketches used `SandboxRun.exit_status.success`, `SandboxRun.duration_ms`, `SandboxRun.cpu_quota`, `SandboxRun.memory_limit_mib`, `SandboxRun.pids_limit`, `SandboxRun.started_at`, `SandboxRun.trace_path`, `SandboxRun.audit_events`. Phase 5 `final-design.md §Components §2` shows `SandboxRun` with at minimum `run_id`, `exit_code`, `logs_dir`, `microvm_seconds`, `gate_isolation_class`, `trace_path: Path | None`, `timed_out: bool`, `killed_by_oom: bool`. The exact names of the CPU-quota / memory-limit / pids-limit / audit-events fields are contingent on Phase 5's shipped model. Before writing tests, `python -c "from codegenie.sandbox import SandboxRun; print(SandboxRun.model_fields.keys())"` and reconcile — the invariant is "resource-budget fields are identical between roles", not literally the field names the story sketched. If a field the story asserts on does not exist on the shipped model, either the field was renamed (use the shipped name) or the invariant is unobservable (drop the assertion and note in `_attempts/S6-03.md`).
8. **Phase 5's audit-event class name is contingent** (inherits from S6-02 §5). Story sketches `audit_events[*].role`; the class name may be `SpawnDispatchedEvent`, `SandboxRunEvent`, `ExecuteDispatchedEvent`, or something else. Grep `src/codegenie/sandbox/` for the shipped name and reuse verbatim. The invariant this story tests is "the audit-event stream carries the role tag with enum-identity on `Role.PROBE` runs and no `Role.PROBE` tags on default-arg runs" — the identity of the event class is contingent.
9. **Baseline JSON discipline** (AC-C bullet 2): `tests/_baselines/phase5_skip_counts.json` must be a **CODEOWNERS-gated file** in the same shape as [`tests/cassettes/anthropic/cassettes.lock`](../../../operations/cassettes.md) (see `docs/operations/cassettes.md §CODEOWNERS gate`). Any diff on the baseline requires the Phase-5 CODEOWNERS approver plus the Phase-7 CODEOWNERS approver in the same PR. If Phase 5's test suite is empty (i.e., no `@pytest.mark.skip` / `@pytest.mark.xfail` / `pytest.skip(...)` sites), the baseline is `{"skip_count": 0, "xfail_count": 0, "runtime_skip_count": 0, "recorded_at": "<ISO>", "recorded_from": "<git-sha>"}`. Bootstrap discipline: the *first* PR to introduce a legitimate skip (e.g., this story's `--privileged`-runner-gated tests) bumps the baseline **in the same commit** and lists the added skip's `reason=` string in the PR description. The fence test compares (a) the count against baseline, and (b) the set of skip `reason=` strings against a `Final[frozenset[str]]` allow-list at module scope — a new skip with an unauthorized `reason=` fails CI even if the count is unchanged (protects against "swap one skip for a stealth different one").
10. **"Everything skipped is a green build" defense is not a single test — it is a CI-workflow assertion.** AC-E bullet 3 as originally written asked for "a CI matrix test" without specifying where it runs. A pytest-level test cannot see what happened on *other* matrix entries. The correct home is a post-matrix aggregation step in `.github/workflows/*.yml`: after all matrix jobs report, an aggregator step downloads each job's junit-xml or pytest-cache summary, asserts that at least one matrix entry executed at least one function from `tests/integration/test_sandbox_client_role_probe.py::` (not just imported it, not just collected it — actually ran it and produced a `PASS` result), and fails the workflow otherwise. This is Phase 7 wiring on Phase 5's runner matrix; coordinate the workflow edit with the CI owner. If the aggregation step is not feasible in one story, split it into a follow-up story and mark AC-E bullet 3 as "shipped in follow-up" in `_attempts/S6-03.md` — but do NOT ship this story without it; the "everything skipped" failure mode defeats the whole purpose of the integration proof.

## Validation notes (phase-story-validator, 2026-08-15)

Hardened by the phase-story-validator pipeline; full audit in [`_validation/S6-03-sandbox-role-probe-integration.md`](_validation/S6-03-sandbox-role-probe-integration.md). Summary of edits:

1. **Preconditions section added (ten items)** — surfaced (§1) Phase 5's `sandbox/` module + tests do not yet exist, (§2) method-name reconciliation inherits from S6-02 §2, (§3) Phase 5 pre-existing test paths + integration-marker convention must be discovered, not assumed, (§4) S6-02's `_normalize_run_for_byte_identity` and its allow-list must be *reused*, not re-declared (Rule 3 + DRY-across-suites), (§5) **category correction — `confidence` / `reason` are probe-level, not `SandboxRun`-level; AC-D was asserting nonexistent Phase-5 surfaces**, (§6) same-backend guard for topology-identity check (a stealth cross-backend comparison would fail for the wrong reason), (§7) `SandboxRun` field-name assumptions are contingent — reconcile against Phase 5's shipped model, (§8) audit-event class name inherits from S6-02 §5, (§9) baseline JSON CODEOWNERS-gate + skip-`reason=` allow-list discipline (mirrors `docs/operations/cassettes.md` precedent), (§10) "everything skipped is a green build" defense is CI-workflow-level, not pytest-level — separate ownership boundary.
2. **AC-A hardening** — added same-backend preflight guard (`assert gate_run.backend == probe_run.backend`); enumerated the resource-budget field set as `Final[tuple[str, ...]]` (contingent on Preconditions §7); rewrote `SandboxRun.exit_status.success is True` → `assert gate_run.exit_code == 0 and probe_run.exit_code == 0` (Phase 5's actual surface per §Failure modes); sharpened the `audit_events[*].role` assertion from "at least one event has `role is Role.PROBE`" to "every event carrying a `role` field on a `Role.PROBE` run has `role is Role.PROBE`, and none have `role is Role.GATE`" (defeats a stealth per-callsite tag inconsistency); added an identity-not-equality note (mirrors S6-02).
3. **AC-B hardening** — MUST import `_normalize_run_for_byte_identity` from the S6-02 helper location (per Preconditions §4), not re-declare; runtime byte-identity is asserted on **exactly two** specs (the minimum needed to prove runtime), not five — the five-spec matrix is unit-level (S6-02's territory) and duplicating five real-backend runs here inflates CI wall-clock for zero additional invariant coverage (Rule 2 + Rule 3); explicit note that the two integration specs must span at least one `command` variation *and* at least one `env` variation to exercise the two hot paths through the dispatcher.
4. **AC-C hardening** — added a companion `test_baseline_shape_valid` guard asserting the JSON structure (`skip_count`, `xfail_count`, `runtime_skip_count`, `recorded_at`, `recorded_from`, `authorized_skip_reasons: list[str]`) so a hand-edit that corrupts the baseline fails loudly; added the skip-`reason=` allow-list assertion (a new skip with an unauthorized `reason=` fails even when the count is baseline-equal — see Preconditions §9); named the file `tests/_baselines/phase5_skip_counts.json` explicitly across all references (was inconsistent in the original); added an empty-baseline bootstrap case (`{"skip_count": 0, ...}` is legal); the "zero golden-file deletions" assertion tightened to hash the pre-existing golden files at story start and compare — a mere `git diff --name-status` misses a delete+re-add cycle.
5. **AC-D rewritten** — original assertions on `SandboxRun.exit_status` carrying a typed `reason: "sandbox_boot_failed"` referenced a nonexistent Phase-5 surface (Preconditions §5). Rewritten as: (a) deliberate backend-boot failure raises `SandboxRunFailed` (Phase 5's actual exception, per `final-design.md §Failure modes`) — assert exception class and message shape; (b) deliberate timeout produces `SandboxRun.timed_out is True` — Phase 5's actual `bool` field; (c) the `Role.PROBE + capture_trace=False` orthogonality-negative case retained (this WAS a valid Phase-5-surface assertion, unchanged); (d) added: on a failed `Role.PROBE` boot, the audit-event stream still carries `role is Role.PROBE` before the exception propagates (so downstream forensics can attribute the failure to a probe run, not a gate run — a stealth "clear audit tags before raising" would silently lose that provenance).
6. **AC-E hardening** — pinned the marker-name discovery discipline to Preconditions §3 (the story no longer prescribes `@pytest.mark.phase07_integration` as if it exists); reframed the "everything skipped" defense as CI-workflow-level per Preconditions §10 (not a pytest test); added the explicit skip-reason string (`Skip: reason="firecracker_requires_privileged_or_kvm; dind_path_tested_on_darwin_runner"` — a stable string the Preconditions §9 allow-list references); the skip-reason string is a `Final[str]` at module scope so a hand-edit that changes the wording fails both the local test file and the baseline allow-list check.
7. **AC-F hardening** — added an S5-01 fence dry-run AC: `pytest tests/fence/test_phase7_byte_edit_allowlist.py` exits 0 on the post-S6-03 branch (implicit before, but this story does add new files in `tests/`, and while `tests/` is not on the fence's covered set today, verifying the fence stays green catches a scope-widening bug); added `pytest tests/fence/` runs green as a whole (all fences, not just S5-01) — this story's baseline JSON introduction under `tests/_baselines/` is a new artifact and a Phase-1-style structural defense sweep is warranted.
8. **New AC-G — Runtime concurrency safety** — the runtime complement of S6-02's stub-level `test_concurrent_spawn_role_tags_correct`. Two concurrent real-backend `spawn(...)` calls (one `Role.GATE`, one `Role.PROBE + capture_trace=True`) via `asyncio.gather` on the same `SandboxClient` instance: each returned `SandboxRun`'s `audit_events` carry only the role tag for its own run (no cross-contamination); each `trace_path` is either `None` (GATE) or a distinct non-empty file (PROBE); no shared eBPF capture file between the two runs. Guards against a stealth module-level `_current_role` mutable and against an eBPF-attach path that captures to a global rather than a per-run buffer. Gated by the same runner-capability skip as the rest of the suite.
9. **TDD plan hardening** — enumerated the concrete test-function names for each new AC (see AC-A, AC-D, AC-G edits); added the "red mode" for AC-D's rewrite: if Phase 5's `SandboxRun` does not have `timed_out`/`killed_by_oom` fields, that's a Phase-5 shipping bug (they're named in `final-design.md`) and the story is `BLOCKED` on Phase 5 fixing the shipped model — not on rewriting AC-D again; added the "red mode" for AC-B: if S6-02's normalization helper does not exist at the expected path, S6-02 shipped incompletely — reopen S6-02, do not re-declare the helper here.
10. **Notes for the implementer** — added five paragraphs: (a) "The two load-bearing categories" clarifies that AC-A + AC-G prove the *new* behavior works, AC-B + AC-C prove the *old* behavior is unchanged, and AC-D proves the *failure modes* are honestly typed — three independent invariant families that must all pass, (b) "Baseline JSON as CODEOWNERS-gated artifact" pointing at `docs/operations/cassettes.md`, (c) "Category correction on `confidence`/`reason`" pointing at Preconditions §5 with a warning against re-inventing the surface, (d) "Marker-name discovery is a Rule-11 obligation" pointing at Preconditions §3, (e) "Same-backend guard is not optional" pointing at Preconditions §6 with an example of the failure mode (Firecracker vs DinD `gate_isolation_class` divergence on the same runner). Preserved the original five paragraphs.
11. **Design-pattern nudge (not an AC — Rule 2)** — extension-by-addition for future roles (`Role.RECIPE`, `Role.AUDIT` per ADR-0003 §Consequences row 8): the topology-identical assertion is a natural `@pytest.mark.parametrize("non_gate_role", [...])` fixture. Today with one non-gate role (`PROBE`), the parametrize saves nothing; when the second non-gate role lands (Phase 8+), the extension is one enum member + one row in a `_TOPOLOGY_EQUIVALENT_ROLES: Final[tuple[SandboxRole, ...]]` tuple at module scope, zero test-body edits (Open/Closed for the test file). Noted in Notes-for-implementer as a shape preference, not required — Rule 2 (Simplicity First) forbids introducing the abstraction before its second consumer arrives, but the *name* and *location* of the tuple are cheap to pick today so the future extension is one row, not a refactor.

**Verdict:** HARDENED — no structural block on the story itself. Story is executable once (a) Phase 5's `sandbox/` module and pre-existing test suite land (Preconditions §1, §3), (b) S6-02 ships the normalization helper at a shared location (Preconditions §4), (c) the CI-workflow-level "everything skipped" aggregator step is scoped with Phase 5's CI owner (Preconditions §10). If any Precondition fails, mark `BLOCKED` in `_attempts/S6-03.md` with the specific §-reference — do not sneak-fix Phase-5-shaped work inside a Phase-7 story.

## Context

S6-01 + S6-02 ship the API surface — the `SandboxRole` enum and the `role` parameter on `SandboxClient.spawn(...)`. This story ships the **integration proof** that ADR-0002 §Decision binds: under `Role.PROBE` the microVM boots with **identical topology** to `Role.GATE` *plus* eBPF host-side trace capture *plus* a short container boot, and the default-arg path remains byte-identical to pre-amendment.

The reason this is a separate story is that the integration test exercises real backend dispatch (Firecracker on KVM-equipped Linux runners, DinD on macOS dev, Lima where Phase 5's stack adopts it). Unit tests with stub `SandboxClient`s already covered the API surface in S6-02; this story is the *behavioral* proof. Without it, "identical-plus-trace-capture" remains an unverified architectural claim — and ADR-0002 §Tradeoffs row 4 explicitly warns "Fence is structural; doesn't catch dynamic code that bypasses the AST check." This integration test is the runtime check that bypass attempts produce observable evidence.

The Phase 5 regression-suite green-light is the **second** load-bearing assertion in this story. ADR-0003 §Consequences row 4 says "Every existing `SandboxClient.spawn(...)` call site in Phase 5 keeps working unchanged (default `Role.GATE`)." This story is where that claim becomes a CI gate, not just a design promise. S5-01's byte-edit allowlist catches *structural* drift; this story catches *behavioral* drift — a stealth change to the dispatcher that breaks gate semantics for `Role.GATE` callers would slip past the fence but fail this story's regression assertion.

This is a `@pytest.mark.phase07_integration` (or equivalent — match Phase 5's existing integration-test marker convention) test; on `--privileged` Linux CI runners Firecracker is exercised, on macOS / non-privileged runners DinD is exercised, and `gate_isolation_class` propagates correctly through both paths.

## References — where to look

- **Architecture:**
  - [`../phase-arch-design.md §Component design §9 (ShellInvocationTraceProbe)`](../phase-arch-design.md) — the call shape this test pins for: `spawn(role=Role.PROBE, workspace=..., command=["docker","buildx","build","--target=builder","."], capture_trace=True)`.
  - [`../phase-arch-design.md §Harness engineering — sandbox amendment`](../phase-arch-design.md) — the integration-test discipline section; describes the fixture portfolio.
  - [`../phase-arch-design.md §Tradeoffs (consolidated)`](../phase-arch-design.md) — row "default-arg path byte-identical".
- **Phase ADRs:**
  - [`../ADRs/0003-sandbox-role-additive-enum-on-spawn.md`](../ADRs/0003-sandbox-role-additive-enum-on-spawn.md) — §Consequences row 4 (every existing callsite unchanged), row 6 (integration test asserts identical topology + audit-log tag).
  - [`../ADRs/0002-shell-invocation-trace-probe-runs-in-microvm.md`](../ADRs/0002-shell-invocation-trace-probe-runs-in-microvm.md) — §Consequences row 5 (failure modes are typed: `confidence: "low"` with `reason: "build_failed"` etc.).
  - [`../ADRs/0001-no-multi-plugin-coordinator-in-phase-7.md`](../ADRs/0001-no-multi-plugin-coordinator-in-phase-7.md) — context: no parallel `probe-control` apparatus.
- **Source design:**
  - [`../final-design.md §Risks #1`](../final-design.md) — the fallback if Phase 5 rejects (the test must distinguish "Role.PROBE means probe-tagged GATE topology" from the fallback path).
- **Phase 5 context:**
  - [`../../05-sandbox-trust-gates/final-design.md §Components §1 SandboxClient`](../../05-sandbox-trust-gates/final-design.md) + [`§Goals #3 / #4`](../../05-sandbox-trust-gates/final-design.md) — KVM-Firecracker vs DinD fallback discipline; this story respects both.
  - [`../../05-sandbox-trust-gates/ADRs/0001-two-chokepoint-sandbox-seam.md`](../../05-sandbox-trust-gates/ADRs/0001-two-chokepoint-sandbox-seam.md) — the seam.
  - Phase 5's existing `tests/integration/test_sandbox_*.py` suite — the regression target.
- **High-level impl:**
  - [`../High-level-impl.md §Step 6`](../High-level-impl.md) — Done criteria bullet 3 + bullet 4.

## Goal

Ship `tests/integration/test_sandbox_client_role_probe.py` and a companion `tests/integration/test_sandbox_role_gate_regression.py` (or equivalent — name per Phase 5's integration test conventions) such that:

1. **`spawn(role=Role.PROBE, capture_trace=True)` boots a microVM with identical topology to `spawn(role=Role.GATE)`** — same backend, same `gate_isolation_class`, same resource budgets — *plus* eBPF host-side trace capture is enabled in the returned `SandboxRun.trace_path` (or backend-equivalent capture artifact) and the audit log carries `role: "probe"`.
2. **The default-arg path** (`client.spawn(spec)` without explicit `role`) produces a `SandboxRun` byte-equal under normalization to `client.spawn(spec, role=Role.GATE)`. This is the runtime check that S6-02's unit test only proves at the stub level.
3. **Phase 5's existing test suite is green with zero new test skips, zero `xfail` markers added, and zero golden file deletions.** The full `pytest tests/unit/sandbox/ tests/integration/test_sandbox_*.py` run exits 0 on the post-S6-03 branch.
4. **Failure modes are typed.** A deliberate broken `Role.PROBE` boot (e.g., a malformed spec) produces a `SandboxRun` with the typed `confidence: "low"` annotation and `reason: "build_failed"` (or backend-specific reason) per ADR-0002 §Consequences row 5 — no uncaught exception, no `None` return.

## Acceptance criteria

### A. `Role.PROBE` topology proof

- [ ] `tests/integration/test_sandbox_client_role_probe.py::test_probe_role_boots_microvm_with_capture` — runs `client.spawn(probe_spec, role=SandboxRole.PROBE, capture_trace=True)` (or `.execute(...)` per Preconditions §2) against the Phase 5 backend the CI runner supports (Firecracker on `--privileged` Linux, DinD on macOS or non-privileged Linux); asserts `probe_run.exit_code == 0` (Phase 5's actual success surface per Preconditions §5) **and** `probe_run.trace_path is not None and probe_run.trace_path.exists() and probe_run.trace_path.stat().st_size > 0`.
- [ ] `tests/integration/test_sandbox_client_role_probe.py::test_probe_topology_identical_to_gate` — constructs **one** `SandboxClient` instance (Preconditions §6, same-backend guard), runs the *same* `spec` (modulo the `capture_trace` flag) under both `SandboxRole.GATE` and `SandboxRole.PROBE`. **Preflight guard:** `assert gate_run.backend == probe_run.backend` (fail-fast if the same client produces divergent backends between calls — that would be a Phase-5 dispatcher bug, not a Phase-7 role-tagging bug). Then asserts field-by-field equality on a `_TOPOLOGY_EQUIVALENT_FIELDS: Final[tuple[str, ...]]` allow-list enumerated at module scope. Initial contents (contingent on Preconditions §7 — reconcile against Phase 5's shipped `SandboxRun` model): `("backend", "gate_isolation_class")` plus whichever resource-budget field names Phase 5 actually shipped for CPU / memory / pids. A stealth widening of the allow-list fails a companion `test_topology_equivalent_fields_enumerated` (see AC-B pattern). The only differences permitted are: (i) `trace_path` is non-`None` only under `Role.PROBE + capture_trace=True`; (ii) role-tagged audit-event entries differ per Preconditions §8; (iii) timing fields (`microvm_seconds` per Phase 5's shipped name — reconcile) and any `run_id`-shaped nonce.
- [ ] `tests/integration/test_sandbox_client_role_probe.py::test_probe_audit_event_role_tag_universal` — asserts **every** audit-event entry (from Preconditions §8 — the actual class name) on a `Role.PROBE` run that carries a `role` attribute satisfies `e.role is SandboxRole.PROBE` (enum-identity, not `==` — guards against a stealth `str`-bleed where `e.role` is the raw string `"probe"` per S6-02's test-1 finding), and **no** such entry has `e.role is SandboxRole.GATE`. Universal-quantification (not existential) defeats a stealth per-callsite tag-inconsistency where only *some* audit sites emit `PROBE`.

### B. Default-arg path byte-identity (runtime)

- [ ] `tests/integration/test_sandbox_role_gate_regression.py::test_default_arg_byte_equal_to_role_gate` — imports `_normalize_run_for_byte_identity` from S6-02's shared helper location (Preconditions §4 — do NOT re-declare); constructs one `SandboxClient` (same-backend guard, Preconditions §6); calls `client.spawn(gate_spec)` and `client.spawn(gate_spec, role=SandboxRole.GATE)` against the real backend the runner supports; asserts `_normalize_run_for_byte_identity(default_run) == _normalize_run_for_byte_identity(explicit_run)`. Runtime complement to S6-02's stub-level check.
- [ ] Runtime byte-identity is asserted on **exactly two** hand-picked `SandboxSpec` variations — one differing in `command`, one differing in `env` — not five. Rationale: the exhaustive 5-spec matrix is unit-level (S6-02's territory); duplicating five real-backend runs here inflates CI wall-clock for zero additional invariant coverage (Rule 2 + Rule 3). The two specs are enumerated in the test file with a comment naming which dispatcher hot path each exercises. A companion `test_normalization_helper_is_shared` asserts the helper is imported from the S6-02 location, not defined locally (guards against a stealth re-declaration that would silently diverge from S6-02's allow-list).
- [ ] `tests/integration/test_sandbox_role_gate_regression.py::test_default_arg_audit_event_role_is_gate_universal` — universal-quantification (not existential): **every** audit event on the default-arg path that carries a `role` attribute satisfies `e.role is SandboxRole.GATE`, and none carry `role is SandboxRole.PROBE`. Identity-not-equality (matches AC-A).

### C. Phase 5 regression suite green

- [ ] On the post-S6-03 branch, the invariant is **"every pre-existing Phase-5 test still passes, and every pre-existing Phase-5 test still runs (or is baseline-authorized to skip)."** Concretely: `pytest tests/unit/sandbox/ tests/integration/test_sandbox_*.py` exits 0. Test paths reconcile against Phase 5's actual layout (Preconditions §3); the invariant is not the literal paths. Capture the full run output in `_attempts/S6-03.md` for auditability.
- [ ] **`tests/fence/test_phase5_regression_no_skips.py`** ships as a static scan (AST + regex) over Phase 5's test directories counting `@pytest.mark.skip`, `@pytest.mark.xfail`, `@pytest.mark.skipif`, and `pytest.skip(...)` call-sites. It compares against `tests/_baselines/phase5_skip_counts.json` (Preconditions §9 — CODEOWNERS-gated, mirroring `docs/operations/cassettes.md`). The baseline JSON schema is: `{"skip_count": int, "xfail_count": int, "runtime_skip_count": int, "recorded_at": "<ISO>", "recorded_from": "<git-sha>", "authorized_skip_reasons": [str, ...]}`. Any net-positive count diff, OR any new skip whose `reason=` string is not in `authorized_skip_reasons`, fails CI. A `test_baseline_shape_valid` companion asserts the JSON structure so a hand-edit that corrupts it fails loudly.
- [ ] The two authorized skip-reason strings this story adds to `authorized_skip_reasons` are defined at module scope as `Final[str]` in the integration test file so a hand-edit that changes the wording fails both the local test and the baseline allow-list (Preconditions §9): `_SKIP_REASON_NO_PRIVILEGE: Final[str] = "firecracker_requires_privileged_or_kvm; dind_path_tested_on_darwin_runner"` and (if this story ships a second skip site) one further stable-`Final[str]` string with matching allow-list entry.
- [ ] **Zero golden-file deletions** under `tests/golden/sandbox/`. Implementation: at story start, capture `{path: sha256(content)}` for every file under `tests/golden/sandbox/` into a fixture and, post-edits, assert every pre-existing path still exists and its sha256 is either unchanged OR appears in a `_AUTHORIZED_GOLDEN_UPDATES: Final[frozenset[str]]` allow-list (empty for this story — this story ships no golden updates). A `git diff --name-status` alone misses a delete+re-add cycle.
- [ ] `make check` exits 0.

### D. Typed failure-mode coverage (Phase-5 surface, per Preconditions §5)

- [ ] `tests/integration/test_sandbox_client_role_probe.py::test_probe_backend_boot_failure_raises_sandbox_run_failed` — induces a microVM boot failure on `Role.PROBE` (e.g., malformed `workspace` path, invalid Firecracker rootfs digest, or a backend-specific deliberate failure). Asserts `pytest.raises(SandboxRunFailed)` — Phase 5's actual exception per `final-design.md §Failure modes`, NOT a `SandboxRun.exit_status.reason` field (which does not exist per Preconditions §5). The exception `str(exc)` (or `.args[0]`) is asserted with anchored regex `match=r"^sandbox_run_failed: role=PROBE"` (or the message shape Phase 5 shipped — reconcile if Phase 5's `SandboxRunFailed` uses a different message discipline; the invariant is "the exception message carries the role so downstream forensics can attribute").
- [ ] `tests/integration/test_sandbox_client_role_probe.py::test_probe_timeout_returns_run_with_timed_out_true` — induces a timeout on `Role.PROBE` (small `time_budget_seconds`, `command` that sleeps). Asserts `probe_run.timed_out is True` (Phase 5's actual `bool` field per `final-design.md §Components §2`) and `probe_run.exit_code != 0`. No exception; a timeout returns a `SandboxRun`, not raises (per Phase 5's `§Failure modes` row for `time_budget_seconds`).
- [ ] `tests/integration/test_sandbox_client_role_probe.py::test_probe_failure_still_tags_role` — on the boot-failure path (before the exception propagates) OR the timeout path (on the returned `SandboxRun`), audit events already emitted carry `role is SandboxRole.PROBE`. A stealth "clear audit tags before raising" would silently lose that forensic attribution — this test pins it.
- [ ] `tests/integration/test_sandbox_client_role_probe.py::test_probe_capture_trace_false_succeeds` — runtime confirmation of S6-02's orthogonality claim: `client.spawn(probe_spec, role=SandboxRole.PROBE, capture_trace=False)` succeeds; `probe_run.trace_path is None`; audit log still carries `role is SandboxRole.PROBE` on all role-carrying events (universal-quantification, matching AC-A).

### E. CI matrix discipline

- [ ] The integration test files declare Phase 5's actual integration-test marker (Preconditions §3 — do NOT invent `@pytest.mark.phase07_integration` if Phase 5 uses a different name; if Phase 5 has no such marker, coordinate the marker-add in a Phase-5 test-infrastructure PR *first*).
- [ ] On runners that lack the privileged capability (no KVM, no `--privileged`), the test gracefully `pytest.skip(reason=_SKIP_REASON_NO_PRIVILEGE)` where `_SKIP_REASON_NO_PRIVILEGE` is the module-scope `Final[str]` from AC-C bullet 3. Phase 5's existing fixture-skip mechanism is the precedent (Phase 5 §Goals #3) — reuse the fixture, don't roll a new capability probe.
- [ ] **"Everything skipped is a green build" defense (Preconditions §10):** a post-matrix aggregator step in `.github/workflows/*.yml` downloads each matrix job's junit-xml (or equivalent artifact), parses it, and fails the workflow if zero matrix entries executed any test function from `tests/integration/test_sandbox_client_role_probe.py::` to a `PASS` state (a skip does not count; an import-only collection does not count). If this workflow edit is scoped to a follow-up story per Preconditions §10, mark the follow-up story ID and open-PR link in `_attempts/S6-03.md` and DO NOT close S6-03 without either (a) the aggregator landing in the same commit, or (b) the follow-up story being `Ready` and referenced.

### F. Fence + regression-detector composition

- [ ] S5-01's byte-edit allowlist fence remains green (this story adds no new byte-edits to Phase 0–6.5 files under S5-01's covered set — it ships only new test files under `tests/`).
- [ ] `pytest tests/fence/` exits 0 (all fences, not just S5-01) — the new `tests/_baselines/phase5_skip_counts.json` artifact is a Phase-1-style structural defense addition; a fence-sweep here catches scope-widening bugs early.
- [ ] `mypy --strict tests/integration/test_sandbox_client_role_probe.py tests/integration/test_sandbox_role_gate_regression.py tests/fence/test_phase5_regression_no_skips.py` clean.
- [ ] `ruff check tests/integration/ tests/fence/` + `ruff format --check tests/integration/ tests/fence/` clean for the new files.

### G. Runtime concurrency safety (new — pairs with S6-02's stub-level `test_concurrent_spawn_role_tags_correct`)

- [ ] `tests/integration/test_sandbox_client_role_probe.py::test_concurrent_gate_and_probe_runs_do_not_cross_contaminate` — constructs one `SandboxClient` instance; via `asyncio.gather` (or the concurrency primitive Phase 5's `SandboxClient` supports — `spawn` returns a coroutine per ADR-0003 wording; reconcile with Phase 5's actual sync/async surface), fires **one** `Role.GATE` run and **one** `Role.PROBE + capture_trace=True` run concurrently against the real backend. Asserts: (i) the two returned `SandboxRun`s have distinct `run_id`s; (ii) `gate_run.trace_path is None` AND `probe_run.trace_path is not None`; (iii) `gate_run.trace_path != probe_run.trace_path` (a stealth global-capture-file bug would make them equal or both `None`); (iv) universal-quantification role-tag check: every role-carrying audit event on `gate_run` has `role is SandboxRole.GATE`, every one on `probe_run` has `role is SandboxRole.PROBE`, and no cross-contamination in either direction. Gated by the same runner-capability skip as the rest of the suite. Guards against a stealth module-level `_current_role` mutable and against an eBPF-attach path that captures to a global buffer rather than a per-run one.

## Implementation outline

1. **Read Phase 5's integration test conventions** under `tests/integration/test_sandbox_*.py`: marker names, fixture names (`gate_spec`, `firecracker_client`, `did_client`, etc.), how runner-capability detection works. Rule 11 — Match existing conventions. The new test files inherit those conventions.
2. **Reconcile the method name** (`spawn` vs `execute` — see S6-02 Notes). Whichever the codebase uses, the integration tests call that method directly. Do not introduce a wrapper or alias.
3. **Write `tests/integration/test_sandbox_client_role_probe.py`** with the AC-A and AC-D test functions. Each function uses Phase 5's existing `gate_spec` / equivalent fixture for the GATE baseline; constructs a parallel `probe_spec` (typically the same spec with `capture_trace` semantics layered on).
4. **Write `tests/integration/test_sandbox_role_gate_regression.py`** with the AC-B test functions. These call `spawn(...)` against a real backend (matrix-selected per runner capability) and compare the default-arg run against the explicit-`Role.GATE` run under a normalization function defined once and reused across both Phase 7 and any future regression suite (extract to `tests/integration/_sandbox_normalization.py` or similar; mind Rule 3 — Surgical Changes — only create the helper if necessary).
5. **Write `tests/unit/test_phase5_regression_no_skips.py`** (AC-C bullet 2) — a small grep/AST scan that counts skip / xfail markers in Phase 5's test directories and asserts the count is `≤` the baseline. Baseline can be a checked-in JSON file (`tests/_baselines/phase5_skip_counts.json`) refreshed only via a CODEOWNERS-gated edit.
6. **Run the full integration suite locally** on whatever backend the dev machine supports; capture output in `_attempts/S6-03.md` for the validator.
7. **Run `make check`** — confirm Phase 5 + Phase 7 green.

## TDD plan (red → green → refactor)

Note: this story is a **test-only** story (the production code lands in S6-02). The "red" here is that the integration tests fail to *find* the behavior they assert — either because S6-02's `role` parameter isn't wired correctly (in which case S6-02 is incomplete and this story blocks) or because Phase 5's backend dispatcher hasn't been wired to honor `capture_trace` end-to-end (in which case S6-02 needs amending). This story's red is a diagnostic for the API-shipping stories.

### Red

Write the test files end-to-end against the AC list. Run on a `--privileged` Linux dev box (or under the CI matrix in draft-PR mode). Enumerated red-mode diagnostic table (each entry maps a red to an owner story — the executor MUST correctly attribute, not silently fix Phase-5-shaped work inside this story):

1. `test_probe_role_boots_microvm_with_capture` red → `capture_trace=True` does not enable trace capture end-to-end. Owner: **S6-02** (Path A/B dispatcher wire-up). S6-03 is BLOCKED; reopen S6-02.
2. `test_probe_topology_identical_to_gate` red → backend silently changes resource budgets under `Role.PROBE`. Two escalation paths: (a) the dispatcher is buggy — owner **Phase 5** / S6-02 depending on where the divergence lives; (b) ADR-0003's "identical topology" claim needs revision — that's a **RESCUE**-tier escalation per validation grammar; file an ADR amendment before touching any code.
3. `test_probe_backend_boot_failure_raises_sandbox_run_failed` red → Phase 5's `SandboxRunFailed` exception does not exist, or does not carry the role in its message. Owner: **Phase 5** shipping bug (per `final-design.md §Failure modes`). S6-03 is BLOCKED; reopen Phase 5's sandbox module.
4. `test_probe_timeout_returns_run_with_timed_out_true` red → Phase 5's `SandboxRun.timed_out` field does not exist. Owner: **Phase 5** shipping bug (Preconditions §7). S6-03 is BLOCKED.
5. `test_default_arg_byte_equal_to_role_gate` red — two sub-modes:
   - Import error on `_normalize_run_for_byte_identity` → S6-02 shipped the helper but not at the shared location (or did not promote it out of `tests/unit/`). Owner: **S6-02** — reopen and add a `[promote]` follow-up commit. Do NOT re-declare the helper here.
   - Equality assertion fails → default-arg path drifted. Owner: **S6-02** (the byte-identity invariant is S6-02's; S6-03 is the runtime detector, not the fix site). Reopen S6-02.
6. `test_concurrent_gate_and_probe_runs_do_not_cross_contaminate` red → hidden shared state (module-level `_current_role`) or global eBPF capture. Owner: **S6-02** dispatcher OR **Phase 5** sandbox backend, depending on where the shared state lives. Bisect and reopen the correct upstream story.

### Green

Path (a): S6-02 + Phase 5 are correct and the runtime + concurrency + failure-mode tests all pass against the real backend on at least one CI matrix entry AND the aggregator confirms not every matrix entry skipped (AC-E bullet 3). Path (b): the tests reveal an S6-02 or Phase-5 gap that must be fixed **upstream** before this story can close. Fix-S6-02 or fix-Phase-5 work is **not in scope** for this story; if any AC fails to a diagnosis in the Red table pointing at an upstream owner, S6-03 is `BLOCKED` and the upstream story is reopened.

### Refactor

Verify the normalization helper is imported from S6-02's shared location, not duplicated (AC-B `test_normalization_helper_is_shared` is the fence); verify `mypy --strict` + `ruff` clean on every new file (AC-F); verify no fixture shadowing or marker collisions with Phase 5's existing test infrastructure (Preconditions §3); verify the topology-equivalent-fields tuple, the skip-reason `Final[str]` constants, and the authorized-golden-updates frozenset are all at module scope (not function-local) so a hand-edit that narrows them fails at import time, not at test time.

## Files to touch

- `tests/integration/test_sandbox_client_role_probe.py` — new file, ACs A + D + parts of E.
- `tests/integration/test_sandbox_role_gate_regression.py` — new file, AC B.
- `tests/integration/_sandbox_normalization.py` (or similar) — optional, only if AC-B's normalization logic is reused.
- `tests/unit/test_phase5_regression_no_skips.py` — new file, AC C bullet 2.
- `tests/_baselines/phase5_skip_counts.json` — new baseline file, CODEOWNERS-gated for future refreshes.

This story **does not touch** `src/codegenie/` — the byte-edit allowlist counter stays at the level S6-02 left it.

## Out of scope

- Fixing any S6-02 gap revealed by AC-A's failure — if `Role.PROBE` doesn't actually enable trace capture end-to-end, that's a defect in S6-02 and reopens that story; S6-03 stays BLOCKED until S6-02 is fixed.
- `ShellInvocationTraceProbe`'s probe-level integration test (calling `ctx.sandbox_client.spawn(role=Role.PROBE)` from inside the probe's `run()`) — owned by **S7-02** and **S7-05**.
- `DistrolessBuildGate` / `ShellInvocationDeltaGate` real-microVM integration (they use `Role.GATE` — the default — which AC-B already covers, but their gate-specific assertions belong to **S10-04** and **S10-05**).
- Phase 5 backend additions (e.g., real Lima integration on macOS) — Phase 5's roadmap, not Phase 7's.
- Performance / latency budgets for `Role.PROBE` boots (cold p99, warm p99) — those are bench-tier concerns in **S12-05**, not integration-correctness concerns here.

## Notes for the implementer

- **Three load-bearing invariant families, not two.** The original story framed AC-A vs AC-B/C as a two-family split; the validator's re-read of ADR-0002 §Consequences separates a third: **AC-A + AC-G prove the new behavior works end-to-end** (topology + concurrency); **AC-B + AC-C prove the old behavior is unchanged** (byte-identity + regression); **AC-D proves the failure modes are honestly typed on Phase 5's *actual* surface** (`SandboxRunFailed` / `SandboxRun.timed_out` — NOT on an invented `SandboxRun.exit_status.reason` field, per Preconditions §5). A green build requires all three. A failure on AC-A or AC-G is a "Phase 7 added new behavior that doesn't actually work" defect. A failure on AC-B or AC-C is a "Phase 7 broke Phase 5" defect — far worse. A failure on AC-D is a "the fallback code path lies about what happened" defect — the worst class (a system that appears to succeed when it silently failed). Treat regressions on AC-B / AC-C / AC-D with elevated severity (Rule 12 — Fail loud).
- **Baseline JSON as CODEOWNERS-gated artifact.** `tests/_baselines/phase5_skip_counts.json` follows exactly the discipline `docs/operations/cassettes.md` describes for the Anthropic-cassette manifest: two-CODEOWNERS approval (Phase 5 + Phase 7), a stable JSON schema pinned by a shape-validity companion test, and a written PR-description convention for any change (name the added skip's `reason=` string, cite the story). Do not invent a new discipline; reuse the cassette-manifest playbook so future baseline-family fences (Phase 6.5 skip counts, Phase 8+) can compose without a taxonomy explosion.
- **Category correction on `confidence` / `reason` — do not re-invent the surface.** Preconditions §5 spells out why AC-D was rewritten. If the executor reads ADR-0002 §Consequences row 5 and sees `confidence: "low"` / `reason: "build_failed"` and reflexively adds an `exit_status.reason` field to Phase 5's `SandboxRun`: **stop.** Those are `ShellInvocationTraceProbe`'s `ProbeOutput` fields (S7-02's territory), not `SandboxClient`'s `SandboxRun` fields. The correct Phase-5-surface assertion is `SandboxRunFailed` (exception) OR `SandboxRun.timed_out`/`killed_by_oom` (fields). Silently widening Phase 5's model to accommodate this story is a Rule-3 violation *and* it collides with S5-01's byte-edit allowlist fence for `src/codegenie/sandbox/` (row 6 authorizes only the `role` parameter — not a new `exit_status` sub-model).
- **Marker-name discovery is a Rule-11 obligation.** Preconditions §3 spells this out. Do not ship a `@pytest.mark.phase07_integration` marker unless Phase 5 already has that marker; if Phase 5 has a different name (`needs_kvm`, `integration_sandbox`, `privileged`), use it. If Phase 5 has *no* marker for `--privileged`-runner-gated tests, do NOT invent a Phase-7-only marker — that fragments the marker vocabulary Phase 5 owns. Coordinate: file a small Phase-5 test-infrastructure PR that adds the marker + registers it in `pyproject.toml § [tool.pytest.ini_options]::markers`, then land S6-03 on top of that.
- **Same-backend guard is not optional.** Preconditions §6 spells out the failure mode: if `test_probe_topology_identical_to_gate` accidentally sends `Role.GATE` to DinD (`gate_isolation_class="shared_kernel"`) and `Role.PROBE` to Firecracker (`gate_isolation_class="microvm"`) on the same runner, the field-by-field equality check fails — but for the wrong reason (backend divergence, not role divergence). The AC-A preflight `assert gate_run.backend == probe_run.backend` catches this early with a clear failure message; without it, the executor spends hours chasing a phantom role-tagging bug that's actually a fixture-selection bug.
- **Runner-capability skipping is a footgun.** The `test_phase5_regression_no_skips.py` baseline check is one structural defense; the CI-workflow-level aggregator (Preconditions §10 + AC-E bullet 3) is the *other*. Both are required. If the CI runner pool changes (e.g., the `--privileged` Linux runner pool is reduced), this story may need to re-skip-strategy — flag in the attempt log. A green build with every matrix entry skipping the integration test is the failure mode that defeats the entire integration story; both defenses exist because either one alone can be evaded.
- **The normalization helper reuse is a Rule-3 obligation, not a preference.** Preconditions §4 spells it out. S6-02 owns the `_normalize_run_for_byte_identity` function and its `Final[tuple[str, ...]]` allow-list; S6-03 imports and uses. A stealth divergence — S6-03's local re-declaration adds `microvm_seconds` to its normalization list, S6-02's does not — silently masks default-path behavioral drift on any spec that hits the runtime path but not the unit path. The `test_normalization_helper_is_shared` companion test is the enforcement; if S6-02 shipped the helper at a non-shared location, that's an S6-02 bug and the fix is a `[promote]` commit on S6-02, not a local re-declaration here.
- **`gate_isolation_class` is the Phase 5 contract surface that propagates to Phase 11.** Phase 5 §Goals #4 says the merge gate refuses to auto-promote `shared_kernel` verdicts. This story must verify that `Role.PROBE` produces the same `gate_isolation_class` value as `Role.GATE` on the same backend (Preconditions §6 same-backend guard) — drift here would silently change Phase 11's merge-promotion eligibility.
- **The fallback (ADR-0003 §Reversibility): if Phase 5 rejects the amendment, route `Role.PROBE` through `Role.GATE` semantically.** This fallback is **not exercised by these tests**. If the fallback is invoked, AC-A's `test_probe_audit_event_role_tag_universal` becomes the *only* observable distinction, and AC-A's topology-identity test must be re-examined for what "identical" means under the fallback (there is no `Role.PROBE` code path at all). Surface in `_attempts/S6-03.md` if the fallback path is in play.
- **Reading order before writing tests:**
  1. [`../ADRs/0002-shell-invocation-trace-probe-runs-in-microvm.md §Consequences`](../ADRs/0002-shell-invocation-trace-probe-runs-in-microvm.md) — but note (Preconditions §5) the `confidence`/`reason` mentions describe probe-level output, not `SandboxRun`.
  2. [`../ADRs/0003-sandbox-role-additive-enum-on-spawn.md §Consequences`](../ADRs/0003-sandbox-role-additive-enum-on-spawn.md) — additive audit-log field, every existing callsite unchanged.
  3. [Phase 5 `final-design.md §Components §1 SandboxClient` and §Components §2 SandboxSpec/SandboxRun and §Failure modes](../../05-sandbox-trust-gates/final-design.md) — **this is the source of truth for Phase-5-surface assertions**; Phase 7's ADRs describe intent, Phase 5's design describes the shipped surface.
  4. Phase 5's `tests/integration/test_sandbox_*.py` — fixture names, marker conventions, runner-capability detection (Preconditions §3).
  5. `src/codegenie/sandbox/client.py` post-S6-02 — actual method shape and audit-event class name (Preconditions §2 + §8).
- **No `pytest.skip("not implemented")` placeholders.** Either the test passes or it fails for a named reason. ADR-0002 §Tradeoffs row 4 warns against structural-only fences; this story is the runtime complement and "placeholder green" defeats its purpose (Rule 12).
- **Coordinate with S7-02 timing.** S7-02 (`ShellInvocationTraceProbe`) is the *only* production caller of `spawn(role=Role.PROBE)` in Phase 7. If this story's tests pin a behavior S7-02 then has to work around, that's a coordination defect — fix here, not in the probe. The arch design and ADR-0002 §Decision are the canonical contract; both stories implement against it, not against each other.
- **Extension-by-addition nudge for future roles (Rule 2 — not required today).** ADR-0003 §Consequences row 8 says `Role.RECIPE` / `Role.AUDIT` land in future phases by the same additive-enum-value shape. When the second non-gate role arrives, the topology-identity assertion is a natural `@pytest.mark.parametrize("non_gate_role", [...])` fixture keyed off a `_TOPOLOGY_EQUIVALENT_ROLES: Final[tuple[SandboxRole, ...]]` tuple at module scope. Today with one non-gate role (`PROBE`), the parametrize saves nothing — Rule 2 (Simplicity First) says don't introduce the abstraction until the second consumer arrives. But the *name* and *location* of the tuple are cheap to pick today: land the topology-identity assertion in a way that a future one-row edit (`_TOPOLOGY_EQUIVALENT_ROLES = (SandboxRole.PROBE, SandboxRole.RECIPE)`) is Open/Closed for the test file. Do NOT ship the tuple + parametrize scaffold in this story if there's still only one member; introducing empty extension seams is a Rule-2 violation on the other side.
