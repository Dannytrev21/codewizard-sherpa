# Story S5-01 — Phase 7 byte-edit allowlist fence (ADR-0009 + amendments enumerated rows)

**Step:** Step 5 — Phase 7 byte-edit allowlist fence + import-linter contracts + `PLUGINS.lock`
**Status:** HARDENED (phase-story-validator, 2026-08-14 — pre-executor pass; see `_validation/S5-01-phase7-byte-edit-allowlist-fence.md`)
**Effort:** M
**Depends on:** S3-03 (npm adapter wiring lands the first Phase-7 byte-edit to a Phase-3 plugin file — currently `BLOCKED`, see Preconditions), S4-04 (SBOM tampering property + AST fence — the last Phase-7 plugin file added before this story closes; `HARDENED`)
**ADRs honored:** Phase 7 ADR-0009 (the ORIGINAL 10-row byte-edit allowlist — verbatim source of truth for rows 1–10); **Phase 7 ADR-0029 (Amendment A: adds 8 additional row-categories for the distroless-migration gather pipeline — landed row-by-row alongside their owning Amendment-A stories, Steps 13–18)**; **Phase 7 ADR-0030 (Layer C sidecar-publishing amendment: 8 Layer C/G kernel probe files — see `## Preconditions` on whether they land as Phase-7 fence rows at S5-01 landing time or are already pre-baseline)**; Phase 7 ADR-0001 (mechanical-additivity discipline keeps `MultiPluginCoordinator` out of Phase 7 by stopping byte-edits at the kernel boundary); Phase 7 ADR-0005 (probes live under the plugin — this fence is the structural enforcer); Phase 7 ADR-0007 (registry stores classes — primitive surface is closed by the fence + import-linter pair); production ADR-0031 (plugin architecture); Phase 3 ADR-0011 (honest framing — audit + lint posture, not runtime guarantee; CODEOWNERS on `tests/fence/` is the social anchor); production ADR-0043 (extension by addition = no *silent* edits — Phase 7's allowlist is the **terminal** per-phase enumerated allowlist *file*; no future phase adds a `_PHASE8_BYTE_EDIT_ALLOWLIST` symbol or a second per-phase fence file. Row **growth** within this file by ADR amendment — ADR-0029, ADR-0030 — remains sanctioned because each row cites an ADR and the audit trail stays intact).

## Preconditions

Read before starting:

1. **S3-03 is `BLOCKED` as of 2026-05-23** (see `docs/phases/07-migration-task-class/stories/S3-03-npm-adapter-plugin-wiring.md`). Rows 1 & 2 of the ADR-0009 allowlist reference files inside `plugins/vulnerability-remediation--node--npm/` that do not yet exist. **This does not block S5-01 mechanically** — the `_LOCKED_SURFACE_GLOBS` glob `plugins/vulnerability-remediation--node--npm/**` simply matches zero files today; AC-9's live check passes trivially; the planted-violation matrix (AC-5) still exercises the mechanism via dependency-injected diffs. Rows 1 & 2 land as *reserved* allowlist rows — enumerated in code and gated by ADR-0009 — so the fence is ready the moment S3-03 unblocks. Document this posture in the fence file's module docstring.
2. **Phase 6.5 is not fully shipped** — S2-05 is `BLOCKED`; several stories are `HARDENED` but not `Done`. There is no `phase-6.5-final` tag. AC-3 pins the baseline SHA to *the most recent commit on `main` authored by a Phase-6.5 story that reached `HARDENED` or `Done` acceptance*. Record the SHA + the story-id in a comment header inside `tests/fence/_phase6_5_baseline.txt` (mirror the sidecar comment in `_phase3_baseline.txt`). If Phase 6.5 later ships additional stories that edit kernel files, rotating the baseline is a one-line commit + ADR-amendment note; not a Phase 7 concern until it happens.
3. **ADR-0030 forward reference — coordinate with S19-01 landing state.** ADR-0030 §Consequences (2026-05-21) explicitly says: *"Whoever implements S5-01 must consult this ADR and enumerate the eight [Layer C/G] files; this ADR is cross-linked from S5-01's story for that reason."* Two possible states at S5-01 landing time — the implementer picks based on what `git log` shows:
   - **State A** (S19-01 landed BEFORE the chosen Phase-6.5 baseline SHA): the 8 files are already at their post-S19-01 state at baseline. They need **no** row in `_PHASE7_BYTE_EDIT_ALLOWLIST`; document the fact + baseline SHA relation in the fence file's module docstring so a future reader understands why ADR-0030 is *cited* but not *enumerated*.
   - **State B** (S19-01 landed AFTER the chosen Phase-6.5 baseline SHA): the 8 files ARE diffs against baseline; enumerate them as row-block **R30** (rows 11–18) with a `# ADR-0030 §Decision Part 2` inline block comment. Under State B, AC-2.a's row-count target is 18 (10 + 8), not 10.
4. **ADR-0029 (Amendment A) row-growth is out-of-scope for S5-01's landing** — those rows land alongside their owning stories (Steps 13–18). AC-2 must be written so Amendment-A stories can extend the allowlist by ADR amendment without editing S5-01's landing invariants (see AC-2 rewrite below).

## Validation notes (phase-story-validator, 2026-08-14)

Hardened by the four-critic phase-story-validator pipeline; full audit in `_validation/S5-01-phase7-byte-edit-allowlist-fence.md`. Summary of edits:

1. **Front-matter + Preconditions** — Added ADR-0029 (Amendment-A row-growth, 2026-05-20) and ADR-0030 (Layer C sidecar-publishing amendment, 2026-05-21) to ADRs-honored. Added a `## Preconditions` section explicitly resolving S3-03 BLOCKED status (rows 1–2 are reserved rows), Phase-6.5 not-fully-shipped baseline choice, and the State-A-vs-State-B decision the implementer must make against ADR-0030 §Consequences.
2. **AC-2 rewrite (blocker-fix)** — Replaced the rigid `len == 10` + "byte-identical to ADR-0009" invariants with an elastic `_EXPECTED_ROW_COUNT` + `_ROW_SOURCE_ADRS` machinery so ADR amendments (0029, 0030) grow the allowlist without breaking S5-01's landing invariants. Added AC-2.c to catch per-row `# row N` comment drift (paths could stay set-equal while row-number comments swap silently). AC-2.b's ADR parser now scoped to `## Decision` heading with a meta-test guarding against decoy numbered lists.
3. **AC-3 hardening** — Added AC-3.c "reachable from origin/main" to close the "baseline pinned to WIP-on-feature-branch" failure mode. Added baseline-sidecar comment-header requirement per Preconditions §2.
4. **AC-4 hardening** — Path-based (not status-based) scope check per precedent; ALL git-diff statuses in scope (`M`/`D`/`R`/`A`/`C`/`T`/`U`). Added AC-4.a explicitly EXCLUDING `plugins/PLUGINS.lock` from `_LOCKED_SURFACE_GLOBS` (S5-04 owns it — resolves the previous dangling "see Notes" reference). Added AC-4.b pinning glob-matching to `fnmatch.fnmatchcase` on POSIX-normalised paths (prevents `PurePath.match`'s cross-version `**` semantics bugs). Added AC-4.c documenting submodule pointer-change behavior.
5. **AC-5 hardening** — Added AC-5.j (typechange + copy status coverage), AC-5.k (nested-depth coverage), AC-5.l (multi-violation error message), AC-5.m (live-check ↔ pure-helper AST-coupling check). Split the single owned-new-trees positive case (AC-5.h) into three discriminating cases (h.i / h.ii / h.iii) — the original single case did NOT exercise the owned-new-trees seam because its path was already outside `_LOCKED_SURFACE_GLOBS`. Added AC-5.i schema (RED/REMOVAL/GREEN 40-hex lines) + AC-5.i.evidence-check runtime assertion.
6. **AC-6 hardening** — Multi-violation coverage; explicit ADR-0030 string requirement under State B.
7. **AC-11 rewrite** — Replaced the previous comment-only meta-assertion (drifted from "ACs are individually verifiable" bar) with five property-based invariants: allowlist-honored, locked-surface-caught, monotonicity, idempotency, and a docstring-scan for the "necessary-not-sufficient" language.
8. **AC-12 hardening** — Complemented the filename glob (AC-12.a) with an AST-based symbol scan (AC-12.b) that catches copycat files under different names.
9. **Design-pattern edits (Implementation outline + Notes)** — Made explicit: `DiffStatus = Literal[...]` closes primitive obsession on status letters; `Violation` dataclass frozen with `reason: Literal[...]` closes anaemic-dataclass smell; the `_run_git_diff` + `_ensure_baseline_reachable` reuse mechanism is copy-paste-with-`# duplicate-of:`-comment (Rule 2 + ADR-0043 says no third consumer will ever exist); glob shape divergence from `_KERNEL_SCOPE_DIRS` is deliberate and documented; the "terminal allowlist" nuance (no new files/symbols, but rows may grow via ADR amendment) is spelled out to avoid two competing readings of ADR-0043.
10. **AC-8 grammar fix** — Rewrote the previous self-contradicting "amended … no not touched" wording into a clean two-clause assertion.
11. **Consistency fixes** — Documented Phase 4/5/6 baseline-skip rationale; corrected the prior Notes claim that "one walks a baseline diff; the other walks an AST" (both walk baselines; the AST walker is `_phase3_fence.py`, unrelated).

**Verdict:** HARDENED — no structural block, all findings translated into concrete AC/Notes edits. Ready for phase-story-executor.

## Context

CLAUDE.md and production design §2.5 commit to **"Extension by addition — new language / new task type = new probes + new Skills, never edits to existing probes or the coordinator."** Across Phase 7's three lens designs, the critic (`critique.md §Roadmap-level critiques §4`) caught the commitment decaying into "byte-edits to existing files are fine because the file grows rather than mutates." Without a mechanical definition of "additive," every future phase silently widens the kernel and we land in Ship-of-Theseus territory — by Phase 10 the coordinator looks nothing like its Phase-0 self and no single phase admits to having changed it.

Phase 7 ADR-0009 takes a position: define "additive" mechanically. Enumerate every byte-edit to a Phase 0–6.5 file Phase 7 is permitted to make. Anything else fails CI before merge. The ADR lists exactly **10 rows** (see Acceptance criteria AC-2); this story lands the fence test that enforces them.

This is the **load-bearing test of Phase 7's headline claim.** If S5-01 ships with a hole (a forgotten row, a regex too loose, a baseline mis-pinned), the entire "extension by addition" framing of Phase 7 — and every later phase that extends this fence — is unprotected. False negatives here invalidate the headline.

The order is deliberate: Steps 3 + 4 land first (the npm adapter wiring and the new Phase-7 plugin tree), then this story lands the fence with rows for the edits Steps 3 + 4 already shipped plus rows reserved for Steps 6–11 still to come. The reserved rows are enumerated in ADR-0009 so the allowlist is the same whether you read it before or after the corresponding step.

**Honest framing (Phase 3 ADR-0011 carry-forward):** this is an audit-and-lint fence, not a runtime guarantee. A PR that edits both the fence file and a violation defeats it; CODEOWNERS on `tests/fence/` + the Phase-6.5 baseline file is the social anchor. The module docstring states this verbatim.

## References — where to look

- **Phase ADR — primary source of truth:**
  - `../ADRs/0009-phase-7-byte-edit-allowlist-fence.md` §Decision — **the 10 enumerated rows below come from here verbatim**. Read cover-to-cover; embed the row text in the fence file's module docstring.
- **Cross-cutting ADRs each row anchors to:**
  - Row 1 (`npm_provenance.py`) — Phase 7 ADR-0004 (primitive home) + Phase 3 ADR-0009 (registry of recipes — same Plugin/Registry shape).
  - Row 2 (`vulnerability-remediation--node--npm/tccm.yaml`) — Phase 7 ADR-0016 (`derived_queries:` band).
  - Row 3 (`src/codegenie/__init__.py`) — Phase 7 ADR-0004 (primitive home).
  - Row 4 (`repo_context.schema.json`) — Phase 7 ADR-0005 (probes under plugin — sub-schemas reach the envelope via `$ref`).
  - Row 5 (`tccm.py`) — Phase 7 ADR-0016.
  - Row 6 (`sandbox/client.py`) — Phase 7 ADR-0003 (additive `role:` enum).
  - Row 7 (`sandbox/__init__.py`) — Phase 7 ADR-0003.
  - Row 8 (`exec/__init__.py`) — Phase 7 ADR-0015 (`ALLOWED_BINARIES` amendment).
  - Row 9 (`pyproject.toml`) — Phase 7 ADR-0013 (`dockerfile-parse` runtime dep).
  - Row 10 (`plugins/loader.py`) — Phase 7 ADR-0005 (plugin's explicit-import).
- **Architecture:**
  - `../phase-arch-design.md §Testing strategy §"Fence / structural"` and §"Phase 7 fence allowlist (exhaustive)" — names the fence file and its row set.
  - `../phase-arch-design.md §Anti-patterns avoided` — calls out semantic-additivity drift as the named pattern this fence kills.
- **High-level-impl:**
  - `../High-level-impl.md §Step 5` — features delivered, exit criteria, risks (especially Risk #3: "byte-edit allowlist fence (Step 5) lands after Steps 3 + 4 have already edited Phase 3 plugin files").
- **Precedent fence (read first):**
  - `tests/fence/test_kernel_frozen.py` — the Phase 3 kernel-frozen fence. Same shape (git-diff against pinned baseline; per-phase `_BASELINES` tuple; CODEOWNERS social anchor; `_KERNEL_ALLOWLIST: Final[frozenset[Path]]` with `# adr:` inline tags). **Reuse the scanner; don't reimplement.** Add `("phase-6_5", Path("tests/fence/_phase6_5_baseline.txt"))` as a one-row append.
  - `tests/fence/_phase2_baseline.txt` — pinned baseline SHA format (40-char lowercase hex; one line). Mirror this shape for `_phase6_5_baseline.txt`.
  - `src/codegenie/_phase3_fence.py` — companion walker pattern. Phase 7's row enforcement does NOT need an AST walker (it's purely git-diff + path-set), but the test layout and `Violation` dataclass discipline mirror it (`Final` tuples, no primitive obsession).
- **Phase 6.5 baseline reference:** identify the last merged Phase 6.5 commit on `main` (likely tagged `phase-6.5-final` or referenced in `docs/phases/06.5-*/High-level-impl.md`); the SHA goes into `tests/fence/_phase6_5_baseline.txt` exactly as `_phase2_baseline.txt` carries Phase 2's.

## Goal

Land `tests/fence/test_phase7_no_byte_edits_to_locked_files.py` so any PR after S5-01 that byte-edits a Phase 0–6.5 file outside the 10 enumerated rows from Phase 7 ADR-0009 fails CI before merge. The fence is git-diff-based, mechanical, and CODEOWNERS-anchored; the 10 rows from ADR-0009 §Decision live in a `Final[frozenset[Path]]` with one inline `# row:` comment per row naming the row number + the owning ADR.

## Acceptance criteria

**Test layout (AC-1)**
- [ ] **AC-1** `tests/fence/test_phase7_no_byte_edits_to_locked_files.py` exists; module docstring (a) cites Phase 7 ADR-0009, (b) embeds the honest-framing language from Phase 3 ADR-0011 ("audit + lint, NOT runtime guarantee — CODEOWNERS on `tests/fence/` + the Phase-6.5 baseline file is the social anchor"), (c) names the CI `fetch-depth: 0` requirement (mirror `tests/fence/test_kernel_frozen.py` line 9–16), (d) lists ADR-0009's 10 rows verbatim. A meta-test scans the docstring for the literal strings `"ADR-0009"`, `"audit + lint"`, `"CODEOWNERS"`, `"fetch-depth"`, `"10 enumerated rows"`.

**The enumerated rows (AC-2) — sourced from ADR-0009, plus amendments (ADR-0030, ADR-0029) as they land**

The allowlist is an ADR-referenced *data* set, not a fixed count. **At S5-01 landing time** it enumerates ADR-0009's 10 original rows, plus — under State B (see Preconditions §3) — ADR-0030's 8-file R30 block. ADR-0029's Amendment-A rows land alongside their owning stories (Steps 13–18) and grow the set by ADR amendment; the AC-2 machinery below is written so that growth is a one-line-per-row edit gated by ADR review, not a story-invariant break.

- [ ] **AC-2 — the 10 ADR-0009 rows (mandatory at landing)** `_PHASE7_BYTE_EDIT_ALLOWLIST` contains these 10 paths; each carries a `# row N — ADR-0009` inline comment matching ADR-0009's enumeration:
  1. `plugins/vulnerability-remediation--node--npm/adapters/npm_provenance.py` — new file under Phase 3 plugin (S3-02 / S3-03). **Reserved row** — file does not exist until S3-03 unblocks (see Preconditions §1).
  2. `plugins/vulnerability-remediation--node--npm/tccm.yaml` — one new `derived_queries:` block (S3-03). **Reserved row**.
  3. `src/codegenie/__init__.py` — one new `import` line for the `vuln_provenance` primitive (Step 1)
  4. `src/codegenie/schema/repo_context.schema.json` — at least two `$ref` insertions at S5-01 landing (one per Phase-7 gather probe from S7-03); Amendment-A ADR-0029 §Decision #3 additively grows the count. AC-2.b's parser reads the `$ref` count from ADR-0009 §Decision AND ADR-0029 §Decision #3; drift fails CI. **Do not** pin `count == 2` in an AC — pin the file's presence in the allowlist and let ADR-parser drive the count.
  5. `src/codegenie/plugins/tccm.py` — one new optional band `derived_queries: list[DerivedQuery] = []` (S8-02)
  6. `src/codegenie/sandbox/client.py` — one new `role: SandboxRole = Role.GATE` parameter on `spawn(...)` (S6-02)
  7. `src/codegenie/sandbox/__init__.py` — one new export `Role` (S6-01)
  8. `src/codegenie/exec/__init__.py` — `ALLOWED_BINARIES` gains `dive` + `docker buildx` (S7-04). Amendment-A ADR-0028 additively grows this with `crane`; AC-2.b's parser reads BOTH ADRs.
  9. `pyproject.toml` — one new runtime dep `dockerfile-parse` (S7-04)
  10. `src/codegenie/plugins/loader.py` — one new explicit-import line for the new plugin (S8-03)
- [ ] **AC-2 — the ADR-0030 R30 block (mandatory under State B, see Preconditions §3)** — enumerate the 8 Layer C/G files as a grouped block with an `# ADR-0030 §Decision Part 2 — R30 block` header comment. Under State A (S19-01 pre-baseline) this block is **absent** from `_PHASE7_BYTE_EDIT_ALLOWLIST` and the fence file's docstring documents *why* (with the baseline SHA and S19-01 landing SHA). Either posture is a valid AC-2 pass; the executor's `_attempts/S5-01.md` MUST record which state was chosen and cite the SHAs used to justify the choice.
- [ ] **AC-2.a — row-count invariant (elastic under ADR amendment)** A unit test `test_allowlist_row_count_matches_adrs` asserts `len(_PHASE7_BYTE_EDIT_ALLOWLIST) == _EXPECTED_ROW_COUNT` where `_EXPECTED_ROW_COUNT: Final[int]` is a module-level constant equal to `10 + (8 if State B else 0) + <ADR-0029 rows landed so far>`. The constant is updated *only* by an ADR amendment (that lands the corresponding row-block). Adding a row without bumping the constant OR bumping the constant without landing the row both fail this test. Rationale: prevents silent row addition/removal while remaining Amendment-A-compatible.
- [ ] **AC-2.b — code ↔ ADR set-equality with strict scoping** `test_every_allowlist_row_matches_referenced_adrs` parses the `## Decision` section of each ADR in a module-level `_ROW_SOURCE_ADRS: Final[tuple[Path, ...]]` (initially `(ADR-0009, ADR-0030)` under State B, `(ADR-0009,)` under State A; grows by adding Amendment-A ADR pointers). The parser (a) locates `## Decision` via markdown heading match, (b) walks until the next `##` heading, and (c) parses only numbered list items inside that slice — a decoy numbered list under `## Consequences` MUST be ignored. Asserts the union of parsed paths equals `_PHASE7_BYTE_EDIT_ALLOWLIST` (byte-identical `Path` set) AND `sum(parsed_row_counts) == _EXPECTED_ROW_COUNT`. **Meta-test:** `test_adr_parser_ignores_decoy_numbered_lists` uses a synthetic in-memory markdown fixture with a numbered list under `## Decision` and a *different* numbered path list under `## Consequences`; the parser MUST return only the `## Decision` set. Belt-and-suspenders against a permissive regex.
- [ ] **AC-2.c — per-row `# row N` comment integrity** `test_row_comments_match_adr_row_numbers` uses `ast.parse` + `tokenize` on the fence module: for each entry in `_PHASE7_BYTE_EDIT_ALLOWLIST`, the trailing `# row N — ADR-XXXX` comment must (a) name a row-number that appears in the cited ADR's `## Decision` numbered list, and (b) map to the same path the ADR lists at that number. Prevents comment-swap drift ("row 3" swapped with "row 4" while paths stay set-equal) that plain set-equality would miss. Alternatively: replace `frozenset[Path]` with `Final[tuple[tuple[int, str, Path], ...]]` (row-number + ADR-id + path) — pick whichever the implementer finds more legible; the AC is that comment-drift is caught.

**Phase 6.5 baseline (AC-3)**
- [ ] **AC-3** `tests/fence/_phase6_5_baseline.txt` exists and contains exactly one line of 40-char lowercase-hex SHA. Preceded (or accompanied by a sidecar file — mirror `_phase3_baseline.txt`'s convention) with a comment header naming the story-id whose acceptance authored the chosen commit (per Preconditions §2). `tests/fence/test_kernel_frozen.py::_BASELINES` is amended to `(("phase-2", ...), ("phase-3", ...), ("phase-6_5", Path("tests/fence/_phase6_5_baseline.txt")))` (one-row append — Open/Closed at the file boundary). Note: Phase 4/5/6 do NOT carry baseline sidecars because their kernel edits landed only via `_KERNEL_ALLOWLIST` extensions covered by the existing Phase-2/Phase-3 baselines; document this in the fence file's docstring so a future reader understands the leap from `_phase3_baseline.txt` to `_phase6_5_baseline.txt`.
- [ ] **AC-3.a** `test_phase6_5_baseline_is_a_real_commit_sha` asserts `re.fullmatch(r"[0-9a-f]{40}", baseline.strip())`.
- [ ] **AC-3.b** `test_phase6_5_baseline_resolves_to_ancestor_of_head_and_is_not_head` asserts `git merge-base --is-ancestor <baseline> HEAD` exits 0 AND `baseline != HEAD_SHA` (accidental `HEAD` paste rejected — mirror `test_kernel_frozen.py::test_baseline_resolves_to_ancestor_of_head_and_is_not_head`).
- [ ] **AC-3.c** `test_phase6_5_baseline_is_reachable_from_origin_main` asserts `git merge-base --is-ancestor <baseline> origin/main` exits 0 — closes the "baseline pinned to a stale WIP commit on a feature branch that happens to be an ancestor of HEAD" failure mode the precedent leaves open. If `origin/main` is not present locally (rare — allowed for offline development), the test emits an explicit `pytest.skip` with a message naming *why* it skipped (must include the literal string `"origin/main not available"`); silently passing is forbidden.
- [ ] **AC-3.d** `CODEOWNERS` covers `tests/fence/_phase6_5_baseline.txt` (add the path under the existing `tests/fence/` rule if not already covered transitively).

**Diff-scope coverage (AC-4)**
- [ ] **AC-4** The fence computes `git diff --name-status -M <baseline>..HEAD` (rename detection on; ALL git-diff status letters — `M`/`D`/`R`/`A`/`C`/`T`/`U` — are in scope; the scope check is path-based, not status-based, mirroring `test_kernel_frozen.py::test_diff_status_classification`) and filters to **paths matching the Phase 0–6.5 locked surface**:
  - `src/codegenie/**` (every file under the core tree)
  - `plugins/vulnerability-remediation--node--npm/**` (Phase 3 plugin)
  - `pyproject.toml`
  - `Makefile`
  - `src/codegenie/schema/repo_context.schema.json` (already included by the `src/codegenie/**` prefix but listed explicitly for reader clarity)
  - Every changed file in this filtered set must be in `_PHASE7_BYTE_EDIT_ALLOWLIST`, OR match a `_PHASE7_OWNED_NEW_TREES` glob (currently `plugins/distroless-migration--node--npm/**` and `src/codegenie/primitives/vuln_provenance/**` — these are Phase-7-owned new trees and are unconstrained by this fence).
  - Any other path under the locked surface = fence failure with the file path named in the error message.
- [ ] **AC-4.a — `plugins/PLUGINS.lock` is deliberately EXCLUDED from `_LOCKED_SURFACE_GLOBS`.** Its integrity is governed by S5-04's dedicated hash-verification fence, not by byte-diff — double-coverage would produce duplicate failures for the same drift and make one of the two fences load-bearing-in-name-only. Verified by `test_plugins_lock_is_out_of_scope_of_this_fence` (dependency-injected diff `[("M", Path("plugins/PLUGINS.lock"))]` → fence passes). Document this scoping choice in the fence file's module docstring so a future reader does not "helpfully" add `plugins/PLUGINS.lock` to `_LOCKED_SURFACE_GLOBS`.
- [ ] **AC-4.b — glob matching semantics are pinned to `fnmatch.fnmatchcase` on POSIX-normalised paths.** `pathlib.PurePath.match` does NOT interpret `**` as recursive globs across Python versions and would silently break the `src/codegenie/**` prefix at depth ≥ 2. The docstring MUST cite this choice. Verified by a parametrized test with paths at depths 1, 2, 3, and 4 (e.g., `src/codegenie/__init__.py`, `src/codegenie/probes/base.py`, `src/codegenie/probes/layer_c/dockerfile.py`, `src/codegenie/probes/layer_b/foo/bar/baz.py`) — every path MUST classify as "in locked surface". A path outside the prefix (`tests/fence/foo.py`, `docs/anything.md`) MUST classify as "not in locked surface".
- [ ] **AC-4.c — submodule pointer changes are explicitly documented.** The fence does NOT set `--ignore-submodules`; a submodule gitlink change under `src/codegenie/` would appear as a `M` status. There are no submodules in this repo today; the docstring states this and states the behavior if one is later added ("a submodule pointer change under the locked surface is treated as a byte-edit and must be allowlisted or reverted"). No runtime test — this is a docstring-only invariant.

**Planted-violation evidence (AC-5) — Rule 12 fail-loud, the load-bearing assertion**
- [ ] **AC-5** Parametrized in-test planted-violation cases that exercise the fence under controlled conditions (red-by-construction every CI run; injects synthetic diff via dependency-injection on the diff source — mirrors `tests/fence/test_kernel_frozen.py::test_helpful_error_message`):
  - **AC-5.a** Plant a synthetic `M` edit to `src/codegenie/coordinator/coordinator.py` → fence fails with an error message containing the literal path `"src/codegenie/coordinator/coordinator.py"` AND the strings `"ADR-0009"` AND `"byte-edit allowlist"`.
  - **AC-5.b** Plant a synthetic `M` edit to `src/codegenie/probes/__init__.py` → fence fails (proves "while I'm here" formatting changes are caught).
  - **AC-5.c** Plant a synthetic `M` edit to `plugins/vulnerability-remediation--node--npm/api.py` (Phase 3 plugin file outside the allowlist — `api.py` is NOT row 1; only `adapters/npm_provenance.py` is) → fence fails.
  - **AC-5.d** Plant a synthetic `M` edit to `src/codegenie/output/sanitizer.py` → fence fails.
  - **AC-5.e** Plant a synthetic **rename** `src/codegenie/types/identifiers.py → src/codegenie/types/identifiers_renamed.py` (`R` status) → fence fails (rename detection works).
  - **AC-5.f** Plant a synthetic **delete** of `src/codegenie/exec/run_external_cli.py` (`D` status) → fence fails.
  - **AC-5.j — status coverage** Plant synthetic `T` (typechange — regular file ↔ symlink) and `C` (copy) diff entries on `src/codegenie/coordinator/coordinator.py` → both fail. Prevents an implementation that filters `status in {"M","D","R","A"}` and silently allows a symlink swap or copy that the fence exists to catch.
  - **AC-5.k — depth coverage** Plant synthetic `M` edits at multiple depths (`src/codegenie/probes/layer_c/dockerfile.py`, `src/codegenie/probes/layer_b/foo/bar/baz.py`) → all fail. Complements AC-4.b — proves the `**` glob semantics chosen actually reach nested paths.
  - **AC-5.l — multi-violation error message** Plant a synthetic diff containing TWO in-scope violations at once (`("M", "src/codegenie/coordinator/coordinator.py")` + `("M", "src/codegenie/output/sanitizer.py")`) → fence fails AND the error message contains BOTH literal paths on separate lines. Prevents an implementation that reports only `violations[0]` and truncates the rest.
- [ ] **AC-5.g** A complementary positive case: plant a synthetic edit to `src/codegenie/sandbox/client.py` (row 6) → fence **passes** (proves the allowlist is honored, not just that the fence is always-red).
- [ ] **AC-5.h — allowlist vs owned-new-tree discrimination** Two complementary positive cases that separately exercise each escape hatch (the original single case did NOT discriminate — `plugins/distroless-migration--node--npm/**` is also outside `_LOCKED_SURFACE_GLOBS`, so it escaped via the locked-surface filter and never touched the owned-new-trees seam):
  - **AC-5.h.i** Plant a synthetic ADD of a new file under `plugins/distroless-migration--node--npm/probes/some_new.py` → fence passes (escapes via locked-surface filter). Original case; retained for regression.
  - **AC-5.h.ii** Plant a synthetic ADD of a new file under `src/codegenie/primitives/vuln_provenance/new_thing.py` → fence passes. This path IS inside `src/codegenie/**` (locked surface) and MUST escape via `_PHASE7_OWNED_NEW_TREES`. If `_PHASE7_OWNED_NEW_TREES = ()` (empty) this test fails — proves the owned-new-trees seam is load-bearing.
  - **AC-5.h.iii** Plant a synthetic ADD at depth ≥ 3 (`src/codegenie/primitives/vuln_provenance/nested/deep/foo.py`) → fence passes. Nails the `**`-depth semantics for owned-new-trees, mirroring AC-4.b's depth check for locked surface.
- [ ] **AC-5.i** **Out-of-test planted-violation evidence:** on a throwaway branch, commit a real edit to `src/codegenie/probes/__init__.py` (e.g., add a trailing comment), run `pytest tests/fence/test_phase7_no_byte_edits_to_locked_files.py`, capture the red output (commit SHAs + failure message) into `_attempts/S5-01.md`; remove the edit, run again, capture green. **Evidence-block format (schema):** exactly three lines, each starting with the literal prefix `RED:`, `REMOVAL:`, `GREEN:` followed by a single space and a 40-char lowercase-hex SHA. Example:
  ```
  RED: 0123456789abcdef0123456789abcdef01234567
  REMOVAL: fedcba9876543210fedcba9876543210fedcba98
  GREEN: 89abcdef0123456789abcdef01234567012345aa
  ```
- [ ] **AC-5.i.evidence-check** `test_attempt_log_has_planted_violation_evidence` opens `docs/phases/07-migration-task-class/stories/_attempts/S5-01-phase7-byte-edit-allowlist-fence.md` and asserts a `re.search` for the three literal-prefix lines above (each followed by a 40-hex SHA on the same line) succeeds. Skipped-with-explicit-reason on branches where the attempt log does not yet exist (first executor pass; message must include the literal `"attempt log not present"`) — but on any pass after the fence file exists, missing evidence = fail.
- [ ] **AC-5.m — live-check ↔ pure-helper coupling** `test_live_check_delegates_to_pure_helper` uses `ast.parse` on the test module: `test_no_byte_edits_outside_allowlist`'s function body MUST contain a `Call` node targeting the `_compute_phase7_violations` helper (the pure function AC-5's DI-injected tests also call). Prevents drift where the live check is inlined and grows a subtle bug that the DI'd tests, still calling the pure helper, cannot see.

**Helpful-error-message guard (AC-6) — Rule 12**
- [ ] **AC-6** When the fence fails, the error message contains:
  - The path of EVERY disallowed change (one per line — verified explicitly by AC-5.l's two-violation case, not just the single-violation cases).
  - The literal string `"Phase 7 ADR-0009"` (with markdown link to `docs/phases/07-migration-task-class/ADRs/0009-phase-7-byte-edit-allowlist-fence.md`).
  - When operating under State B, the literal string `"Phase 7 ADR-0030"` (with markdown link) — enumerates the R30 block source. Under State A this string is absent.
  - The phrase `"ADR amendment required"` (mirrors `tests/fence/test_kernel_frozen.py`'s "ADR amendment" guard).
  - The exact allowlist text (row-by-row) so the engineer hitting the fence sees what's allowed without leaving the failure output. Length grows with amendments; wrap for readability.
  - Verified by `test_failure_message_is_helpful` (single violation) AND AC-5.l's two-violation case.

**Determinism + CI requirement (AC-7)**
- [ ] **AC-7** Module docstring + `_attempts/S5-01.md` Notes name the **CI `fetch-depth: 0`** requirement (mirror `tests/fence/test_kernel_frozen.py` line 9–16). If `_ensure_baseline_reachable` cannot resolve the SHA, the fence surfaces a clear error naming the missing SHA + the recovery command (`git fetch --unshallow`). A unit test parametrizes a shallow-clone fixture and asserts the recovery message contains the literal `"fetch-depth"` AND `"unshallow"`.

**Wiring (AC-8 through AC-10)**
- [ ] **AC-8** `make fence` exits 0. `Makefile` is NOT touched by this story: (a) `Makefile` is not in `_PHASE7_BYTE_EDIT_ALLOWLIST` (any edit would fail this fence), and (b) the existing `make fence` recipe (established in S1-05's AC-3) already runs `pytest tests/fence/` transitively — the new test file is picked up without any Makefile change.
- [ ] **AC-9** `pytest tests/fence/test_phase7_no_byte_edits_to_locked_files.py -v` exits 0 on the current diff at story landing time. Note: while S3-03 is `BLOCKED`, the glob `plugins/vulnerability-remediation--node--npm/**` matches zero files and AC-9 passes trivially on those rows; the planted-violation matrix (AC-5) is what proves the mechanism works. When S3-03 unblocks, AC-9 becomes semantically load-bearing on rows 1 & 2.
- [ ] **AC-10** `ruff check`, `ruff format --check`, `mypy --strict tests/fence/test_phase7_no_byte_edits_to_locked_files.py` clean.

**Property tests (AC-11) — hidden-invariant coverage**

Replaces the previous comment-only AC-11 (which named a limitation but asserted no invariant — drifted from the "ACs are individually verifiable" bar). The Phase 3–6.5 regression suite + `bench/vuln-remediation/` cassette replay remain the semantic-drift complement; that framing moves to the module docstring and to a hardening note below.

- [ ] **AC-11.a — allowlist-honored property** For any diff whose every entry is either (i) a path in `_PHASE7_BYTE_EDIT_ALLOWLIST` or (ii) a path matching any `_PHASE7_OWNED_NEW_TREES` glob at any depth, `_compute_phase7_violations(diff)` returns `[]`. Parametrize over ≥ 8 (status, path) tuples spanning both escape hatches; a hypothesis strategy is welcome but not required.
- [ ] **AC-11.b — locked-surface-caught property** For any diff whose every entry is under a `_LOCKED_SURFACE_GLOBS` prefix AND not in `_PHASE7_BYTE_EDIT_ALLOWLIST` AND not matching a `_PHASE7_OWNED_NEW_TREES` glob, ALL N entries appear in the violation list (no silent truncation, complements AC-5.l).
- [ ] **AC-11.c — monotonicity** Removing any row from `_PHASE7_BYTE_EDIT_ALLOWLIST` cannot DECREASE the violation-set size for a fixed diff. Catches an implementation that accidentally treats the allowlist as a *deny*-list.
- [ ] **AC-11.d — idempotency** `_file_is_violating(status, path)` returns the same value on repeated calls with identical arguments. Catches hidden mutable state (e.g., a stray `functools.lru_cache` on the wrong function; a module-level list that grows on each call).
- [ ] **AC-11.e — module docstring names the "necessary-not-sufficient" complement** The fence file's module docstring MUST contain the literal strings: `"necessary not sufficient"`, `"semantic drift"`, and `"make check"`. Cites Phase 7 ADR-0009 §Consequences verbatim on this. Verified by a docstring-scan test (mirrors AC-1).

**Terminal-allowlist guard (AC-12) — production ADR-0043**
- [ ] **AC-12** Enforces production ADR-0043 §Decision: Phase 7 is the last per-phase enumerated allowlist *file*. Two complementary sub-checks (filename glob alone is fragile — a copy-cat file named `test_phase8_locked_kernel_paths.py` would sneak past):
  - **AC-12.a — filename glob** `test_this_is_the_terminal_byte_edit_allowlist_file` globs `tests/fence/*.py` for the pattern `*no_byte_edits*` (case-sensitive) and asserts exactly one match — this file.
  - **AC-12.b — symbol scan** `test_this_is_the_terminal_byte_edit_allowlist_symbol` uses `ast.parse` on every `tests/fence/*.py` module and looks for module-level `Assign` nodes whose target `Name` matches the regex `^_PHASE\d+_BYTE_EDIT_ALLOWLIST$`. Asserts exactly ONE such symbol exists across the whole `tests/fence/` tree. A future engineer who names their file cleverly is still caught by the symbol scan.
  - The test docstring cites ADR-0043 and states that new frozen surfaces use a contract + snapshot test (the probe-ABC pattern in `tests/unit/test_probe_contract.py` is the precedent shape), not allowlist rows.

## Implementation outline

1. **Identify the Phase 6.5 baseline SHA per Preconditions §2.** Walk `git log --author-date-order --oneline main` for the most-recent commit whose message names a Phase 6.5 story-id that is `HARDENED`-or-`Done`. Write the 40-char hex SHA to `tests/fence/_phase6_5_baseline.txt`; add a comment sidecar (e.g., `_phase6_5_baseline.md` or an inline comment header in the `.txt` — mirror whatever `_phase3_baseline.txt` does) recording the story-id and one-line justification. Commit on a branch that the fence's `_ensure_baseline_reachable` can resolve.
2. **Determine State A vs State B per Preconditions §3.** Run `git merge-base --is-ancestor <S19-01-landing-SHA> <phase-6.5-baseline-SHA>`: exit 0 → State A (S19-01 pre-baseline; 8 Layer C/G files need NO rows); exit 1 → State B (add the R30 block). Record the state and both SHAs in `_attempts/S5-01.md` before writing code.
3. **Amend `_BASELINES` in `tests/fence/test_kernel_frozen.py`** (one-row append: `("phase-6_5", Path("tests/fence/_phase6_5_baseline.txt"))`). `test_kernel_frozen.py` lives under `tests/fence/`, which is out of scope for THIS fence's path-filter — no `_PHASE7_BYTE_EDIT_ALLOWLIST` row needed for this edit. Add a defensive parametrized test row proving `tests/fence/foo.py` classifies as out-of-scope.
4. **Write the new fence file** `tests/fence/test_phase7_no_byte_edits_to_locked_files.py`:
   - Module docstring (AC-1 strings + AC-4.a/b/c + AC-11.e strings embedded).
   - `DiffStatus: TypeAlias = Literal["M", "D", "R", "A", "C", "T", "U"]` — closes primitive obsession on git-status letters. `_parse_status(letter: str) -> DiffStatus` smart-constructor raises on unexpected letters (fail-loud on git-behavior drift).
   - `@dataclass(frozen=True) class Violation: status: DiffStatus; path: Path; reason: Literal["not_in_allowlist"]` — anaemic-dataclass fix; the `reason` field lets AC-6 enumerate specifically why a row failed (single reason today, but the seam is present for future extension without a schema break).
   - `_EXPECTED_ROW_COUNT: Final[int]` — the AC-2.a invariant. Value = 10 (State A) or 18 (State B) at S5-01 landing; grows by ADR amendment.
   - `_ROW_SOURCE_ADRS: Final[tuple[Path, ...]]` — pointers to the ADR files whose `## Decision` section AC-2.b's parser reads.
   - `_PHASE7_BYTE_EDIT_ALLOWLIST: Final[frozenset[Path]]` with per-row `# row N — ADR-XXXX` comments (AC-2).
   - `_LOCKED_SURFACE_GLOBS: Final[tuple[str, ...]]` = `("src/codegenie/**", "plugins/vulnerability-remediation--node--npm/**", "pyproject.toml", "Makefile")`. Note: `plugins/PLUGINS.lock` is EXCLUDED (AC-4.a); `src/codegenie/schema/repo_context.schema.json` is included by the `src/codegenie/**` prefix.
   - `_PHASE7_OWNED_NEW_TREES: Final[tuple[str, ...]]` = `("plugins/distroless-migration--node--npm/**", "src/codegenie/primitives/vuln_provenance/**")`.
   - Copy `_run_git_diff` and `_ensure_baseline_reachable` from `tests/fence/test_kernel_frozen.py` verbatim, with a leading comment: `# duplicate-of: test_kernel_frozen.py::_run_git_diff — Rule 2, extract on third instance; per production ADR-0043 there is no third.` This is intentional duplication; do NOT extract to a shared `tests/fence/_git.py` (see Notes).
   - `def _matches_glob(path: Path, patterns: tuple[str, ...]) -> bool`: uses `fnmatch.fnmatchcase(path.as_posix(), pattern)` (AC-4.b — pinned matcher).
   - `def _file_is_violating(status: DiffStatus, path: Path) -> bool`: pure predicate — returns True iff path matches `_LOCKED_SURFACE_GLOBS`, is NOT in `_PHASE7_BYTE_EDIT_ALLOWLIST`, and does NOT match `_PHASE7_OWNED_NEW_TREES`.
   - `def _compute_phase7_violations(diff: list[tuple[str, Path]], allowlist=_PHASE7_BYTE_EDIT_ALLOWLIST, locked_globs=_LOCKED_SURFACE_GLOBS, owned_new_trees=_PHASE7_OWNED_NEW_TREES) -> list[Violation]`: pure function, one code path for the live check AND the DI'd tests.
   - `def _diff_against_phase6_5_baseline(diff_source: Callable[[], list[tuple[str, Path]]] | None = None) -> list[Violation]`: the caller-facing wrapper. If `diff_source` is None, runs real git diff; otherwise substitutes the injected function (used by AC-5).
   - `_parse_decision_section(adr_path: Path) -> list[tuple[int, Path]]`: parser scoped to `## Decision` heading, walks to next `##`, extracts numbered list items only. Returns `(row_number, path)` tuples. Meta-tested by `test_adr_parser_ignores_decoy_numbered_lists` (AC-2.b).
   - Test functions matching every AC above.
5. **Run the live check** — should pass on the current diff at story landing. Per Preconditions §1, rows tied to `plugins/vulnerability-remediation--node--npm/**` match no files while S3-03 is blocked; AC-9 passes trivially there and the mechanism-proof rests on AC-5.
6. **Capture out-of-test planted-violation evidence per AC-5.i's schema** (three lines with `RED:` / `REMOVAL:` / `GREEN:` prefixes). Append to `_attempts/S5-01-phase7-byte-edit-allowlist-fence.md`. `test_attempt_log_has_planted_violation_evidence` (AC-5.i.evidence-check) will then pick it up.
7. **Run the full test suite** (`make check`) — Phase 3–6.5 regression suite green; no other fence regresses.

## TDD plan (red → green → refactor)

**Red:**
1. Write `tests/fence/test_phase7_no_byte_edits_to_locked_files.py` with all ACs above (including AC-2.a/b/c, AC-4.a/b/c, AC-5.a–m, AC-11.a–e, AC-12.a/b). On a scratch branch, plant a real `M` edit to `src/codegenie/coordinator/coordinator.py` (trailing comment). Run `pytest tests/fence/test_phase7_no_byte_edits_to_locked_files.py -v` — expect `test_no_byte_edits_outside_allowlist` red with a helpful error message naming the coordinator path AND `ADR-0009` (AC-6).
2. Confirm the DI-injected planted-violation cases (AC-5.a–m) are ALSO red — they stay red-by-construction on every CI run (the injection is inside the test module, not the working tree).
3. Confirm `test_adr_parser_ignores_decoy_numbered_lists` (AC-2.b meta) is green even without the fence being live — it tests the parser purely.

**Green:**
1. Remove the planted `coordinator.py` edit.
2. Run `pytest tests/fence/test_phase7_no_byte_edits_to_locked_files.py -v` — `test_no_byte_edits_outside_allowlist` now green; DI-injected tests remain green (they use `diff_source`).
3. Run `make check` — Phase 3–6.5 regression suite green; no other fence regresses.
4. Capture the AC-5.i evidence block (RED/REMOVAL/GREEN SHA lines) in `_attempts/S5-01-phase7-byte-edit-allowlist-fence.md`. Re-run `pytest` — `test_attempt_log_has_planted_violation_evidence` (AC-5.i.evidence-check) now green.

**Refactor:**
1. Confirm `_compute_phase7_violations` is the single code path shared by the live check AND all DI-injected tests. Verify via AC-5.m (`test_live_check_delegates_to_pure_helper`).
2. Confirm `ruff check` + `ruff format --check` + `mypy --strict` clean (AC-10).
3. Sort `_PHASE7_BYTE_EDIT_ALLOWLIST` rows in ADR-0009 numerical order (rows 1–10) then ADR-0030's R30 block (rows 11–18, when applicable) then Amendment-A blocks — engineers reading the fence file see the row numbers in the same order as the ADRs.
4. Confirm the `# duplicate-of: test_kernel_frozen.py::_run_git_diff` comment is present on the copied helpers (per Notes) — a code reviewer should not "helpfully" DRY these into a shared module.

## Files to touch

- `tests/fence/test_phase7_no_byte_edits_to_locked_files.py` — new test file (this story's load-bearing artifact).
- `tests/fence/_phase6_5_baseline.txt` — new baseline file (one-line SHA + comment sidecar).
- `tests/fence/test_kernel_frozen.py` — `_BASELINES` tuple gains one row for Phase 6.5 (Open/Closed at the tuple boundary). Note: this edit is to a `tests/fence/` file, NOT a Phase 0–6.5 locked-surface path, so it does not require an ADR-0009 allowlist row. (A defensive `("M", "tests/fence/foo.py") → not-a-violation` parametrized case in AC-4/AC-5's test matrix proves this classification.)
- `.github/CODEOWNERS` — add explicit coverage for `tests/fence/_phase6_5_baseline.txt` if not already transitive (verify with `gh api ... /codeowners`; one-line edit if needed — this file is outside the Phase 7 byte-edit allowlist scope as CODEOWNERS is not under `src/codegenie/`).
- `docs/phases/07-migration-task-class/stories/_attempts/S5-01-phase7-byte-edit-allowlist-fence.md` — append-only attempt log with the RED/REMOVAL/GREEN evidence block per AC-5.i.

## Out of scope

- **The actual Phase 6.5 baseline SHA value** — picked at implementation time per Preconditions §2. Do not hardcode here; the implementer resolves via `git log` at story landing time.
- **Semantic verification of the allowlisted edits.** That's `make check` + `bench/vuln-remediation/` cassette replay's job. This fence only catches *unallowlisted byte-edits* — necessary, not sufficient. AC-11.e's docstring assertion names this explicitly.
- **`PLUGINS.lock` row for the new plugin** — that's S5-04. AC-4.a EXPLICITLY excludes `plugins/PLUGINS.lock` from `_LOCKED_SURFACE_GLOBS` to avoid double-coverage with S5-04's dedicated hash-verification fence.
- **Plugin-directory probe-placement fence** — that's S5-02.
- **Import-linter contracts for the primitive + plugin tree** — that's S5-03.
- **ADR-0029 Amendment-A row-growth** — those 8 row-categories land alongside their owning stories (Steps 13–18), each row gated by ADR review. S5-01 does NOT pre-emptively enumerate them; instead, `_EXPECTED_ROW_COUNT` + `_ROW_SOURCE_ADRS` grow additively when each Amendment-A story lands.
- **Phase 8+ per-phase enumerated allowlist FILES or SYMBOLS — there are none.** Per [production ADR-0043](../../../production/adrs/0043-extension-by-addition-means-no-silent-edits.md), Phase 7's allowlist is the *last* per-phase enumerated allowlist file/symbol; no future phase adds a `_PHASE8_BYTE_EDIT_ALLOWLIST` symbol or a `test_phase8_no_byte_edits_*.py` fence. `_PHASE7_BYTE_EDIT_ALLOWLIST` is **terminal in file/symbol identity** — do NOT design it for a `_PHASE8_BYTE_EDIT_ALLOWLIST` successor. AC-12.a + AC-12.b enforce this via file glob AND symbol scan. Row growth *within this file* by ADR amendment remains sanctioned (see Notes on the "terminal" nuance).
- **Frozen-surface contracts in Phase 8+** — those use the probe-ABC contract + snapshot test pattern (`tests/unit/test_probe_contract.py` byte-pins `Probe.__init_subclass__` against `docs/localv2.md §4`), not another allowlist. Cite the pattern in this fence's docstring so a Phase 8+ engineer looking here for precedent finds the right seam.

## Notes for the implementer

- **ADR-0009 §Decision is the source of truth for the original 10 rows; ADR-0030 §Decision for the R30 block; ADR-0029 §Decision for Amendment-A rows.** The fence's `_PHASE7_BYTE_EDIT_ALLOWLIST` must match those ADRs byte-for-byte. AC-2.b's ADR-parsing test exists specifically to prevent silent drift; if you find the ADR text needs to change, amend the ADR first, then update the fence (never the reverse) — Rule 7 (surface conflicts, don't average them).
- **`api.py` vs `adapters/npm_provenance.py` distinction is load-bearing.** Row 1 names `adapters/npm_provenance.py` only. The Phase 3 plugin's `api.py` is NOT allowlisted by row 1 — if S3-03's wiring touches `api.py`, that's a separate question (and a separate row would be needed, which would require an ADR amendment). AC-5.c verifies this is caught.
- **Phase-7-owned new trees fall OUTSIDE the fence's scope, not INSIDE the allowlist.** The distinction matters: the allowlist is for byte-edits to *existing* files; new files under `plugins/distroless-migration--node--npm/**` and `src/codegenie/primitives/vuln_provenance/**` are unconstrained (they are the Phase-7 ownership surface). The fence's path-filter must include this exclusion — verified by AC-5.h.ii (the discriminating case that exercises the seam specifically, not just the redundant AC-5.h.i case).
- **CODEOWNERS is the social anchor (Phase 3 ADR-0011 carry-forward).** The fence catches accidents and "while I'm here" edits; a determined adversary editing both the fence file and the violation defeats it. Acceptable; the cost of stronger guarantees (cryptographic attestation) is deferred to the Sigstore migration in production ADR-0011 / Phase 11.
- **Order of operations during the in-test planted-violation matrix:** because the `diff_source` parameter is dependency-injected, the synthetic-injection tests do NOT mutate the working tree. This avoids the test-leakage problem where a parametrized test leaves the repo in a dirty state. **Do not be tempted to use `subprocess.run(["git", "checkout", ...])` in the tests** — `subprocess.run` with `shell=True` is banned (forbidden-patterns hook) and the dependency-injected path is cleaner.
- **`make fence` collects this transitively** via the existing `pytest tests/unit/test_pyproject_fence.py tests/fence/` recipe; no Makefile edit required (and Makefile is not in the allowlist).
- **Terminal allowlist (production ADR-0043) — nuanced.** `_PHASE7_BYTE_EDIT_ALLOWLIST` is the *last* per-phase enumerated allowlist *file* / *symbol*. No future phase adds a `_PHASE8_BYTE_EDIT_ALLOWLIST` symbol or a second `test_phase8_no_byte_edits_*.py` fence. **Row growth *within this file* by ADR amendment (ADR-0029, ADR-0030) remains sanctioned** — each row cites an ADR and the audit trail stays intact. Do NOT shape the test layout for a `_PHASE8_BYTE_EDIT_ALLOWLIST` successor and do NOT add a phase-extension seam. AC-12.a + AC-12.b's guards fail if a second allowlist file OR a second allowlist symbol appears. Later phases protect new frozen surfaces with a contract + snapshot test (the probe-ABC pattern: `tests/unit/test_probe_contract.py` byte-pins `Probe.__init_subclass__` against `docs/localv2.md §4`), never another allowlist file.
- **Anti-pattern explicitly avoided — no `Fence` ABC extraction.** The kernel-frozen fence (`test_kernel_frozen.py`) and the byte-edit allowlist fence (this file) share *category* (both are git-diff-against-baseline audit-and-lint fences) but their allowlist *shape* differs (kernel uses `_KERNEL_SCOPE_DIRS` + `_TOP_LEVEL_PHASE3_PACKAGES` prefix walker; Phase 7 uses `fnmatch` globs on POSIX paths — see AC-4.b). Per ADR-0043 there will be NO third such fence. Under Rule 2 ("three similar lines is better than premature abstraction") extraction earns its rent only on the third consumer — and that third consumer will never exist. Correct final state: **two fence files that share ~40 lines of copy-pasted git-diff helpers, each carrying a `# duplicate-of:` comment naming the twin.** Treat the duplication as permanent-by-design. (Correction to a prior draft that said "one walks a baseline diff; the other walks an AST" — that comparison confused the kernel-frozen fence with `src/codegenie/_phase3_fence.py`, which is a separate AST walker unrelated to this discussion.)
- **`_LOCKED_SURFACE_GLOBS` divergence from `_KERNEL_SCOPE_DIRS` is deliberate.** The precedent uses `Path` prefix-match because Phase-3 kernel scope is essentially "the src/codegenie kernel dirs". Phase 7's scope crosses `src/`, `plugins/`, and top-level files — globs match the actual shape more legibly. Pinned matcher: `fnmatch.fnmatchcase` on POSIX-normalised paths (AC-4.b); do NOT reach for a regex or a `PurePath.match` walker.
- **`Violation` dataclass is `frozen=True` and typed with `Literal` for `reason`** — no `dict[str, Any]` primitive shuffling. Extending the reason enum is a schema change; add the new literal and grow the test cases (Open/Closed at the value boundary).
- **Surface conflicts (Rule 7):** if at implementation time you find that ADR-0009's rows have drifted from reality (e.g., S6-01 landed `Role` under `src/codegenie/sandbox/types.py` instead of `src/codegenie/sandbox/__init__.py`), STOP and surface the drift in `_attempts/S5-01.md`. The fix is an ADR amendment to ADR-0009, not a silent edit to the row text.
