# Attempt log: S19-01 — Layer C raw-sidecar publishing

## Attempt 1 — 2026-05-21 18:02 ET — SUCCESS

**Approach:** Added the recovered producer/consumer integration oracle first, observed the unfixed sidecar contract fail, re-applied reverted commit `5055292` via `git revert --no-commit 7f6a009`, then made the kernel edit loud with the ADR-0030 `_KERNEL_ALLOWLIST` block.

**ReAct cycles:** 9

**Red evidence:**
- `pytest tests/integration/test_layer_c_sidecar_contract.py -q --no-cov` before the fix: `3 failed in 6.13s`.
- Failures were the intended oracle:
  - `test_declared_raw_sidecar_inputs_are_produced_by_a_gather`: missing `['dockerfile', 'runtime_trace']`.
  - `test_dockerfile_consumers_populate_on_real_gather`: `entrypoint["confidence"] == "unavailable"`.
  - `test_runtime_trace_consumer_populates_on_real_gather`: `certificate["confidence"] == "unavailable"`.

**Green evidence:**
- `pytest tests/integration/test_layer_c_sidecar_contract.py -q --no-cov`: `3 passed in 5.50s` after re-applying `5055292`; `3 passed in 5.83s` after formatter cleanup.
- Targeted story regression suite: `pytest tests/integration/test_layer_c_sidecar_contract.py tests/unit/probes/layer_c/test_dockerfile.py tests/unit/probes/layer_c/test_runtime_trace.py tests/unit/probes/layer_b/test_index_health_probe.py tests/unit/probes/layer_g/test_semgrep.py -q --no-cov`: `138 passed in 6.14s`.

**Kernel fence red→green evidence:**
- Because `test_kernel_frozen.py` diffs baseline against committed `HEAD`, the uncommitted worktree could not produce the required red. I made a temporary local commit without the allowlist, then ran `pytest tests/fence/test_kernel_frozen.py::test_phase3_has_not_modified_phase012_kernel_outside_allowlist -q --no-cov`.
- Expected red: `1 failed in 0.10s`; violations listed exactly the eight ADR-0030 files: `certificate.py`, `cve.py`, `dockerfile.py`, `entrypoint.py`, `runtime_trace.py`, `sbom.py`, `shell_usage.py`, `semgrep.py`.
- After adding the ADR-0030 block: same fence test `1 passed in 0.10s`; `PATH="$PWD/.venv/bin:$PATH" make fence`: `372 passed in 29.27s` and later `372 passed in 31.36s` inside `make check`.

**Validator report:**

### Per-AC results
- [x] AC-1: DockerfileProbe writes `raw/dockerfile.json` on absent and parsed paths → PASS — targeted unit tests passed; integration test produced the declared sidecar.
- [x] AC-2: RuntimeTraceProbe funnels exits through sidecar-publishing `_finalize()` → PASS — targeted unit tests passed; integration test proved `certificate` consumed `runtime_trace`.
- [x] AC-3: Layer C consumers dispatch in the `runs_last` tail → PASS — `test_index_health_probe.py` targeted suite passed; `@register_probe(..., runs_last=True)` re-applied.
- [x] AC-4: Semgrep zero-loaded-rules scan reports `config_absent` skip → PASS — `test_semgrep.py` targeted suite passed.
- [x] AC-5: Goldens and `docs/get-started.md` from `5055292` restored → PASS — re-applied diff includes the expected docs and golden files.
- [x] AC-6: Full `5055292` diff restored → PASS — the re-applied source/test/docs/golden change matches the 32-file `git show 5055292 --stat` surface; this story adds only ADR/story docs, the integration oracle, and the fence allowlist.
- [x] AC-7: Kernel allowlist gains exactly eight ADR-0030 paths → PASS — `tests/fence/test_kernel_frozen.py` diff is only one grouped block with eight `Path(...)` entries.
- [x] AC-8: Kernel fence red before allowlist and green after → PASS — temporary commit captured red; final allowlist captured green.
- [x] AC-9: Fence logic untouched → PASS — diff only adds the eight-entry block and comment.
- [x] AC-10: Integration test exists with the three recovered tests and helpers → PASS — `tests/integration/test_layer_c_sidecar_contract.py`.
- [x] AC-11: Declared raw sidecars are produced by real gather → PASS — integration test red→green.
- [x] AC-12: Dockerfile consumers populate from real gather → PASS — integration test red→green.
- [x] AC-13: Runtime trace consumer populates from real gather → PASS — integration test red→green.
- [x] AC-14: Integration tests fail pre-fix and pass post-fix → PASS — red and green outputs recorded above.
- [x] AC-15: Gates green → PASS — `make check`, `make lint-imports`, and `make docs` passed.

### Gates
- `PATH="$PWD/.venv/bin:$PATH" make check`: PASS — `5981 passed, 40 skipped, 4 deselected, 9 xfailed, 10 warnings`, then fence lane `372 passed`.
- `PATH="$PWD/.venv/bin:$PATH" make lint-imports`: PASS — 6 contracts kept, 0 broken.
- `PATH="$PWD/.venv/bin:$PATH" make docs`: PASS — docs built in 31.12 seconds.
- Design quality: PASS — story reinforces existing probe registry, `runs_last` scheduling, functional-core/imperative-shell sidecar writers, and the existing kernel-fence data allowlist. No new broad exception handling, raw semantic IDs, coordinator branching, or speculative abstraction introduced.

**CI follow-up after initial push:**
- GitHub `ci` for commit `08ed220` failed only in `portfolio (3.11, ubuntu-24.04)`. The failure was not the Layer C sidecar story; it was a pre-existing scheduling race where `semantic_index_meta` could read the transient `tsconfig.json` created by `scip-typescript --infer-tsconfig`.
- Red evidence: `pytest tests/unit/coordinator/test_coordinator_sort_order.py::test_runs_last_tail_waits_for_non_tail_rest_even_with_parallel_budget -q --no-cov` failed because the `runs_last` reader started before the non-tail rest sidecar writer ended.
- Green evidence: `ScipIndexProbe` now registers `runs_last=True`, coordinator dispatches the `runs_last` tail after non-tail rest probes, and `pytest tests/integration/portfolio/test_portfolio_sweep.py -q --no-cov --tb=short` passes locally (`1 passed in 29.20s`).

**Refactor decisions:**
- Reused the known-good `5055292` implementation verbatim instead of redesigning the sidecar contract; this preserved the story's surgical regression-rescue scope.
- Kept the integration test as an end-to-end contract, not a unit mock, because the original bug lived at the producer/consumer seam.
- Added a data-only `_KERNEL_ALLOWLIST` block rather than changing fence logic; mechanical-policy enforcement remains unchanged.

**Final files touched:**
- `docs/get-started.md` — restored `5055292` FAQ entries.
- `src/codegenie/probes/layer_c/{certificate,cve,dockerfile,entrypoint,runtime_trace,sbom,shell_usage}.py` — restored sidecar publishing and consumer `runs_last` behavior.
- `src/codegenie/probes/layer_g/semgrep.py` — restored loaded-rules classification.
- `tests/golden/probes/{certificate,entrypoint,shell_usage}/*.json` — restored non-broken Layer C baselines.
- `tests/unit/**` — restored `5055292` unit coverage.
- `tests/integration/test_layer_c_sidecar_contract.py` — added permanent seam regression oracle.
- `tests/fence/test_kernel_frozen.py` — added ADR-0030 eight-file allowlist block.
- `src/codegenie/probes/layer_b/scip_index.py` and `src/codegenie/coordinator/coordinator.py` — CI follow-up: made the SCIP inferred-tsconfig artifact invisible to concurrent tsconfig readers while preserving `index_health` sidecar ordering.
- `tests/unit/coordinator/test_coordinator_sort_order.py` and `tests/unit/probes/layer_b/test_scip_index.py` — CI follow-up regression coverage.
- `docs/phases/03-vuln-deterministic-recipe/ADRs/0014-scip-index-probe-removes-infer-tsconfig-artifact.md` — documented cleanup plus scheduling as the complete fix.
- `docs/phases/07-migration-task-class/ADRs/0030-amend-kernel-allowlist-for-layer-c-sidecar-publishing.md` and README indexes — documented the amendment.
- `docs/phases/07-migration-task-class/stories/S19-01-layer-c-sidecar-publishing.md` and README indexes — documented and marked the story complete.

**Tests added:**
- `tests/integration/test_layer_c_sidecar_contract.py::test_declared_raw_sidecar_inputs_are_produced_by_a_gather` — AC-10/11/14.
- `tests/integration/test_layer_c_sidecar_contract.py::test_dockerfile_consumers_populate_on_real_gather` — AC-10/12/14.
- `tests/integration/test_layer_c_sidecar_contract.py::test_runtime_trace_consumer_populates_on_real_gather` — AC-10/13/14.

**Documentation updated:**
- `docs/get-started.md`
- `docs/phases/07-migration-task-class/ADRs/README.md`
- `docs/phases/07-migration-task-class/ADRs/0030-amend-kernel-allowlist-for-layer-c-sidecar-publishing.md`
- `docs/phases/07-migration-task-class/stories/README.md`
- `docs/phases/07-migration-task-class/stories/S19-01-layer-c-sidecar-publishing.md`

**Deviations from the story spec:**
- None in behavior. The integration test was formatted by `ruff format`, so its whitespace is project-canonical rather than byte-for-byte markdown appendix whitespace.

**Follow-ups surfaced this attempt:**
- Phase 7 S5-01 must enumerate the eight ADR-0030 files when it lands `tests/fence/test_phase7_no_byte_edits_to_locked_files.py`, per ADR-0030 consequences.
