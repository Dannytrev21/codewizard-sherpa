# Contributing

<!-- TODO(S5-02): replace this placeholder with the real contributor docs. -->

This page is a placeholder so the curated `mkdocs.yml` `nav` resolves under
`mkdocs build --strict` from Phase 0 onward (story S1-04 AC-10). The real
contributor onboarding (development environment setup, branch policy, PR
template, the `make check` chain, the SHA-pinned pre-commit hook flow) lands
in story S5-02 of Phase 0.

Until then, see:

- The repository-level `README.md` for bootstrap and `make` targets.
- `docs/phases/00-bullet-tracer-foundations/` for the architecture and the
  current milestone.
- `.pre-commit-config.yaml` for the local enforcement layer.
- `CLAUDE.md` for the project's coding conventions and load-bearing
  architectural commitments.

`TODO(S5-02)`
